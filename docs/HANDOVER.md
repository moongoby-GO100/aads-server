# AADS HANDOVER
최종 업데이트: 2026-08-31

## 2026-08-31
- AADS-RUNNER-PUSHFAIL-DIAGNOSTICS-P0-FOLLOWUP-20260831: 13:28 KST 기준 `runner-18894b34` 배포 실패 후속 조치. 원인은 AADS 서버 대상 작업인데도 대시보드 최근 커밋 1시간 이내 조건(`DASHBOARD_LAST_COMMIT`/`DIFF_SECONDS`) 때문에 `aads-dashboard/deploy.sh`가 실행된 점으로 확인했다. `scripts/pipeline-runner.sh`와 `.local`에서 최근 커밋 휴리스틱을 제거하고, `target_repo=aads-dashboard` 또는 명시적 대시보드 변경일 때만 대시보드 Blue-Green/QA를 수행하게 했다. 재발 방지로 `tests/unit/test_pipeline_runner_worktree_policy.py`에 최근 커밋 기준 대시보드 배포 금지 회귀 테스트를 추가하고, `.gitignore`에 `.tmp*/.codex_tmp*/tmp_*` 및 `pc_agent_collection_queue.json`을 추가했다. `runner-18894b34`가 잘못 추적한 GO100 임시 패치/리포트/숫자 파일/PC Agent 큐 파일은 보정 커밋에서 Git 추적 제거 대상으로 정리했다. 검증: `bash -n scripts/pipeline-runner.sh`, `bash -n scripts/pipeline-runner.sh.local`, `python3 -m pytest -q tests/unit/test_pipeline_runner_worktree_policy.py tests/unit/test_pipeline_runner_script_guards.py`, `git diff --check` 기준.
- AADS-RUNNER-PUSHFAIL-DIAGNOSTICS-P0-20260831: Pipeline Runner의 승인/배포 Git 경계를 isolated worktree 기준으로 강화했다. 작업 완료 후 `awaiting_approval` 전 `/tmp/aads-wt-{job_id}` 자체에서 커밋하고 `pipeline_jobs.commit_hash`에 40자리 SHA를 저장하며, SHA가 비었거나 worktree HEAD와 다르면 `approval_commit_sha_*` 오류로 중단한다. 승인 후에는 worktree 경로/공유 main과의 분리/저장 SHA 일치를 재검증하고, 공유 main 작업트리에 변경을 복사하거나 그곳에서 push하지 않고 검증된 worktree HEAD를 `origin/main`으로 push한다. push 성공/실패 모두 exit code, branch, HEAD SHA, 마스킹된 origin URL, safe status, stdout/stderr를 `pipeline_jobs.logs`에 저장하고, 실패 시 같은 마스킹 진단 요약을 `error_detail`에 남긴다. Admin Task API는 과거 `phase=push_fail`인데 상세 원인이 없는 row(예: runner-e215901c 계열)에 원인 미기록 안내를 보강해 대시보드에서 빈 원인으로 보이지 않게 했다. 스키마는 `migrations/138_pipeline_runner_commit_gate.sql`에 `commit_hash` 컬럼/인덱스를 추가했다. 검증: `bash -n scripts/pipeline-runner.sh`, `python3 -m pytest -q tests/unit/test_pipeline_runner_worktree_policy.py tests/unit/test_pipeline_runner_script_guards.py` 22 passed, `git diff --check` 통과. 롤백: `scripts/pipeline-runner.sh`와 `.local`에서 approval commit/SHA gate 및 isolated push 블록을 이전 승인 후 main-copy commit/push 흐름으로 되돌리고, `app/api/admin.py`의 `_admin_error_detail` 적용을 제거한다. `commit_hash`는 하위 호환 가능한 nullable 컬럼이므로 남겨도 되며, 완전 롤백 시 해당 인덱스와 컬럼을 순서대로 제거한다.
- AADS-OPS-SERVERS-PERF-POWERSHELL-20260831: 12:56 KST 기준 CEO 지시(`https://aads.newtalk.kr/ops/servers 각 서버 성능확인해서 카드에 반영`, `내 PC에서 각 서버에 Windows PowerShell로 접속`)에 따라 서버 성능 카드와 PC Agent PowerShell 접속 경로를 보강했다. 실측: contabo116 메모리 5,700/24,031MB, 디스크 162G/193G(84%), load 5.71/5.97/7.22; contabo14 메모리 10,204/24,031MB, 디스크 201G/387G(52%), load 11.68/11.58/11.22; cafe24_114 메모리 2,614/15,919MB, 디스크 768G/875G(89%), load 1.03/1.07/1.07. 백엔드 `app/api/ops.py`는 `/api/v1/ops/server-health/{server_id}`에서 원격 9090 헬스 서버 실패 시 SSH/local `free -m`, `df -h /`, `uptime`, `/proc/meminfo`, `/proc/loadavg` 폴백으로 카드용 성능 수치를 반환한다. `app/services/pc_agent_manager.py`는 PowerShell 명령을 UTF-16LE `-EncodedCommand`로 전달해 파이프/복합 명령이 깨지지 않게 수정했다. 대시보드 `src/app/ops/servers/page.tsx`는 전 서버 헬스 API에 인증 헤더를 붙이고 디스크/메모리/load 카드, SSH 명령, PC Agent 기반 `PowerShell` 실행 버튼을 표시한다. `src/lib/api.ts`에는 `/pc-agent/route-execute` 클라이언트를 추가했다. CEO PC `oby-ceo`에서 Windows PowerShell 5.1, OpenSSH 클라이언트, 116:22/14:22/114:7916 TCP 접근을 확인했다. 검증: `python3 -m py_compile app/api/ops.py app/services/pc_agent_manager.py` 성공, 컨테이너 py_compile 성공, 컨테이너 함수 호출로 contabo116 성능 폴백 반환 확인, 대시보드 `npx eslint src/app/ops/servers/page.tsx` 성공, `npm run build` 성공. 후속으로 선별 커밋, 푸시, 서버/대시보드 배포, `/ops/servers` 화면 캡처와 API/PC Agent 재검증을 진행한다.

## 2026-08-30
- AADS-ANDROID-OHVIS-APP-UX-P1-20260830: 18:40 KST 기준 CEO 지시(`아이콘 앱이름을 오비스`, `앱 실행 즉시 오비스 채팅`, `세션 만료 시 로그인`, `오비스어드민 앱설정 메뉴`, `사용자 중심 UI/UX 글로벌 반영`)에 따라 Android 앱과 대시보드, 글로벌 프롬프트 자산을 보강했다. Android는 `AndroidManifest.xml`에 `@drawable/ic_ohvis_launcher` 런처/라운드 아이콘을 지정하고 신규 `ic_ohvis_launcher.xml`을 추가했다. `MainActivity`는 일반 앱 실행 시 즉시 `https://aads.newtalk.kr/chat` WebView를 열고, 페어링/서비스/권한/음성 설정은 `Show Settings` 뒤로 접어 사용자 첫 화면을 채팅 중심으로 정리했다. `app/api/device.py` manifest는 앱 이름 `오비스`, `launch_route=/chat`, `expired_session_route=/login?next=/chat&reason=session_expired`, `admin_settings_route=/ops/mobile-agent`를 내려준다. 대시보드는 `src/components/Sidebar.tsx`에 관리자 메뉴 `오비스 앱 설정`을 추가하고 `/admin/app-settings`를 `/ops/mobile-agent`로 redirect하며, 설치 화면을 앱 실행 흐름/세션 복구/wake link 확인 화면으로 보강했다. 글로벌 지침은 `migrations/136_global_user_centered_ux_contract.sql`로 `prompt_assets.slug='global-user-centered-product-ux-contract'` L1 자산에 저장했고 운영 DB 적용 후 `enabled=true`, `workspace_scope=*`, `chars=803`을 확인했다. 후속 검증/커밋/푸시/배포 결과는 최종 보고에 분리 기록한다.
- AADS-ANDROID-OHVIS-VOICE-WAKE-P1-20260830: 07:04 KST 기준 CEO 지시(`빅스비 연결`, `오비스 자체 깨우기`, `현재 음성 인식 기능 에러 조치`, `배포 및 결과 보고`)에 따라 Android Agent에 OHVIS 음성 깨우기 P1을 구현했다. 조치: 릴리즈 `AndroidManifest.xml`에 `RECORD_AUDIO`와 `FOREGROUND_SERVICE_MICROPHONE` 권한, `dataSync|microphone` foreground service type, `ohvis://wake`/`aads-agent://wake` 딥링크, Android shortcut `ohvis_wake`를 추가했다. 앱 라벨은 `오비스`로 변경했다. `VoiceWakeController`/`VoiceWakeState`를 추가해 foreground service 상태에서 Android `SpeechRecognizer`가 `오비스`, `ohvis`, `obis`, `aads` 호출어를 감지하면 `MainActivity`를 `ohvis://wake?source=voice`로 전면 표시한다. `MainActivity`에는 Start Wake/Stop Wake/Bixby Wake UI를 추가했고, WebSocket capability와 서버 device routing에는 `voice_wake_start`, `voice_wake_stop`, `voice_wake_status`를 추가했다. `app/api/device.py`는 APK manifest의 version/version_code를 `android_agent/app/build.gradle`에서 읽고 `voice_wake_deep_links`, `bixby_quick_command`, `voice_wake_capabilities`를 내려주도록 보정해 다운로드 메타데이터 불일치와 설치 안내 누락을 막는다. 문서 `android_agent/README.md`에는 Bixby Quick Command 연결명, 딥링크, 한계(Android OS hotword 대체 아님), 권한 조건을 기록했다. 검증: `.venv-playwright/bin/python -m pytest -q tests/unit/test_voice_service.py tests/unit/test_android_voice_wake_release_guard.py` 9 passed, `python3 -m py_compile app/api/voice.py app/services/voice_service.py app/services/pc_agent_manager.py` 성공, `git diff --check` 성공, `./build_debug_apk.sh` 성공(`dist/aads-agent-debug.apk` 1.4M), `./build_release_apk.sh` 성공(`dist/aads-agent-release.apk` 1.1M). 시스템 Python 테스트는 `fastapi` 미설치로 실패해 `.venv-playwright`로 대체했다. 연결된 Android 디바이스는 0대라 실제 단말 설치/E2E는 미수행했다.

## 2026-08-28
- AADS-BROWSER-TASK-LIVE-VIEW-P0-20260828: 09:28 KST 기준 CEO 지시(`브라우져 실행되는 과정을 화면에 구현`, `오비스에 메뉴반영`, `사용자가 눈으로 각 작업을 확인`)에 따라 Managed Browser Live View를 구현했다. 백엔드는 `migrations/133_browser_task_live_frames.sql`로 `browser_task_live_frames` 최신 프레임 테이블을 추가하고, `app/api/browser_tasks.py`에 `/api/v1/browser-tasks/{task_id}/events`, `/live-frame` 조회/업데이트 API를 추가했다. `app/services/browser_task_gateway.py`는 최신 프레임 upsert, 최근 이벤트 조회, 선택 작업의 PC Agent `browser_screenshot` 기반 best-effort 캡처를 제공한다. 대시보드는 `src/app/browser-tasks/page.tsx`에 작업 선택형 Live View 패널, 3초 단위 선택 작업 캡처/갱신, 최근 실행 이벤트 타임라인을 추가했고 `src/lib/api.ts`에 관련 클라이언트 함수를 붙였다. 민감 텍스트 값은 기존 `mask_sensitive_value`를 거치며, 화면 이미지는 최신 프레임 중심으로 보관한다. 검증: `python3 -m py_compile app/api/browser_tasks.py app/services/browser_task_gateway.py` 성공, `docker exec -i aads-postgres psql -U aads -d aads -v ON_ERROR_STOP=1 < migrations/133_browser_task_live_frames.sql` 성공, information_schema 14컬럼 확인, 대시보드 `npx tsc --noEmit --pretty false`, `npx eslint src/app/browser-tasks/page.tsx`, 서버/대시보드 `git diff --check` 성공. 후속으로 선별 커밋, 푸시, bluegreen 배포, 공개 `/browser-tasks` 화면 검증을 진행한다.
- AADS-APILESS-BROWSER-RECIPE-SEED-DEPLOY-20260828: 08:53 KST 기준 CEO 지시(`다음단계 커밋 배포까지 진행하고 오비스로그인을 진행해봐`)에 따라 `migrations/133_seed_browser_recipes.sql`을 추가했다. 기본 seed는 `ohvis.login.basic`과 `delivery.ddangyo.sales_collect` 2건이며, 각각 오비스 로그인 smoke 레시피와 땡겨요 승인형 CAPTCHA/업로드 레시피를 제공한다. 운영 DB 적용 명령 `docker exec -i aads-postgres psql -U aads -d aads -v ON_ERROR_STOP=1 -f - < migrations/133_seed_browser_recipes.sql`은 2건 INSERT로 성공했다. 검증: `python3 -m py_compile app/api/browser_recipes.py app/services/browser_recipe_registry.py app/main.py app/services/browser_task_gateway.py app/services/browser_permission_policy.py` 성공, `.venv-playwright/bin/python -m pytest tests/unit/test_browser_task_policy.py` 26 passed. 후속으로 선별 커밋, 푸시, bluegreen 배포, `/api/v1/browser-recipes` API 검증, 오비스 로그인 smoke 검증을 진행한다.
- AADS-APILESS-BROWSER-RECIPE-REGISTRY-P0-20260828: 08:51 KST 기준 CEO 지시(`여러작업의 동시성 확보 리소스배분등 고려해서 기획반영하고 즉시 구현`)에 따라 `docs/plans/20260828_APILESS_AUTHENTICATED_ADMIN_AUTOMATION_PLAN.md`에 멀티 작업 동시성/리소스 배분 설계를 보강했다. 구현: `migrations/132_browser_recipe_registry.sql` 신규 작성 및 운영 DB 적용으로 `browser_recipes`, `browser_recipe_runs`, `browser_artifacts`, `browser_parse_results`, `browser_upload_results` 5개 테이블을 추가했다. `app/services/browser_recipe_registry.py`와 `app/api/browser_recipes.py`를 추가해 BrowserRecipe 저장/조회/dry-run/run-plan/runs API를 구현하고, `app/main.py`에 `/api/v1/browser-recipes` 라우터를 등록했다. recipe는 `concurrency_policy`, `resource_policy`, `runtime_policy`, `version_hash`를 저장하며 dry-run에서 승인 필요 액션, 차단 액션, 런타임, 메모리/시간/artifact 예산을 계산한다. 저장된 recipe는 `browser_recipe_runs` 활성 실행 수와 `concurrency_key` 기준으로 `start`/`queue`/`queue_latest_only`/`reject`를 판정하고, 실행 레코드에 `resource_claim`을 남긴다. 검증: `python3 -m py_compile app/api/browser_recipes.py app/services/browser_recipe_registry.py app/main.py app/services/browser_task_gateway.py app/services/browser_permission_policy.py` 성공, `.venv-playwright/bin/python -m pytest tests/unit/test_browser_task_policy.py` 26 passed, DB information_schema에서 5개 테이블과 핵심 컬럼 존재 확인, `/api/v1/browser-recipes` HTTP 401로 라우터 로드 확인. 이번 조치는 코드/문서/DB 적용까지이며 커밋, 푸시, 정식 bluegreen 배포는 아직 수행하지 않았다.
- AADS-APILESS-AUTH-ADMIN-AUTOMATION-PLAN-20260828: 08:17 KST 기준 CEO 지시(`api가 제공되지 않는 웹사이트를 파싱 및 스크래핑 방법`, `로그인이 필요한 어드민에서 로그인 처리 후 파싱/스크래핑/자료 업로드 실행`, `이전 대화 내용 확인 후 기획서 우선 작성 보고`)를 반영해 `docs/plans/20260828_APILESS_AUTHENTICATED_ADMIN_AUTOMATION_PLAN.md`를 신규 저장했다. 문서는 Browserbase/Firecrawl/Aside 비교 대화, OHVIS 자체 PC Agent/Browser Bridge 기반, 승인형 위험 액션 정책, 땡겨요 승인형 CAPTCHA 자동입력 정정 지시를 통합했다. 범위는 BrowserRecipe, 로그인 세션, Vault 자동입력, DOM/다운로드/네트워크/LLM 파싱, 파일 업로드, OTP/CAPTCHA/인증서/결제/게시/삭제 승인 토큰, 감사로그, artifact 저장, 자체/외부 관리형 브라우저 로드맵, P0 구현 지시서 초안까지 포함한다. 이번 조치는 기획서/HANDOVER 문서 저장이며 코드 구현, DB 변경, 커밋, 푸시, 배포는 수행하지 않았다.
- AADS-APPROVAL-SCOPED-RISKY-ACTION-AUTOMATION-20260828: 08:09 KST 기준 CEO 지시(`OTP/CAPTCHA 우회 금지`, `사용자 승인 범위 내 LLM 분석/입력/자동승인 후 자동화`, `사람이 화면을 계속 볼 수 없는 상황을 고려해 자동입력 차단 금지`)를 `docs/plans/20260828_APPROVAL_SCOPED_RISKY_ACTION_AUTOMATION_PLAN.md`에 상세 기획서로 저장했다. 범위는 CAPTCHA뿐 아니라 OTP, 인증서, 결제/이체/환불, 게시/전송, 삭제/취소, 파일 업로드까지 포함한다. 구현 보강: `app/services/browser_task_gateway.py`의 승인/토큰 소비 이벤트에 승인자, 승인시각, 승인 origin, 실제 origin, work_key, action, selector, scope, 실행횟수를 기록하도록 강화했고, 토큰 원문은 기록하지 않는다. `app/services/browser_permission_policy.py`는 `captcha_value` 계열 키도 민감값으로 마스킹하도록 보강했다. 검증은 `tests/unit/test_browser_task_policy.py`에 승인 감사 로그와 토큰 소비 로그 테스트를 추가해 수행한다.
- AADS-FOOD-DDANGYO-APPROVED-CAPTCHA-AUTOMATION-20260828: 07:43 KST 기준 CEO 지시(`OTP/CAPTCHA는 우회는 절대 금지`, `사용자 승인 범위 내 LLM 분석 입력/자동승인 후 자동화`, `자동화 차단하면 안 된다`)에 따라 CAPTCHA 정책과 땡겨요 수집 경로를 정정 반영했다. 정책 기준은 `docs/plans/20260828_APPROVED_CAPTCHA_AUTOMATION_POLICY.md`에 신규 저장했다. 조치: 승인 없는 CAPTCHA 모델 판독은 차단하되 `captcha_auto_approved=true`와 승인 scope가 있는 경우 같은 origin/work_key/session/run 범위에서 비전 모델 판독, 자동입력, 자동제출, 재판정을 허용한다. `app/services/captcha_vision_solver.py`는 승인 컨텍스트 없이는 실행하지 않고 모델 raw/CAPTCHA 값을 로그에 남기지 않도록 수정했다. `app/services/yeoljeong_finance_service.py`는 땡겨요 CAPTCHA 감지 시 승인자/승인시각/승인페이지/run_id를 `delivery_browser_session_events.jsonl`에 기록하고 값 자체는 기록하지 않는다. `app/services/browser_permission_policy.py`와 `app/services/browser_task_gateway.py`는 `solve/read/vision captcha`를 전면 deny가 아니라 승인 필요 ask로 분류하고, `bypass captcha`/OTP 무단 모델 생성·읽기는 계속 deny한다. `migrations/131_browser_approval_tokens.sql`은 Browser Task 승인 토큰과 실행횟수 scope를 추가한다. `.claude/skills/sales-channel-collector/SKILL.md`, 기존 은행/판매채널 기획서도 같은 정책으로 정정했다. 검증은 아직 실행 전이며, 커밋/푸시/배포도 아직 수행하지 않았다.

## 2026-08-27
- AADS-FOOD-MEMBER-ACCESS-SIMPLE-UX-20260827: 12:12-12:26 KST 기준 CEO 지시(`회원/권환관리 페이지가 왜이리 복잡해 심플하고 단순하게 다시 정리반영`, `권한부여도 가입된 직원리스트에서 바로 설정`)에 따라 매장비서 운영 정적앱 `app/static/apps/yeoljeong-finance/index.html#audit`와 직원 권한 API를 보강했다. 조치: `회원·권한관리` 화면에서 5단계 설명 카드, 권한 초대 폼, 상세 권한표, 권한 변경 이력표를 제거하고 `가입 대기`, `가입 직원`, `운영 권한` 3개 요약과 `가입 직원 권한설정` 테이블, `가입 승인 대기` 테이블만 남겼다. 승인 완료 직원 행에는 `직원/조회 전용/운영 입력자/운영관리자` select와 저장 버튼을 배치해 리스트에서 바로 권한을 설정하게 했다. 백엔드는 `PATCH /api/v1/yeoljeong-finance/employees/approved/{request_id}/role`을 추가했고 `yeoljeong_employee_join_requests.role` 값을 갱신한다. 직원 로그인 세션은 승인 직원 row의 role을 읽어 `employee/viewer/member/admin` 권한으로 반영한다. 검증: `python3 -m html.parser app/static/apps/yeoljeong-finance/index.html` 성공, 인라인 JS `vm.Script` 파싱 성공, `python3 -m py_compile app/api/yeoljeong_finance.py app/services/yeoljeong_finance_service.py` 성공, `git diff --check -- app/api/yeoljeong_finance.py app/services/yeoljeong_finance_service.py app/static/apps/yeoljeong-finance/index.html tests/unit/test_yeoljeong_finance_api.py tests/unit/test_yeoljeong_finance_service.py docs/HANDOVER.md` 성공, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_finance_api.py tests/unit/test_yeoljeong_finance_service.py` 160 passed. DB 집계는 `approved/employee 14건`, `pending/employee 2건`이다. 커밋/푸시: `d8812add feat(food): simplify member access management`를 `origin/main`에 push했고, 배포 게이트 보정 문서 커밋 `63cae693 docs(food): record member access deploy gate`도 push했다. 공개 정적 URL은 HTTP 200/610,018 bytes이며 `가입 직원 권한설정`, `data-save-employee-role`, 새 권한 API 경로 문자열을 확인했다. 배포: `deploy.sh bluegreen` 3회 시도 모두 전환 대상 `aads-server-green:8102`의 활성 스트림 1건 보호 게이트로 중단됐고, 강제 플래그는 사용하지 않았다. 따라서 새 API 런타임 반영은 다음 안전 배포 재시도 또는 CEO 승인 기반 강제 배포 후 확정해야 한다.
- AADS-FOOD-MEMBER-ACCESS-MANAGEMENT-UI-20260827: 11:53-12:03 KST 기준 CEO 지시(`회원 권환관리페이지에 회원가입현황과 권한부여등이 없는데 상세 기획`, `모두 구현 반영`)에 따라 매장비서 운영 정적앱 `app/static/apps/yeoljeong-finance/index.html#audit`의 회원·권한관리 화면을 단순 권한 안내표에서 실제 관리 화면으로 확장했다. 조치: 회원가입 현황 카드/테이블을 추가해 직원 가입요청의 pending/approved/rejected 상태를 한 화면에서 확인하고, pending 요청은 기존 `reviewEmployeeJoinRequest()` API 연결 버튼으로 승인/반려 처리할 수 있게 했다. 권한부여 패널에는 직원 전화번호, 이름, 지점, 부여 권한, 유효시간을 받는 권한 초대 폼을 추가하고 기존 `createEmployeeInvite()` 서버 API를 재사용해 운영자/admin/member/viewer 초대 생성 흐름을 연결했다. 권한부여 대상 표는 가입 승인 대기, 승인 완료 직원, 대기 초대를 함께 보여주고, 권한 변경 이력 표는 가입요청과 초대 생성/수락 이력을 최신순으로 표시한다. `#audit` 탭 진입 시 가입요청/초대/서류 데이터를 자동 조회하도록 보강했다. 기존 회원의 테넌트 권한 직접 변경 API는 현 정적앱 범위에 없어 새 권한 부여는 초대 기반으로 제한했고, 기존 회원 권한 변경은 별도 API 설계 후 확장 대상으로 분리했다. 검증: `python3 -m html.parser app/static/apps/yeoljeong-finance/index.html` 성공, 인라인 JS `vm.Script` 파싱 성공, `git diff --check -- app/static/apps/yeoljeong-finance/index.html docs/HANDOVER.md` 성공, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_finance_api.py` 24 passed, DB 기준 직원 가입요청 `approved 14건/pending 2건` 확인. 커밋/푸시: `83b0a417 feat(food): add member access management UI`를 `origin/main`에 push했다. 배포: 선행 deploy PID 때문에 1회 blocked 후 같은 커밋 `83b0a417`이 deploy_history success 처리됐고 active 슬롯 `aads-server-green:8102`, standby `aads-server:8100` 모두 새 HTML을 포함한다. 공개 URL은 HTTP 200/612,767 bytes이며 `회원가입 현황`, `권한부여`, `권한 초대 생성`, `권한 변경 이력` 문구를 확인했다. deploy_history downtime_seconds는 12초로 기록됐다.
- AADS-FOOD-OPS-PRIORITY-UI-DEPLOY-20260827: 11:25-11:36 KST 기준 CEO 지시(`디자인 기획화면을 기준으로 최우선순위 수정 반영 해야할 페이지별 순서`, `즉시 우선도 순 직접 구현진행하고 운영 반영까지`)에 따라 매장비서 운영 정적앱 `app/static/apps/yeoljeong-finance/index.html`에 우선순위 UI를 직접 반영했다. 반영 순서: P1 회원관리/권한, P2 연동관리, P3 사업자·지점, P4 직원관리/급여관리 분리, P5 전문 세무관리. 조치: 로그인 게이트를 로그인 기본 화면으로 되돌리고 직원 회원가입은 이메일·희망 지점·휴대폰·비밀번호 중심으로 축소했다. 좌측 메뉴는 `직원관리`와 `급여관리`를 분리하고 `권한·감사로그`를 `회원·권한관리`로 명확히 바꿨다. `회원·권한관리` 화면에는 owner/admin/employee/employee_pending/employee_rejected 5상태 카드와 상세표를 추가했다. 연동관리에는 사업자·지점·서비스 기준 중복 연동 점검표를 추가해 정상값이 많은 1건만 유지 추천하도록 했다. 사업자·지점 화면과 설정 요약은 active 지점만 운영 지점으로 집계하도록 보정했다. 세무·회계 화면은 전문 세무관리 카드, 부가세 매출·매입대장, 원천세/지급명세서, 증빙 누락, 신고서·납부서 A4 미리보기 기준의 세무 상세 대장으로 확장했다. 검증: `python3 -m html.parser app/static/apps/yeoljeong-finance/index.html` 성공, 인라인 JS 추출 후 `node --check /tmp/yeoljeong-finance-inline.js` 성공, `git diff --check -- app/static/apps/yeoljeong-finance/index.html docs/HANDOVER.md` 성공, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_finance_api.py` 24 passed. 커밋/푸시: `07fb2091 feat(food): prioritize ops management UI`를 `origin/main`에 push했다. 배포: `deploy.sh bluegreen`은 기존 PID 2080553 진행 중이라 1회 blocked row를 남겼으나, 기존 배포가 같은 커밋 `07fb2091`로 success 처리되어 active 슬롯이 `aads-server-green:8102`로 전환됐다. 공개 URL `https://fb.newtalk.kr/static/apps/yeoljeong-finance/index.html?v=priority-ui-20260827-1134`는 HTTP 200/600,137 bytes이며 `매장 운영 로그인`, `직원 회원가입`, `회원 권한 구분 (5단계)`, `전문 세무관리`, `중복 연동 점검` 문구를 확인했다. `capture_screenshot`은 timeout으로 실패해 브라우저 캡처는 미완이고, HTTP/헬스/컨테이너/본문 검증으로 대체했다.
- AADS-FOOD-DELIVERY-AUTO-COLLECT-RESUME-CLEANUP-20260827: 09:59-10:06 KST 기준 CEO 지시(`이어서 진행해`)에 따라 중단됐던 FOOD 배달 자동수집 상태를 이어서 실측했다. 요기요 09:49 실행은 성신여대역점이 succeeded로 sales/reviews/settlements/ads 각 99건 저장, 미아점은 `PORTAL_AUTH_CHALLENGE`, 성신여대점은 대응 프로세스가 없는 running orphan으로 확인됐다. 10:00 KST 쿠팡 자동 catchup은 대체 PC `7f99c528-24d`에서 실행됐지만 쿠팡이츠 로그인 화면 탭이 4개까지 증식하고 수집 데이터는 0건이었다. 조치: 대체 PC 쿠팡 중복 탭 4개를 닫아 쿠팡 탭 0개로 정리했고, 살아 있던 쿠팡 자동수집 프로세스 2개(PID 1747160/1747247)에 TERM을 보내 종료했다. `flock -n app/data/yeoljeong_finance/.delivery_sync.lock true` 성공으로 lock 점유 해제를 확인했다. DB `yeoljeong_delivery_collection_status` running orphan 3건(`f1c8100b-27c0-469b-a75d-7dd9b670621a`, `4da91585-5a3b-4a85-8a08-bc03cb6bb8be`, `c659e142-6d9f-4a4a-b8ec-e481adc155d3`)은 `db_safe_write`로 `failed/MANUAL_TERMINATED_OR_ORPHANED` 처리했고 결과는 `UPDATE 3`, 전체 row count 3192 유지였다. 최종 검증: 배달 자동수집 프로세스 0건, running 상태 0건, CEO PC 배달 포털 탭 0건, 대체 PC 쿠팡 탭 0건/요기요 탭 1건. 남은 제한: 쿠팡은 포털 로그인 필요, 요기요 미아점은 추가 인증 필요이며 로그인/인증 완료 후 재수집해야 한다. 커밋/푸시/배포는 수행하지 않았다.
- AADS-FOOD-BAEMIN-SECURITY-HARD-STOP-20260827: 10:47-10:50 KST 기준 배민 보안차단 후 남은 매장으로 계속 넘어가던 결함을 코드로 차단했다. 원인: `sync_delivery()`가 all-business scope를 순회하면서 한 배민 scope에서 `BAEMIN_SECURITY_BLOCKED`가 나와도 같은 호출 안의 이후 배민 scope를 계속 처리했다. 조치: `app/services/yeoljeong_finance_service.py`에 `baemin_security_blocked` 플래그를 추가해 `BAEMIN_SECURITY_BLOCKED`/`PORTAL_BLOCKED`/`SECURITY_BLOCKED` 발생 후 남은 배민 scope를 skip하게 했다. 회귀 테스트 `test_sync_delivery_stops_remaining_baemin_scopes_after_security_block`를 추가해 2개 배민 매장 중 첫 매장 차단 후 두 번째 collector가 호출되지 않음을 검증했다. 검증: `python3 -m py_compile app/services/yeoljeong_finance_service.py` 성공, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_finance_service.py::test_sync_delivery_stops_remaining_baemin_scopes_after_security_block tests/unit/test_yeoljeong_finance_service.py::test_delivery_browser_session_result_event_records_security_block` 2 passed, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_delivery_scheduler_contract.py tests/unit/test_yeoljeong_auto_collect.py::test_until_complete_stops_on_raw_baemin_security_blocked` 10 passed, 대상 파일 `git diff --check` 성공. 커밋/푸시/배포는 아직 수행하지 않았다.
- AADS-E2E-AGENT-VAULT-BROWSER-CAPTURE-P0-20260827: 05:54-05:58 KST 기준 CEO 지시(`권장조치 p0-p2까지 진행`, `비밀번호관리자기능을 e2e검증에 사용안하나?`)에 따라 E2E 브라우저/캡처/비밀번호 관리자 연동 경로를 실측하고 P0 패치를 적용했다. 확인 결과 DB에는 `agent_vault_credentials` 363건, `e2e_credentials` 30건이 존재하지만, 기존 `/e2e/credentials/test-login` 및 브라우저 자동 로그인 일부 경로는 구형 `e2e_credentials` 중심이었고 `capture_screenshot`은 로그인 주입 경로를 타지 않아 인증 필요 URL에서 타임아웃 또는 로그인 화면 캡처로 이어질 수 있었다. 또한 `ToolExecutor.execute()`가 tenant_id를 계산하고도 browser/capture 하위 함수 입력에 주입하지 않아 멀티테넌트 vault 조회가 `tenant_scope_required`로 실패할 수 있었다. 조치: `app/services/tool_executor.py`에서 resolved tenant_id를 tool_input에 보존하고 `browser_navigate`/`capture_screenshot`에 전달하도록 수정했다. `app/services/agent_vault_service.py`에 URL origin/work_key 기준 Agent Vault 자격증명 조회 함수 `get_agent_credential_for_url()`을 추가하고 접근 로그 `credential_e2e_resolve`를 남기게 했다. `app/api/ceo_chat_tools.py`는 AADS e2e vault 조회 시 tenant_id를 전달하고, 인증 폼 감지 시 Agent Vault 비밀번호 관리자 자격증명을 우선 사용하며, `capture_screenshot`은 `networkidle` 고정 대기 대신 `domcontentloaded` 후 짧은 networkidle 폴백과 로그인 주입 후 재진입을 수행하게 했다. 검증: `python3 -m py_compile app/api/ceo_chat_tools.py app/services/tool_executor.py app/services/agent_vault_service.py app/browser_bridge/service.py` 성공, `.venv-playwright/bin/python -m pytest -q tests/unit/test_credential_vault.py` 13 passed, `git diff --check -- app/api/ceo_chat_tools.py app/services/tool_executor.py app/services/agent_vault_service.py` 성공, `capture_screenshot(url=https://aads.newtalk.kr/api/v1/health, browser_work_key=aads-e2e-audit-20260827)` 성공. 참고: `tests/unit/test_browser_bridge.py tests/unit/test_credential_vault.py` 통합 실행은 기존 PC Agent route API 후보 순서 기대값 불일치로 2 failed/53 passed였고, 이번 변경 파일과 직접 관련 없는 `app/browser_bridge/service.py` route ordering 회귀로 분리한다. 운영 반영은 아직 커밋/푸시/배포 전이며, 기존 무관 dirty 파일이 있어 선별 커밋이 필요하다.

## 2026-08-26
- AADS-FOOD-BAEMIN-BACKFILL-RETRY-WINDOW-20260826: 23:25-23:44 KST 기준 CEO 지시(`이어서 진행해`)에 따라 배민/쿠팡 자동수집 상태를 재실측하고 배민 백필 timeout 재시도 결함을 보정했다. 실측: AADS health는 HEALTHY, DB latency 118-124ms, active Pipeline Runner는 0건이었다. 운영 프로세스에는 `delivery-auto-pc_agent_catchup-2026-08-26` 배민 full_backfill 1건이 살아 있었고, DB 기준 23:00 회차는 성신여대역점/성신여대점 succeeded, 미아점 ATTEMPT_TIMEOUT, 23:23/23:30 재시도는 `date_from=2024-01-01`, `date_to=2026-08-26` 전체 구간으로 다시 떠 있었다. 23:29:39 KST 미아점 재시도는 `LOGIN_FORM_NOT_FOUND` action_required로 종료됐고, 23:30 orphan running row는 PID 부재 확인 후 `BACKGROUND_SYNC_STALE`로 정리했다. 원장 현황은 배민 sales 236건, 배민 reviews 1,186건, 쿠팡이츠 reviews 14건, delivery_ads 0건이다. 쿠팡 자동수집은 23:25:53 KST `delivery_auto_collect_done reason=coupangeats_catchup`로 스케줄 실행됐으나, 배민 수집 lock 때문에 4개 매장 모두 `COLLECTION_ALREADY_RUNNING`으로 차단됐다. 조치: `app/services/yeoljeong_finance_service.py`에 legacy wide failed full_backfill row를 retry할 때 `window_days` 기준으로 최신 1일 window로 clamp하는 `_delivery_clamp_backfill_retry_window()`를 추가했고, `tests/unit/test_yeoljeong_finance_service.py`에 직접 queue 1일 window 계약과 내부 retry clamp 회귀 테스트를 추가했다. 검증: `.venv-playwright/bin/python -m py_compile app/services/yeoljeong_finance_service.py scripts/yeoljeong_auto_collect.py app/main.py` 성공, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_finance_service.py::test_retry_full_backfill_clamps_legacy_wide_failed_window tests/unit/test_yeoljeong_finance_service.py::test_ensure_full_backfill_retry_clamps_legacy_wide_failed_window tests/unit/test_yeoljeong_finance_service.py::test_queue_full_backfill_creates_store_day_windows tests/unit/test_yeoljeong_finance_service.py::test_sync_full_backfill_claims_one_queued_window_and_enqueues_next_day tests/unit/test_yeoljeong_finance_service.py::test_sync_delivery_upserts_only_incoming_delivery_records tests/unit/test_yeoljeong_auto_collect.py::test_main_child_no_timeout_runs_collectors_without_recursing` 6 passed. 운영 반영: 커밋 `779c8702 fix(food): clamp baemin backfill retry windows`를 push했고, `deploy_safe(mode=reload)` 결과 hot-reload 86개와 post-health OK를 확인했다. 운영 검증: `manual-baemin-window-retry-20260826-2332` queue-only가 4개 매장을 모두 `2026-08-26~2026-08-26` 1일 window로 생성했고, 성신여대역점 수집은 150초 ATTEMPT_TIMEOUT, 중화점 별도 scope 수집은 120초 ATTEMPT_TIMEOUT으로 종료됐다. 쿠팡 `manual-coupangeats-catchup-20260826-2341`은 성신여대역점 120초 ATTEMPT_TIMEOUT, 나머지 3개 매장은 같은 수집 lock으로 `COLLECTION_ALREADY_RUNNING`이었다. 23:43:43 KST 최종 프로세스 확인 결과 배민/쿠팡 자동수집 프로세스는 0건이다. 남은 제한: 광고 원장은 아직 0건이며, 현재 차단점은 전체 기간 큐 문제가 아니라 PC Agent CDP/브라우저 응답 timeout이다.
- AADS-FOOD-SHINHAN-INDIVIDUAL-QUERY-COMPLETE-20260826: 14:45-15:03 KST 기준 CEO 지시(`신한 로그인 성공 확인`, `계좌조회 페이지 이동`, `계좌 선택/계좌비번 입력`, `조회기간 설정/조회 버튼 클릭`, `거래내역 파싱`, `imported_rows > 0 검증` 즉시 구현)에 따라 신한 개인사업자 간편조회 상태머신을 보강하고 미아점 오늘자 실수집을 재실행했다. 조치: `app/services/yeoljeong_bank_browser_connector.py`의 신한 query flow가 same-origin iframe까지 DOM을 탐색하도록 확장했고, 로그인 성공 후 `#210101000000` 계좌조회 hash/WebSquare 이동을 명시 진단하며, 계좌비밀번호 입력은 일반 password input 이전에 TransKey 보안키패드 객체를 먼저 채우도록 보강했다. `login_success`, `account_page_direct_hash`, `account_resolved`, `account_secret`, `date_from`, `date_to`, `query_submitted` 진단을 수집 결과에 남긴다. 테스트: `python3 -m py_compile app/services/yeoljeong_bank_browser_connector.py` 성공, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_bank_browser_connector.py` 56 passed, `docker exec aads-server-green python -m pytest -q /app/tests/unit/test_yeoljeong_bank_browser_connector.py /app/tests/unit/test_yeoljeong_auto_collect.py` 87 passed, `git diff --check -- app/services/yeoljeong_bank_browser_connector.py tests/unit/test_yeoljeong_bank_browser_connector.py` 성공. 운영 조치: stale `.bank_auto_collect.lock`은 실행 중 은행 프로세스가 없어 삭제했고, `7f99c528-24d`의 신한/금융인증 탭 폭주를 URL 기준으로 정리해 탭 수가 143개에서 25개 이하로 감소, PC 메모리 사용률이 93.1%에서 72.8%로 내려간 것을 확인했다. 실수집: `docker exec aads-server-green python /app/scripts/yeoljeong_auto_collect.py --bank-only --business-id biz-mia --branch branch-gangbuk-mia --date-from 2026-08-26 --date-to 2026-08-26 --browser-agent-id 7f99c528-24d --browser-preferred-port 9222 --bank-browser-work-key yeoljeong-bank-shinhan-individual-00e6447fd39dad84 --force-recreate-bank-browser --bank-browser-timeout-seconds 180 --attempt-timeout-seconds 240 --max-attempts 1 --job-id bank-manual-shinhan-mia-20260826-1500` 실행 결과 `status=no_records`, `connector_status=CONFIGURED`, `browser_session_id=bb-5376f5a35453`, `current_url=https://bank.shinhan.com/rib/easy/index.jsp#210101000000`, `parser_transaction_header_found=true`, `login_success=1`, `account_page_navigation=1`, `account_page_direct_hash=1`, `login_secret=1`, `account_no=1`, `account_resolved=1`, `account_secret=1`, `date_from=1`, `date_to=1`, `query_submitted=1`, `collected_rows=0`, `imported_rows=0`이다. `bank_transactions.json`은 오늘자 신규 거래 행이 없어 생성되지 않았다. 남은 제한: `imported_rows > 0`은 오늘자 실제 거래 행이 존재해야 충족 가능하며, 이번 실측에서는 신한 조회 화면까지 도달했지만 신규 거래 행이 0건이다. 배포: active green 컨테이너 바인드 소스에서 검증/실행했으며, 이 기록 시점에는 커밋/푸시/정식 `deploy.sh` 재배포는 아직 수행하지 않았다.
- AADS-FOOD-BANK-ONLY-SCHEDULER-20260826: 08:34-08:58 KST 기준 CEO 지시(`은행 전용 자동수집 스케줄을 별도로 추가하고, 1회 실수집 실행 후 imported_rows 기준으로 완료 검증`)에 따라 배달 채널 루프와 분리된 은행 전용 자동수집 경로를 구현했다. 조치: `scripts/yeoljeong_auto_collect.py`에 `--bank-only` 모드를 추가해 배달 포털 수집을 건너뛰고 active/auto_sync 은행 계좌만 수집하도록 했다. `--bank-browser-timeout-seconds`를 추가해 은행 브라우저 자동화 timeout을 명시 전달한다. `app/main.py`에는 `bank_auto_collect` APScheduler job을 추가했고, KST 08-23시 매시 10분 기본 실행, `YEOLJEONG_BANK_AUTO_COLLECT_HOURS`, `YEOLJEONG_BANK_AUTO_COLLECT_MINUTE`, `YEOLJEONG_BANK_BROWSER_TIMEOUT_SECONDS`, `YEOLJEONG_BANK_AUTO_COLLECT_TIMEOUT_SECONDS` env로 조정 가능하게 했다. 스케줄 job은 PC Agent 온라인 확인 후 독립 자식 프로세스로 bank-only CLI를 실행하고, 프로세스 timeout에 걸리면 고착 프로세스를 남기지 않고 로그로 종료한다. 검증: `python3 -m py_compile app/main.py scripts/yeoljeong_auto_collect.py` 성공, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_auto_collect.py tests/unit/test_yeoljeong_bank_browser_connector.py` 84 passed, `git diff --check -- app/main.py scripts/yeoljeong_auto_collect.py tests/unit/test_yeoljeong_auto_collect.py` 성공. 실수집 결과: KST 2026-08-26 기준 미아점 신한 active 계좌는 1건(`**********1031`)이고 수집 전/후 오늘자 `bank_transactions.json` 미아점 거래는 0건이다. `7f99c528-24d`에서 같은 `work_key=yeoljeong-bank-shinhan-individual-00e6447fd39dad84`로 세션 `bb-cce9a6485f6c`/CDP 9222 확보는 성공했으나, bank-only 수집은 150초 OS timeout으로 종료되어 `imported_rows` 생성까지 완료하지 못했다. `2e9379a1-fed`도 같은 work_key 세션 확보가 `command timeout`으로 실패했다. 남은 차단: 신한 로그인/조회 상태머신 구현 이전 단계인 PC Agent 브라우저 명령 응답 계층이 timeout으로 고착된다. 다음 P0는 은행 수집 전용 Chrome 프로필 정리 또는 PC Agent command timeout/결과 반환 계층 복구 후 동일 work_key로 재수집하는 것이다.

## 2026-08-25
- AADS-FOOD-BANK-RECONNECT-RECOVERY-20260825: 16:28-17:26 KST 기준 CEO 지시(`재연결 로직 반영안되어있어?`, `배포후 검증해`)에 따라 은행 브라우저 수집의 끊김 회복 경로를 보강하고 배포 검증을 진행했다. 조치: `app/services/yeoljeong_bank_browser_connector.py`에 `CDP_NOT_READY`, `COMMAND_TIMEOUT`, `RUNTIME_EVALUATE_TIMEOUT`, `STALE_TARGET`, session closed/target closed 계열을 회복 가능 오류로 분류하고, 수집 중 `_context_for_session` 또는 페이지 snapshot 단계에서 끊기면 동일 `browser_work_key`를 `force_recreate=True`로 1회 재생성한 뒤 같은 계좌 수집을 재진입하도록 추가했다. `scripts/yeoljeong_auto_collect.py`에는 은행 수집 오류 코드가 다음 attempt의 `force_recreate_bank_browser=true`로 이어지도록 별도 판정/출력 플래그를 추가했다. `app/api/pc_agent.py`에는 active slot에서 PC Agent가 비어 있을 때 peer backend의 온라인 Agent를 health/offline monitor에 반영하도록 보강했다. 테스트 보강: `tests/unit/test_pc_agent_api_disconnects.py`, `tests/unit/test_yeoljeong_bank_browser_connector.py`, `tests/unit/test_yeoljeong_auto_collect.py`에 peer health fallback, CDP 끊김 후 재생성 수집, `CDP_NOT_READY` 이후 은행 브라우저 강제 재생성 케이스를 추가했다. 검증: `docker exec aads-server python -m pytest -q tests/unit/test_pc_agent_api_disconnects.py tests/unit/test_yeoljeong_bank_browser_connector.py tests/unit/test_yeoljeong_auto_collect.py` 90 passed, 7 warnings, `docker exec aads-server python -m py_compile app/services/yeoljeong_bank_browser_connector.py scripts/yeoljeong_auto_collect.py` 통과, 대상 파일 `git diff --check` 통과. 제한: 호스트 `.venv`는 Python 3.11 symlink/ABI 불일치로 pytest 수집이 불가해 컨테이너 환경으로 검증했다. 무관한 UI/명함/GO100 문서 변경은 선별 커밋 대상에서 제외했다.
- AADS-FOOD-SHINHAN-AUTO-COLLECT-P0-20260825: 15:17-15:24 KST 기준 CEO 지시(`신한은행 계좌 자동수집 진행 완료`)에 따라 미아점 신한 개인사업자 간편조회 자동로그인/자동수집 경로를 재검증했다. 조치: 신한 개인사업자 전용 work_key(`yeoljeong-bank-shinhan-individual-00e6447fd39dad84`)를 유지하고, Browser Bridge가 force recreate 시 work_key/profile suffix를 만들지 않도록 고정했다. CDP_NOT_READY 발생 시 같은 9222 포트에 대해 `browser_health`/`browser_tabs` probe로 attach recovery를 시도하도록 보강했고, 신한 상태머신은 간편조회 로그인 안내 확인, 보안 안내 닫기, 계좌조회 이동, 계좌번호/계좌비밀번호/조회기간/조회 제출, 거래 테이블 파싱 진단을 남긴다. 검증: `.venv-playwright/bin/python -m pytest -q tests/unit/test_browser_bridge.py tests/unit/test_yeoljeong_bank_browser_connector.py tests/unit/test_yeoljeong_finance_api.py` 114 passed, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_auto_collect.py` 29 passed, stale delivery 격리 테스트 2 passed, 대상 파일 `git diff --check` 성공. 운영 실수집: `biz-mia/branch-gangbuk-mia` 신한 계좌 1건(`9aa33cb9-5597-4f0d-ab0d-43146c35f318`)에 대해 2026-08-25 범위 수집을 실행했고 결과는 `status=no_records`, `collected_rows=0`, `imported_rows=0`, `bank_transactions` 오늘자 0건이다. 차단 원인: 자격정보/계좌 중복/URL 문제가 아니라 PC Agent `7f99c528-24d`의 `browser_eval`이 `RUNTIME_EVALUATE_TIMEOUT`으로 실패해 DOM 단계 실행이 1단계에서 멈춘 것이다. 배포/커밋: 이 기록 시점에는 선별 커밋/푸시/배포를 이어서 진행 중이며, PC Agent 런타임 평가 장애 복구 또는 agent 전환 후 같은 work_key로 재수집해야 한다.
- AADS-FOOD-BAEMIN-AUTO-COLLECT-OPERATIONS-20260825: 14:22-14:31 KST 기준 CEO 지시(`자동 수집될수 있게 조치해`)에 따라 배민 full_backfill 자동수집 운영 경로를 실제 실행 기준으로 확인하고 보강했다. 조치: `app/services/yeoljeong_finance_service.py`에 `sync_delivery()` 시작 전 `queued/running` stale row를 `BACKGROUND_SYNC_STALE`로 정리하는 `_settle_stale_delivery_collection_statuses()`를 추가했고, `tests/unit/test_yeoljeong_finance_service.py`에 다음 수집 시작 전 stale running 정리 회귀 테스트를 추가했다. 운영 반영: `/api/v1/ops/hot-reload`로 `app.api.yeoljeong_finance`, `app.services.yeoljeong_finance_service`를 재로드했고 결과는 success=2, tasks_lost=0이었다. 실측: DB 기준 14:18:48 KST 배민 full_backfill이 시작되어 중화점은 14:25:49 KST `succeeded`로 sales 1, settlements 1, reviews 20, ads 1건을 저장했고, 성신여대점은 14:31 KST 기준 `running`이었다. 14:30 KST 예약 실행은 기존 수집 락 때문에 `COLLECTION_ALREADY_RUNNING`으로 중복 차단됐고, 오래된 07:43 KST stale row는 `BACKGROUND_SYNC_STALE`로 정리됐다. `list_scheduled_tasks`로 임시 외부 fallback 예약 4건을 등록 후 중복 busy row 발생을 확인해 모두 제거했으며, 최종 사용자 예약 작업은 0건이다. 자동 실행은 `app/main.py` 내장 APScheduler의 배민 full_backfill 정시 job(02:30/14:30/22:30 KST)과 PC Agent catch-up job 경로가 담당한다. 검증: `.venv-playwright/bin/python -m py_compile app/services/yeoljeong_finance_service.py app/main.py app/api/yeoljeong_finance.py scripts/trigger_delivery_sync.py scripts/yeoljeong_auto_collect.py` 성공, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_finance_service.py -k 'stale or full_backfill or sync_delivery_settles' tests/unit/test_yeoljeong_delivery_scheduler_contract.py tests/unit/test_yeoljeong_auto_collect.py tests/unit/test_yeoljeong_finance_api.py -k 'full_backfill or auto_collect or scheduler_contract'` 35 passed/140 deselected, 대상 파일 `git diff --check` 성공. 이번 조치는 코드/테스트/HANDOVER/hot reload/예약 정리이며 커밋, 푸시, 정식 deploy.sh/컨테이너 재시작은 수행하지 않았다.
- AADS-FOOD-BAEMIN-AUTO-COLLECT-COLLECTOR-FIX-20260825: 13:33-13:35 KST 기준 CEO 지시(`배민 데이터 자동수집 빨리 구현완료해`)에 따라 배민 자동수집 구현 산출물을 직접 검수하고 실행 차단 버그를 보정했다. 사전 확인: 활성 AADS Pipeline Runner 및 활성 pipeline job은 0건이며, 작업트리에는 기존 FOOD 관련 미완 변경이 다수 남아 있어 기존 변경은 되돌리지 않았다. 조치: `app/services/baemin_order_history_collector.py`에서 주문내역 수집 결과 `records`를 생성하기 전에 checkpoint diagnostics에서 참조하던 오류를 수정했고, 화면 row 후보 탐색 정규식을 `T*` 주문번호 전용에서 `B2FQ001QU5` 같은 비-T 배민 주문번호까지 허용하도록 확장했다. 테스트: `tests/unit/test_baemin_order_history_collector.py`에 async collector 진입점이 비-T 주문번호를 저장하고 `checkpoint_out.last_order_no`를 반환하는 회귀 테스트를 추가했다. 검증: `python3 -m py_compile app/services/baemin_order_history_collector.py app/services/baemin_review_collector.py app/services/baemin_ads_collector.py app/services/yeoljeong_finance_service.py scripts/trigger_delivery_sync.py` 성공, `.venv-playwright/bin/python -m pytest -q tests/unit/test_baemin_order_history_collector.py tests/unit/test_baemin_review_collector.py tests/unit/test_baemin_ads_collector.py` 9 passed, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_finance_service.py -k "full_backfill or sync_delivery_persists_delivery_ledgers"` 2 passed/116 deselected, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_finance_api.py tests/unit/test_yeoljeong_finance_service.py tests/unit/test_yeoljeong_bank_browser_connector.py tests/unit/test_baemin_order_history_collector.py tests/unit/test_baemin_review_collector.py tests/unit/test_baemin_ads_collector.py` 201 passed. 구현 범위는 PC Agent/Browser Bridge 로그인 세션이 있을 때 배민 주문내역·리뷰·광고 collector를 full_backfill 모드로 호출해 sales/settlements/reviews/ads 원장에 저장하는 코드 경로다. 이번 조치는 코드/테스트/HANDOVER 보강이며 DB 변경, 커밋, 푸시, 정식 배포, 실제 배민 포털 수집 실행은 수행하지 않았다.
- AADS-FOOD-MEMBER-LEVEL-AUDIT-UI-20260825: 13:10-13:14 KST 기준 CEO 지시(`다음단계 진행해`) 후속으로 매장비서 권한 구분을 실제 운영 화면에 반영했다. 조치: `app/static/apps/yeoljeong-finance/index.html`의 권한·감사로그 화면에 회원 권한 5단계(`owner`, `admin`, `employee`, `employee_pending`, `employee_rejected`) 표시와 현재 로그인 권한 highlight를 추가했고, 미정의 CSS 변수 `var(--fg)`를 `var(--ink)`로 보정했다. 로그인 게이트는 운영관리 로그인 우선, 직원 회원가입 보조 진입 유지 상태를 유지했다. 테스트: `tests/unit/test_yeoljeong_finance_api.py`에 권한 5단계 렌더 계약을 추가했다. 검증: `python3 -m py_compile app/api/yeoljeong_finance.py app/services/yeoljeong_finance_service.py app/services/yeoljeong_bank_browser_connector.py app/services/baemin_order_history_collector.py` 성공, HTMLParser 및 `node --check /tmp/yeoljeong-finance-inline.js` 성공, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_finance_api.py tests/unit/test_yeoljeong_finance_service.py tests/unit/test_yeoljeong_bank_browser_connector.py tests/unit/test_baemin_order_history_collector.py` 194 passed, `git diff --check` 성공. 공개 `https://fb.newtalk.kr/static/apps/yeoljeong-finance/index.html?v=member-level-20260825-1314`는 HTTP 200, 593,215 bytes, `회원 권한 구분 (5단계)`/`운영관리 로그인`/`employee_pending` 포함, `var(--fg)` 미검출. Runner `runner-f6427e3e`는 여전히 `error/deploy_preflight_git_state` 상태이며 재승인 대상이 아니고, 현재 AADS 작업트리는 다수 dirty 상태다. 이번 조치는 정적 파일/테스트/HANDOVER 보강이며 DB 변경, 커밋, 푸시, 정식 배포는 수행하지 않았다.
- AADS-FOOD-BAEMIN-RUNNER-APPROVAL-BLOCKED-20260825: 13:06:06 KST 기준 CEO 지시(`다음단계 진행해`)의 후속으로 배민 4개 매장 전체 백필 Runner 승인 단계를 처리했다. 실제 조회 결과 승인 대기 작업은 2건이었고, 최신 재제출 `runner-f6427e3e`를 approve, 중복 구버전 `runner-ef69aaed`를 reject 처리했다. `runner-f6427e3e`는 구현 결과를 가진 상태에서 `deploying`으로 넘어갔으나 배포 preflight에서 `deploy_preflight_git_state` 오류로 중단됐다. 차단 원인은 main workdir이 `origin/main`보다 1커밋 ahead 상태였기 때문이며, ahead 커밋은 `0d10d256 fix(browser-bridge): resolve Playwright browsers path by glob before driver start`, 변경 파일은 `app/browser_bridge/service.py` 1개다. Runner 결과상 구현 변경 파일은 `app/services/yeoljeong_finance_service.py`, `scripts/trigger_delivery_sync.py`로 보고됐고, 신규 collector/test 파일 일부는 결과 요약에는 있으나 actual_changed_files에는 누락돼 있어 후속 검수 시 작업트리 대조가 필요하다. 이번 조치는 Runner 승인/중복 거부/상태 확인이며, DB 변경, 직접 푸시, 직접 배포는 수행하지 않았다. 다음 단계는 ahead 커밋 푸시 승인 후 Runner 재승인/재시도 또는 Runner 산출물 수동 검수다.
- AADS-FOOD-REGRESSION-CONTRACT-ALIGN-20260825: 13:04:46 KST 기준 CEO 지시(`다음단계 진행해`)에 따라 FOOD 운영 변경 후속 회귀 검증을 진행했다. 확인 결과 실행 중인 Pipeline Runner는 0건이고, 작업트리에는 기존 FOOD/AADS 관련 미완 변경이 다수 남아 있었다. 조치: 최근 정책 변경(첫 화면 로그인 우선 + 직원 회원가입 보조 진입 유지, 삭제된 `branch-eonni-naengmyeon` 기본 지점 재삽입 금지)에 맞춰 `tests/unit/test_yeoljeong_finance_api.py`, `tests/unit/test_yeoljeong_finance_service.py`의 기대값을 갱신했다. 검증: `.venv-playwright/bin/python -m py_compile app/api/yeoljeong_finance.py app/services/yeoljeong_finance_service.py app/services/yeoljeong_bank_browser_connector.py app/services/baemin_order_history_collector.py` 성공, `.venv-playwright/bin/python -m pytest -q tests/unit/test_yeoljeong_finance_api.py tests/unit/test_yeoljeong_finance_service.py tests/unit/test_yeoljeong_bank_browser_connector.py tests/unit/test_baemin_order_history_collector.py` 193 passed. 운영 컨테이너 `/app/tests`는 이미지 내부 구버전 테스트 사본이라 같은 명령은 이전 기대값 기준 3건 실패했으며, 코드 문법 검증은 컨테이너에서도 통과했다. 이번 조치는 테스트 계약/HANDOVER 정렬이며 DB 변경, 배포, 커밋, 푸시는 수행하지 않았다.
- AADS-FOOD-BAEMIN-4STORE-FULL-BACKFILL-READY-RECHECK-20260825: 12:54 KST 기준 배민 4개 매장 전체 백필 구현 준비 상태를 재점검하고 `docs/plans/20260825_BAEMIN_ALL_STORES_FULL_BACKFILL_IMPLEMENTATION_PLAN.md`에 섹션 11을 추가했다. DB 기준 배민 platform_accounts raw row는 15건이나 삭제/중복 이력이 섞여 있고, 삭제되지 않은 대표 대상은 중화점/성신여대점/성신여대역점/열정국밥_미아점 4건이다. 배민 원장은 sales 199건, settlements 196건, reviews 955건, ads 0건이고, reviews의 `rating=0`은 placeholder 성격이라 품질값으로 보기 어렵다. 배민 queued/running status는 7건이며 15분 초과 stale 후보와 과거 `언니냉면` scope 1건이 포함된다. AADS Pipeline Runner에는 배민 백필 구현 작업 `runner-ef69aaed`, 재제출 `runner-f6427e3e` 2건이 `awaiting_approval` 상태로 있어, 실제 착수 시 최신 재제출 `runner-f6427e3e`만 승인하고 `runner-ef69aaed`는 중복 거부하는 것이 안전하다. 이번 조치는 문서 보강/구현 준비 재점검이며 Runner 승인, 코드 추가 구현, DB 변경, 배포, 커밋, 푸시는 수행하지 않았다.
- AADS-FOOD-BAEMIN-4STORE-FULL-BACKFILL-PLAN-20260825: 12:21 KST 기준 CEO 지시(`배민 매장 4건 모두 주문건별, 리뷰, 광고현황등 모든정보 전체 백필 추가 구현/보강 기획`)에 따라 배민 4개 등록 계정과 원장 품질을 재점검하고 `docs/plans/20260825_BAEMIN_ALL_STORES_FULL_BACKFILL_IMPLEMENTATION_PLAN.md`를 작성했다. 확인 결과 배민 계정은 중화점/성신여대점/성신여대역점/열정국밥_미아점 4건이며, DB 기준 배민 원장은 sales 199건, settlements 196건, reviews 955건, ads 0건이다. 리뷰는 row가 있으나 본문/평점/일자 채움률이 낮고, 광고현황은 배민 row가 없어 별도 collector가 필요하다. AADS 활성 Pipeline Runner는 0건이지만 `yeoljeong_delivery_collection_status`에는 배민 queued/running stale 후보 7건이 있어 실제 백필 실행 전 정리 로직이 필요하다. 문서에는 주문건별 주문/정산, 리뷰관리, 광고관리 수집 필드, 4개 매장 순차 백필, checkpoint, stale cleanup, 테스트/검증 기준, Runner 지시 초안을 저장했다. 이번 조치는 문서 작성/구현 준비이며 코드 구현, DB 변경, 배포, 커밋, 푸시는 수행하지 않았다.
- AADS-FOOD-LOGIN-SIGNUP-RECOVERY-20260825: 11:52 KST 기준 CEO 지적(`회원가입 버튼은 어디갔냐? 로그인하면 관리자, 직원 구분되서 로그인되나?`)에 따라 운영 정적 앱 `app/static/apps/yeoljeong-finance/index.html`의 로그인 게이트를 재점검했다. 원인: 직전 로그인 단순화에서 첫 화면의 `직원 회원가입` 진입 버튼이 제거되고 상단 `signupBtn`도 미로그인 상태에서 계속 hidden 처리되어 직원 가입 진입이 보이지 않았다. 조치: 로그인 카드 하단에 `직원 회원가입` 보조 버튼을 복구하고, 회원가입 폼을 직원 전용 간편 가입 기준(이메일, 소속/지점, 휴대폰, 비밀번호/확인)으로 축소했다. 이름 등 기본정보는 로그인 후 직원 기본정보 화면에서 받도록 하고, 가입 API에는 이메일 앞부분을 임시 표시명으로 전달한다. 모달형 회원가입도 동일 기준으로 맞췄고, `is_admin=false` 초기 권한 표시는 `운영관리자`가 아니라 `직원/승인 대기`로 표시되게 보정했다. 로그인 권한 분기는 `/api/v1/auth/login` 응답의 `is_admin` 1차 구분과 `/api/v1/yeoljeong-finance/session`의 `permissions.role`, `employee_request_status` 보정 구조를 유지한다. 검증: `HTMLParser` 파싱 성공, `<script>` 본문 `node --check` 성공, `git diff --check -- app/static/apps/yeoljeong-finance/index.html` 성공, 공개 URL HTTP 200 및 공개 본문에서 `직원 회원가입`, `직원/승인 대기`, `가입 후 직원 계정` 문구 확인. 커밋, 푸시, 정식 `deploy.sh`는 수행하지 않았다.
- AADS-FOOD-EONNI-BRANCH-SCOPE-ALIGN-20260825: 09:25 KST 기준 중단 응답 복구 후 `언니냉면` 지점 삭제/연동 연결 상태를 이어서 점검했다. DB `yeoljeong_branches` 기준 삭제 대상 `branch-eonni-naengmyeon`은 `inactive`/`deleted_at=2026-08-24 17:58:00+09`이고, 실제 active 지점은 `80en7mlszkesr`/`성신여대역점` 1건이다. DB `yeoljeong_platform_accounts` 4건과 파일 `app/data/yeoljeong_finance/platform_accounts.json` 4건은 이미 `business_id=biz-eonni-naengmyeon`, `branch=성신여대역점`으로 맞아 있었다. 남은 불일치는 DB `yeoljeong_settings(scope='ui').data.integrations` 4건이 `branch=언니냉면`으로 남아 있던 것과, 파일 `app/data/yeoljeong_finance/settings.json`의 `ui_settings.branches`에 삭제된 `branch-eonni-naengmyeon` active row가 남고 `ui_settings.integrations`에 언니냉면 판매채널 4건이 빠진 것이다. 조치: DB settings integrations 4건을 `branch=성신여대역점`, `branch_id/branchId=80en7mlszkesr`로 갱신했고, 파일 settings에서 `branch-eonni-naengmyeon`을 제거한 뒤 배민/쿠팡이츠/요기요/땡겨요 `srv-*` 4건을 `성신여대역점` active 지점으로 추가했다. 검증: DB bad integration count 4→0, DB good count 4, `python3 -m json.tool app/data/yeoljeong_finance/settings.json` 성공, 운영 컨테이너 `yeoljeong_finance_service.get_settings_persisted()` fallback 결과도 branches 1건(`80en7mlszkesr`), integrations 4건으로 확인했다. `curl http://127.0.0.1:8100/health`는 HTTP 200이다. `/api/v1/yeoljeong-finance/settings` 외부 API 직접 호출은 인증 필요로 HTTP 401이며 정상 보호 동작이다. 코드 로직 변경, 서비스 재시작, 커밋, 푸시, 정식 배포는 수행하지 않았다.

## 2026-08-24
- AADS-FOOD-BAEMIN-ORDER-HISTORY-FULL-PARSER-PLAN-20260824: 19:17-19:20 KST 기준 CEO 지시(`https://self.baemin.com/orders/history 여기페이지의 모든 정보를 파싱해야 매출, 정산자료를 수집할수 있다`)에 따라 첨부 화면 3장과 현재 배민 수집 코드/원장 상태를 점검하고 `docs/plans/20260824_BAEMIN_ORDER_HISTORY_FULL_PARSER_PLAN.md`를 작성했다. 확인 결과 현재 구현은 `app/services/yeoljeong_finance_service.py`의 배민 홈 대시보드 요약 추출과 현재 DOM 표 파싱 중심이며, 주문 행 펼침의 `주문정보/정산정보`, `주문 추가 정보` 모달, 과거 페이지네이션/백필, 리뷰관리 상세 파싱은 아직 전용 collector로 구현되지 않았다. DB 실측 기준 원장은 sales 594건, settlements 779건, reviews 1,858건, status 1,994건이고, 배민 sales는 16건 중 주문번호가 채워진 row 2건, 배민 reviews는 913건 중 review_text/rating/occurred_on 채움 0건이라 주문건별 모든 정보 수집 완료로 볼 수 없다. 문서에는 목록 행, 펼침 주문정보, 정산정보, 추가정보 모달 필드 맵과 신규 `baemin_order_history_collector.py`, checkpoint 기반 최근순/과거순 백필, D+1 정산 재조회, 리뷰관리 상세 collector, 리소스 제한, 수용 기준을 정리했다. 이번 조치는 문서 작성이며 코드 구현, DB 변경, 배포, 커밋, 푸시는 수행하지 않았다.
- AADS-FOOD-BRANCH-DELETE-UI-DEPLOY-20260824: 18:43:21 KST 기준 CEO 지시(`화면에 삭제가 반영안되었는데 배포 재시작까지 진행하고 반영`)에 따라 매장비서 운영 `index.html#business`의 지점목록 삭제 미반영을 추가 조치하고 blue-green 배포까지 완료했다. 원인: DB에서는 `branch-eonni-naengmyeon`이 inactive/소프트삭제 상태였으나, 프론트 `activeBranches()`가 `status !== "closed"` 기준이라 `inactive`도 활성 지점처럼 취급했고, `renderBusinessPage()`는 `cfg.branches` 전체로 카운트/사업자별 지점/지점목록을 렌더링했다. 또한 CEO 브라우저 localStorage에 예전 `branch-eonni-naengmyeon` active row가 남아 있으면 새 HTML에서도 다시 병합될 수 있었다. 조치: `app/static/apps/yeoljeong-finance/index.html`에 `REMOVED_BRANCH_IDS`/`pruneRemovedBranches()`/`isActiveBranch()`를 추가하고 `mergeSettings()`/`normalizeBusinessScope()`/`activeBranches()`/`selectedBusinessBranches()`/`renderBusinessPage()`/`renderSettings()`가 삭제 지점을 폐기하고 active 지점만 사용하도록 변경했다. 파일 원장 `app/data/yeoljeong_finance/settings.json`에 남아 있던 `branch-eonni-naengmyeon` active row도 제거해 fallback/재시작 시 재노출을 차단했다. 백엔드 `app/services/yeoljeong_finance_service.py`의 이전 조치(`CANONICAL_BRANCHES`에서 언니냉면 제거, 저장 payload 누락 지점 소프트삭제)는 유지한다. 검증: `python3 -m json.tool app/data/yeoljeong_finance/settings.json` 성공, `python3 -m py_compile app/services/yeoljeong_finance_service.py` 성공, Node 인라인 JS 파싱 성공, `git diff --check -- app/data/yeoljeong_finance/settings.json app/static/apps/yeoljeong-finance/index.html app/services/yeoljeong_finance_service.py docs/HANDOVER.md` 성공, DB active 지점은 4개(`중화점`, `성신여대점`, `성신여대역점`, `열정국밥_미아점`)로 확인. 배포: `bash /root/aads/aads-server/deploy.sh bluegreen`을 18:35:58 KST 실행했고 active 슬롯은 `8102`에서 `8100(aads-server)`으로 전환됐다. 배포 게이트는 Health/DB/Chat/LLM 모두 통과했고, nginx graceful reload 시 기존 슬롯 활성 스트림 3건은 기존 worker에서 유지되었다. 공개 URL은 HTTP 200/590,205 bytes, `REMOVED_BRANCH_IDS`/`pruneRemovedBranches()` 포함, `branch-eonni-naengmyeon` 기본 지점 객체 없음으로 확인했다. 컨테이너 내부 원장에서도 `branch-eonni-naengmyeon`은 제거됐고 `성신여대역점` 연동만 유지된다.
- AADS-FOOD-EONNI-INTEGRATION-LINK-20260824: 17:49:24 KST 기준 CEO 지시(`언니냉면 사업자정보 입력했다 연동설정부분이 다른세션통해서 미리 생성했을거야 사업자 지점으로 연결`)에 따라 언니냉면 사업자/지점/연동 원장 불일치를 직접 보정했다. 확인 결과 `yeoljeong_platform_accounts`의 판매채널 4개(배민, 쿠팡이츠, 요기요, 땡겨요)는 이미 `biz-eonni-naengmyeon`/`언니냉면`으로 연결되어 있었지만, DB `yeoljeong_businesses`에는 임시 사업자정보가 남아 있었고 `yeoljeong_settings(scope='ui')`에는 언니냉면 연동 UI 항목이 빠져 있었다. DB 트랜잭션으로 `yeoljeong_businesses`의 언니냉면 사업자정보(사업자번호 773-12-03049, 대표자 오병용, 일반과세, 개업일 2026-07-21, 주소 서울 성북구 동소문로 90 1층 102호)를 갱신하고, 지점 `branch-eonni-naengmyeon`/`80en7mlszkesr`를 `biz-eonni-naengmyeon`에 연결했다. 이어 `yeoljeong_settings.data.integrations`에 플랫폼 계정 원장 기반의 `srv-*` 4개 항목을 추가해 운영 화면 사업자 필터에서도 언니냉면 연동이 표시되도록 했다. 파일 원장 `app/data/yeoljeong_finance/settings.json`의 언니냉면 연동 4개도 `businessId/business_id=biz-eonni-naengmyeon`, `branch=언니냉면`으로 확인했다. 검증: DB SELECT 기준 사업자정보 1건, 언니냉면 지점 2건, UI settings 언니냉면 연동 4건, platform accounts 언니냉면 연동 4건 확인. 4개 연동의 포털 상태는 `action_required`로, 사업자/지점 연결은 완료됐지만 실제 자동수집은 PC Agent 포털 로그인/메뉴 확인 후 재실행이 필요하다. 코드 변경, 배포, 커밋, 푸시는 수행하지 않았다.
- AADS-FOOD-BUSINESS-REGISTRATION-PAGE-20260824: 17:26:08 KST 기준 CEO 지시(`실 운영서비스에 사업자정보 수정 및 신규등록페이지가 없다`)에 따라 운영 정적 앱 `app/static/apps/yeoljeong-finance/index.html#business`를 사업자/지점 현황 목록 중심 화면에서 사업자정보 등록·수정 전용 페이지로 확장했다. 신규 `사업자 신규등록/수정` 폼은 개인/법인 구분, 상호, 사업자등록번호, 대표자, 과세유형, 개업일, 사업장 주소, 사업자등록증 파일, 메모를 입력받고 기존 settings 원장에 저장한 뒤 `syncSettingsToServer()`를 호출한다. 신규 `지점 신규등록/수정` 폼은 지점명, 소속 사업자, 운영상태, 전화, 주소를 등록·수정하며 목록의 `수정`, `지점추가`, `삭제` 액션을 전용 폼으로 연결했다. 사업자 상세현황, 등록 후 정산계좌/연동관리/직원관리 후속 작업 카드, 사업자별 완성도/연동/지점/계좌 요약도 추가했다. 기존 설정 화면의 폼 ID와 충돌하지 않도록 `businessPageForm`, `branchPageForm`을 별도로 두고 기존 `businessForm`, `branchForm`은 유지했다. 검증: `git diff --check -- app/static/apps/yeoljeong-finance/index.html` 통과, Node `vm.Script` 인라인 JS 파싱 통과, 공개 URL `https://fb.newtalk.kr/static/apps/yeoljeong-finance/index.html?v=business-registration-20260824#business` HTTP 200/589,645 bytes, 공개 본문에서 신규 폼/렌더러/액션 키워드 확인, DOM 필수 키워드 검사 통과. Browser Bridge `capture_screenshot`은 timeout으로 실패해 HTTP/본문/JS 검증으로 대체했다. 17:30 KST 재확인 기준 커밋 `812bf88c feat(finance): add business registration page`가 `origin/main`에 반영됐고, 17:42 KST blue-green 배포에서 active `8100`에 포함 반영됐다.
- AADS-FOOD-BANK-AUTO-COLLECT-P0P1-IMPLEMENT-20260824: 06:04 KST 기준 CEO 지시(`앞으로 진행사항 P0,P1 즉시 직접 구현`)에 따라 은행 자동수집 기획서의 P0/P1 중 코드화 가능한 항목을 직접 반영했다. `app/services/auth_challenge_orchestrator.py`는 은행 화면 상태를 `login_required`, `otp_required`, `captcha_required`, `identity_check_required`, `certificate_password_required`, `transaction_table`, `no_records`, `parse_failed` 등으로 확장하고 소형 모델/provider 출력의 state/action/confidence allowlist 검증을 추가했다. `app/services/yeoljeong_bank_browser_connector.py`는 원본 HTML/민감값 저장 없이 안전 텍스트 요약으로 화면 상태를 분류하고, 비밀번호관리자 focus, OTP/CAPTCHA/인증서/본인인증 입력칸 focus, 1회 recheck, selector 후보 diagnostics를 collection diagnostics에 남기도록 보강했다. `scripts/yeoljeong_auto_collect.py`는 `BANK_BROWSER_AUTH_CHALLENGE_DETECTED`, 계좌/인증서 비밀번호 필요 상태를 blocking으로 분리하고 은행 diagnostics를 자동수집 결과에 보존한다. 검증: 일회성 `aads-server-aads-server` 컨테이너에서 `python -m pytest tests/unit/test_auth_challenge_orchestrator.py tests/unit/test_yeoljeong_bank_browser_connector.py tests/unit/test_yeoljeong_auto_collect.py` 62 passed, 추가 `python -m pytest tests/unit/test_bank_browser_connector.py tests/unit/test_yeoljeong_finance_service.py -k 'bank or Bank'` 62 passed/92 deselected, `python3 -m py_compile app/services/auth_challenge_orchestrator.py app/services/yeoljeong_bank_browser_connector.py scripts/yeoljeong_auto_collect.py` 통과, 대상 파일 `git diff --check` 통과. 커밋/푸시/배포는 아직 수행하지 않았다.

## 2026-08-21
- AADS-CHAT-RESUME-RECLAIM-FOLLOWUP-20260821: 18:11:15 KST 기준 중단 응답 복구 후속을 이어서 점검했다. 현재 작업트리에는 `app/main.py`, `tests/unit/test_bank_browser_connector.py`, `docs/CHANGELOG-dashboard-direct.md`, `docs/CHANGELOG-direct-edit.md`, `docs/CHANGELOG-go100-direct.md` 변경이 남아 있다. `app/main.py`는 `execution_resume_reclaim` APScheduler job을 등록해 standalone resume scanner가 실패하거나 누락될 때도 `_resume_pending_executions_once()`를 30초 간격으로 재호출하도록 보강된 상태다. `tests/unit/test_bank_browser_connector.py`는 은행 브라우저 세션 미지정 시 실제 커넥터가 반환하는 `PC_AGENT_LOGIN_REQUIRED` 계약에 맞춰 기대값을 정정했다. 검증: `python3 -m py_compile app/main.py app/services/yeoljeong_bank_browser_connector.py` 통과, `pytest tests/unit/test_bank_browser_connector.py -q` 38 passed, 현재 소스 read-only 마운트 일회성 컨테이너에서 `python -m py_compile app/main.py app/services/yeoljeong_bank_browser_connector.py` 통과, `python -m pytest tests/unit/test_chat_service.py -q -k "auto_resume or retrying or missing_done or owner"` 1 passed/63 deselected, `python -m pytest tests/unit/test_bank_browser_connector.py -q` 38 passed, `git diff --check` 통과. 기존 실행 중 컨테이너 `/app`는 현재 작업트리의 테스트 파일을 반영하지 않아 직접 검증 기준에서 제외했다. 커밋/푸시/배포는 아직 수행하지 않았다.
- AADS-FOOD-DDANGYO-CAPTCHA-APPROVAL-RESUME-20260821: 16:02 KST 기준 CEO 지시(`땡겨요 로그인페이지처럼 CAPTCHA가 있으면 AI 판단 후 승인받아 이후 자동화`) 후속으로 판매채널 수집의 땡겨요 숫자 CAPTCHA 처리 계약을 코드/CLI/스킬/기획서에 반영했다. 직접 CAPTCHA 해독/우회는 구현하지 않았고, PC Agent 화면/DOM이 `DDANGYO_NUMERIC_CAPTCHA_REQUIRED`를 감지하면 스크린샷과 동일 `work_key` 세션을 보존한 뒤 `operator_approved=true`와 1회성 `approved_input`이 들어온 실행에서만 입력·제출·재판정·수집 재개한다. `app/api/yeoljeong_finance.py`는 write-only `operator_approved`/`approved_input` payload를 받도록 보강했고, `app/services/yeoljeong_finance_service.py`는 승인 없는 `captcha_value`를 무시해 계정 원장 CAPTCHA 저장/재사용을 차단했다. `scripts/yeoljeong_auto_collect.py`에는 `--operator-approved --approved-input` 재시도 옵션을 추가했다. 문서와 `.claude/skills/sales-channel-collector/SKILL.md`도 같은 운영 계약으로 갱신했다. 검증: 소스 전체를 마운트한 `aads-server-aads-server:latest` 컨테이너에서 `python -m py_compile app/api/yeoljeong_finance.py app/services/yeoljeong_delivery_collectors.py app/services/yeoljeong_finance_service.py scripts/yeoljeong_auto_collect.py` 통과, `python -m pytest tests/unit/test_yeoljeong_finance_service.py -q` 116 passed, `git diff --check` 통과. 커밋/푸시/배포는 아직 수행하지 않았다.
- AADS-CHAT-RESUME-RECLAIM-SAFETY-NET-20260821: 15:33 KST 기준 중단 응답 복구 지연을 조사했다. 원인은 `chat_turn_executions`의 이전 execution `5e1484eb...`가 `retrying` 상태로 남았고, standalone execution resume scanner가 해당 row를 reclaim하지 못한 상태였다. `app/main.py`에 APScheduler 기반 `execution_resume_reclaim` 안전망을 추가해 30초 간격으로 `_resume_pending_executions_once()`를 호출하도록 보강했다. 운영 중 stuck execution은 삭제 없이 `_mark_execution_interrupted()`로 `superseded_by_newer_user:manual_stale_retrying_clear` 처리하여 `current_execution_id`를 해제하고 placeholder를 `_archived_partial`로 보존했다. 검증: `python3 -m py_compile app/main.py` 통과, 컨테이너 `python -m py_compile app/main.py` 통과, 컨테이너 관련 키워드 테스트 `python -m pytest tests/unit/test_chat_service.py -q -k "auto_resume or retrying or missing_done or owner"` 1 passed. 전체 `tests/unit/test_chat_service.py`는 기존 visible-message filter 문자열 기대값 1건으로 실패했다. active stream 3건이 있어 API reload/bluegreen 배포, 커밋, 푸시는 아직 수행하지 않았다.
- AADS-FOOD-AUTH-CHALLENGE-FULL-AUTO-PLAN-20260821: 15:33 KST 기준 CEO 지시(`OTP/CAPTCHA 등도 완전 자동화가 목표다. 자동화를 구현해라 이후 수동/자동만 운영적인부분만 승인여부로 진행하겠다 다시 기획해서 보고해`)에 따라 은행/판매채널 자동수집 기획을 재보강했다. `docs/plans/20260821_YEOLJEONG_BANK_AUTO_COLLECTION_PLAN.md`에는 공식 API/조회전용 계정/신뢰 PC Agent 세션/Vault write-only/상태 머신/소형 모델 판단을 묶은 완전 자동화 재기획, `AuthChallengeOrchestrator`, `SafeScreenJudge`, `CredentialActionGate`, `SessionContinuityManager`, `OperatorApprovalLedger` 모듈과 `auto`, `approval_required`, `manual_only` 운영 모드를 추가했다. `docs/plans/20260821_SALES_CHANNEL_PC_AGENT_COLLECTION_ARCHITECTURE.md`와 `.claude/skills/sales-channel-collector/SKILL.md`에는 CAPTCHA/OTP를 즉시 수동 차단으로만 보지 않고 자동 가능/승인 필요/수동 전용/정책 차단으로 분류하는 운영 기준과 완료 매트릭스를 반영했다. CAPTCHA 자동 판독, 허가 없는 OTP 생성/대리 입력, 외부 기기 조작, 권한 변경 동의 자동화는 정책 차단으로 명시했다. 이번 조치는 문서/스킬/HANDOVER 반영이며 코드 구현, DB 변경, 배포, 커밋, 푸시는 수행하지 않았다.
- AADS-FOOD-BANK-SMALL-MODEL-REALTIME-LOOP-20260821: 15:25 KST 기준 CEO 지시(`소형 AI모델을 활용해서 검증 판단 수정등 실시간 진행되게 하면 효율성이 올라오지 않을까?`)에 따라 `docs/plans/20260821_YEOLJEONG_BANK_AUTO_COLLECTION_PLAN.md`에 소형 모델 기반 실시간 검증·판단·수정 루프를 추가했다. 반영 내용은 DOM/파서 우선, 실패 시 민감값 제거 요약만 소형 모델에 전달, JSON schema 기반 상태 분류, allowlist selector 게이트, 1회 자동 수정 재시도, OTP/CAPTCHA 해독 금지, collection run 상태 기록 기준이다. 이번 조치는 기획서/HANDOVER 문서 반영이며 코드 구현, DB 변경, 배포, 커밋, 푸시는 수행하지 않았다.
- AADS-FOOD-BANK-AUTH-CHALLENGE-MINIMAL-APPROVAL-20260821: 15:20 KST 기준 CEO 지시(`OTP/CAPTCHA/본인인증/인증서 비밀번호 화면이면 operator_action_required로 멈추지 말고 자동처리 가능하면 처리하고 최소한으로 안되는부분만 CEO승인 형태로 진행`)에 따라 `docs/plans/20260821_YEOLJEONG_BANK_AUTO_COLLECTION_PLAN.md`를 재수정했다. 변경 내용은 OTP/CAPTCHA/본인인증/인증서 비밀번호 화면을 첫 감지 즉시 중단하지 않고 `auto_resumable`, `operator_input_needed`, `blocked_by_policy`로 분류해 자동처리 가능 항목을 먼저 수행하는 정책이다. 인증서 별칭 선택, 인증서 비밀번호 write-only 입력, 푸시 승인 polling, CEO 입력 감지 후 자동 재개는 자동처리 대상으로 두고, CAPTCHA 판독, 외부 OTP 기기 조작, 휴대폰 본인확인처럼 시스템이 처리할 수 없는 최소 단계만 `operator_action_required`로 남기도록 기획서를 보강했다. 이번 조치는 문서/HANDOVER 수정이며 코드/DB/배포/커밋/푸시는 수행하지 않았다.
- AADS-FOOD-BANK-AUTOLOGIN-PLAN-REVISION-20260821: CEO 지시(`은행 자동로그인 및 계좌비번등 완전 자동화 진행으로 다시 수정보고`, `자동수집 루프 상세 보고`, `PC에이전트 안정화`, `빠르고 작은 모델 적용 가능 여부`)에 따라 `docs/plans/20260821_YEOLJEONG_BANK_AUTO_COLLECTION_PLAN.md`를 보강했다. 추가 반영 내용: 비밀번호관리자 focus fallback과 Agent Vault write-only 자동입력 설계, 계좌비밀번호 자동입력 보안 경계, 은행별 매크로 상태 머신, 자동수집 루프 함수별 상세, PC Agent 세션 안정화 기준, 소형 모델 사용 범위. OTP/CAPTCHA/본인인증 우회는 금지로 유지했다.
- AADS-FOOD-BANK-AUTO-COLLECTION-PLAN-20260821: 14:27 KST 기준 CEO 지시(`은행데이터 자동수집 기획서 문서로 작성 보고`) 후속으로 현재 은행 자동수집 구현 상태를 코드/기존 handover 문서 기준으로 재확인하고 `docs/plans/20260821_YEOLJEONG_BANK_AUTO_COLLECTION_PLAN.md`를 작성했다. 문서에는 계좌 등록부, 은행 거래 원장, CSV/수동/목업 반영, PC Agent/Browser Bridge 수집, 자동수집 루프, 실은행 E2E 미완료, 파일 원장 DB 승격 계획, 보안 정책, 실패 처리, 테스트 계획, 운영 적용 순서를 정리했다. 검증: `python3 -m py_compile app/api/yeoljeong_finance.py app/services/yeoljeong_finance_service.py app/services/yeoljeong_bank_browser_connector.py scripts/yeoljeong_auto_collect.py` 통과, 현재 저장소 전체를 `aads-server-aads-server` 이미지에 `/workspace`로 마운트한 은행 관련 pytest 77 passed, `git diff --check -- docs/plans/20260821_YEOLJEONG_BANK_AUTO_COLLECTION_PLAN.md docs/HANDOVER.md` 통과. 이번 조치는 문서/HANDOVER 작성이며 코드/DB/배포/푸시는 수행하지 않았다.
- AADS-ARCH-DOCS-CANONICAL-CLEANUP-20260821: CEO 지시(`최신 아키텍처 문서와 관련문서 정리`, `개선 권장안 즉시 조치`) 후속으로 아키텍처 문서 공백을 정리했다. `docs/knowledge/CTO-SYSTEM-MAP.md`와 `docs/knowledge/5-LAYER-PROMPT-GOVERNANCE.md`는 origin/main에 반영돼 있음을 확인했고, 러너 read-only 처리로 main에 남지 않았던 프로젝트 별칭 문서는 `docs/knowledge/PROJECT-ALIAS-LAYER.md`로 직접 재작성했다. 러너 동시성/clean worktree/deploy preflight/parallel_group 보강은 `docs/pipeline-runner/PIPELINE-RUNNER-ARCHITECTURE.md`에 반영했다. `docs/ARCHITECTURE-INDEX.md`는 CTO 지도, 러너 아키텍처, 프로젝트 별칭 레이어를 현행 문서로 갱신하고 완료된 P0/P1 공백을 제거했다. 검증: `git diff --check` 통과, AADS health_check server 68 기준 HEALTHY(DB latency 148ms, pipeline running 0). 남은 이슈: `aads-docs` 별도 repo의 2026-03월 아키텍처 문서는 P2 노후 정리 대상으로 남아 있다.

## 2026-08-20
- AADS-SERVER68-RETIREMENT-FOLLOWUP-20260820: 17:32-17:35 KST 기준 CEO 지시(`68서버 종료했다 문제 여부 확인바람`, `문제 되는건 모두 조치`) 후속을 수행했다. 폐기 대상 구 서버 IP `68.183.183.11`은 SSH 시 기존 known_hosts와 다른 ED25519 host key를 반환하고 publickey 인증이 거부되어, 예전 AADS 서버로 신뢰 접속 가능한 상태가 아님을 확인했다. 운영 도메인 `https://aads.newtalk.kr/api/v1/health`는 HTTP 200, 운영 컨테이너는 `aads-server`, `aads-server-green`, `aads-dashboard`, `aads-dashboard-green`, `aads-postgres`, `aads-redis`, `aads-litellm` 모두 running/healthy 계열로 확인했다. PC Agent는 116 API 기준 online 2대(`DESKTOP-TBKF5M3`, `DESKTOP-TNO85R8`)로 확인했다. DB `monitored_services`의 레거시 서버 라벨 `68` 9건은 `contabo116`으로 정규화했으며, 재조회 결과 `aads-server`, `aads-postgres`, `nginx`, `watchdog-daemon`, `aads-redis`, `aads-dashboard`, `aads-socket-proxy`, `aads-litellm`, `aads-searxng`가 모두 `contabo116`/`ok`/`consecutive_failures=0`이다. 코드에는 `server_68` 하위호환 키가 116 상태를 그대로 보여 혼선을 만들 수 있어 `app/services/chat_tools.py`, `app/services/health_checker.py`, `app/api/watchdog.py`, `app/services/unified_healer.py`에 legacy alias 메타와 `contabo116` 정규 판정을 추가했다. 검증: `python3 -m py_compile app/services/chat_tools.py app/services/health_checker.py app/api/watchdog.py app/services/unified_healer.py` 통과, DB `SELECT` 재조회 통과, 외부 health HTTP 200, `docker ps` 운영 컨테이너 확인. 미완료: 코드 패치는 아직 커밋/푸시/배포하지 않았으므로 실행 중인 API의 `health_check` 도구 응답에는 `server_68` legacy 메타가 아직 반영되지 않았다. 작업트리에는 이번 변경 외 기존 dirty 파일 `app/api/browser_bridge.py`, `app/browser_bridge/service.py`, `docs/CHANGELOG-direct-edit.md`, `tests/unit/test_yeoljeong_finance_service.py`가 남아 있어 커밋 시 파일 선별이 필요하다.

## 2026-08-19
- AADS-FOOD-ALL-DELIVERY-AUTO-SYNC-20260819: 15:02 KST 기준 CEO 지시(`모두 자동 수집될수 있게 진행`)에 따라 매장비서 판매채널 자동수집 범위를 단일 선택 사업자·지점에서 등록된 모든 사업자·지점으로 확장했다. 백엔드 `queue_delivery_sync()`와 `sync_delivery()`는 `business_id=all`, `branch=전체`, `all_businesses=true` 요청을 전체 스코프로 해석하고, `사업자|지점|서비스` run key로 `delivery_collection_status` queued/running/final 상태를 서비스×지점 단위로 분리 저장한다. 기존 단일 지점 호출은 `queued_run_ids[service]` 호환을 유지한다. 운영 화면 `전체 동기화` 버튼은 배달 4사 수집을 전체 스코프로 큐잉하고, 개별 `수집 실행` 버튼은 기존처럼 해당 항목 스코프로 유지한다. 전체 스코프 API가 오래된 상태 원장 전체를 매번 DB 업서트해 응답이 지연될 수 있는 문제도 함께 보정해, 상태 원장은 파일 전체 저장+현재/신규 row DB 업서트 방식으로 제한했다. 단위 테스트에는 전체 스코프 큐 등록과 중화점/미아점 다중 지점 수집 회귀를 추가했고, PC Agent 자동 탐색이 서버 headless/업로드 필요 테스트에 섞이지 않도록 테스트별 브라우저 인증 옵션을 명시했다. 검증: `python3 -m py_compile app/services/yeoljeong_finance_service.py app/api/yeoljeong_finance.py` 통과, 현재 소스 마운트 컨테이너 `JWT_SECRET_KEY=test-secret-key-for-unit-tests-only python3 -m pytest tests/unit/test_yeoljeong_finance_service.py tests/unit/test_yeoljeong_finance_api.py tests/unit/test_yeoljeong_delivery_collectors.py tests/unit/test_yeoljeong_finance_print_static.py -q` 107 passed, 대상 파일 `git diff --check` 통과. 운영 배포와 실제 전체 수집 실행은 후속 단계에서 수행한다.
- AADS-CHAT-HIDDEN-RESPONSE-LEDGER-RECONCILE-20260819: 14:11-14:20 KST 기준 CEO 재지시(`최종 완료보고 조건 충족`, `commit/push/deploy/document ledger 충돌 해소`)에 따라 직전 "응답 작성 후 세션 미노출" 조치의 실제 상태를 재검증했다. `main`과 `origin/main`은 `a54278de`로 일치했지만 작업트리에 `app/services/chat_service.py` 미커밋 변경이 남아 있어 이전 완료보고의 커밋/푸시/배포 판정은 불완전했다. 런타임 `aads-server` 컨테이너에는 `_already_interrupted` 중복 처리 방지와 `render` 메시지 목록 heavy column 제외 표식이 이미 존재했고, 컨테이너 기준 `pytest tests/unit/test_chat_service.py::test_list_messages_minimal_is_read_only_and_selects_light_fields tests/unit/test_chat_service.py::test_list_messages_render_keeps_content_and_omits_heavy_detail_fields tests/unit/test_chat_service.py::test_cleanup_overlong_running_executions_closes_live_task`는 3 passed로 통과했다. `chat_workspace_change_ledger`에는 현재 세션의 `HANDOVER.md`/`docs/HANDOVER.md`/onboarding 업로드 파일이 `dirty`로 남아 있었지만 실제 `git status --short -- HANDOVER.md docs/HANDOVER.md app/data/yeoljeong_finance/uploads/onboarding`는 clean이라 stale ledger로 분리했다. 남은 조치는 코드/문서 커밋, push, 정식 배포 재검증 및 ledger 상태 정리다.
- AADS-SERVER-CANONICAL-FINAL-VERIFY-20260819: 13:09 KST 기준 CEO 재지시(`최종 완료보고 조건 충족`, `남은 확인/조치/검증 계속 수행`)에 따라 서버 신 명칭 통일 작업을 최종 재검증했다. 대시보드 `/ops/servers` 토폴로지 SVG가 카드 목록과 달리 구 ID `68/211/114`를 조회해 `UNKNOWN`으로 보일 수 있는 잔여 결함을 발견했고, `aads-dashboard` 커밋 `897c139`(`fix(infra): use canonical server names in topology`)으로 `contabo14`, `contabo116`, `cafe24_114` 노드/상태 조회로 교정해 `dashboard-write/main`에 푸시했다. `/root/aads/aads-dashboard/deploy.sh` blue-green 배포는 active 슬롯을 blue `:3100(aads-dashboard)`로 전환하고 standby green까지 동일 release `897c13984b0f`로 동기화했다. 검증: 대시보드 `npx eslint src/app/ops/servers/page.tsx src/app/server-status/page.tsx src/components/chat/ArtifactDashboard.tsx` 통과, `npm run build` 통과, 배포 로그 `dashboard-deploy-20260819-130100.log` 기준 nginx reload/external health/standby sync/release 확인 성공, `docker inspect` 기준 `aads-dashboard`와 `aads-dashboard-green` 모두 `AADS_RELEASE_SHA=897c13984b0f`, 공개 `/ops/servers`와 `/server-status`는 비로그인 307 로그인 보호 정상. 백엔드 컨테이너 양 슬롯은 이미 `app/services/server_registry.py`에 `contabo116`, `contabo14`, `cafe24_114`를 포함하고 있었으나, 재실행한 `bash deploy.sh bluegreen`은 standby target `aads-server:8100`에 활성 스트림 1건(`d19a0e9e`)이 남아 안전장치로 중단되어 강제 배포하지 않았다. active API `:8102` health는 OK, `/api/v1/ops/health-check`는 `pipeline_healthy=true`, stalled/running pipeline 0, 컨테이너는 `aads-server`, `aads-server-green`, `aads-dashboard`, `aads-dashboard-green`, `aads-postgres`, `aads-redis`, `aads-litellm` healthy. 대시보드 내장 Step 7 QA는 `UNKNOWN`이라 성공으로 간주하지 않고 API/HTTP/컨테이너 검증으로 대체했다. 미완료/후속: `server_env_history` 컬럼 구조상 최신 서버별 스냅샷은 별도 수집 작업이 필요하고, `ops/recovery` 등 복구/부속 화면의 구 번호 필터는 이번 통합 서버 현황 화면 범위 밖 P1으로 남았다. 디스크 정리는 CEO 지시대로 제외했다.
- AADS-SERVER-CANONICAL-FINAL-DEPLOY-20260819: 12:21-12:40 KST 기준 CEO 지시(`서버 신 명칭 contabo116, contabo14, cafe24_114`, `디스크 정리는 추후`) 후속 배포 차단을 해소하고 서버/대시보드 개선을 운영 반영했다. 배포 전 main workdir dirty로 Pipeline Runner 배포가 차단되어, 관련 없는 기존 dirty 파일은 임시 stash로 보존한 뒤 clean 상태에서 백엔드 `bash deploy.sh bluegreen`을 실행했다. 백엔드는 active 슬롯을 `:8100`에서 `:8102(aads-server-green)`으로 전환했고 health, DB schema, chat table, LLM 서비스 검증이 통과했다. 대시보드는 `/root/aads/aads-dashboard/deploy.sh`로 active 슬롯을 blue `:3100(aads-dashboard)`로 전환했고 standby green 동기화까지 완료했다. 검증: `python3 -m py_compile app/services/server_registry.py app/core/project_config.py app/api/ops.py app/api/admin.py app/services/cross_server_checker.py app/services/health_checker.py` 통과, `http://127.0.0.1:8102/api/v1/health` 200, `http://127.0.0.1:8102/api/v1/ops/health-check` 200, `https://aads.newtalk.kr/ops/servers` 및 `/server-status` 비로그인 307 로그인 보호 확인, `docker ps` 기준 `aads-server-green`, `aads-dashboard`, `aads-dashboard-green`, `aads-postgres` healthy. 서버 레지스트리는 `contabo116=5.104.86.116:22`, `contabo14=5.104.86.14:22`, `cafe24_114=114.207.244.86:7916` 및 프로젝트 매핑 `AADS→contabo116`, `KIS/GO100→contabo14`, `SF/NTV2/NAS→cafe24_114`로 확인했다. 대시보드 배포 스크립트 Step 7 QA는 `UNKNOWN`으로 끝나 성공 판정에 포함하지 않았고, API/HTTP/컨테이너 검증으로 대체했다. 기존 dirty 변경은 복원했으며 `nginx-aads-upstream.conf` stash 복원은 슬롯 정합성 보호를 위해 제외했다.
- AADS-SERVER-CANONICAL-DASHBOARD-DEPLOY-20260819: 12:01-12:10 KST 기준 서버 현황 UI 개선 후속 배포를 완료했다. 대시보드 커밋 `9f3f968`(`fix(infra): consolidate server status dashboard`)은 `dashboard-write/main`에 푸시했고, `/root/aads/aads-dashboard/deploy.sh` blue-green 배포로 active dashboard 슬롯을 green(3101)으로 전환했다. green 헬스체크, nginx test/reload, external `/login` 헬스체크, standby blue 동기화가 모두 성공했고 최종 로그는 `/root/aads/aads-dashboard/deploy-logs/dashboard-deploy-20260819-120148.log`다. 공개 비로그인 검증은 `/server-status`와 `/ops/servers` 모두 307 로그인 리다이렉트로 admin 보호 동작을 확인했다. API blue-green은 기존 `deploy.sh` 프로세스가 `docker compose ... aads-server-green` 단계에서 5분 이상 정체되어 중복 배포가 차단됐고, active 서비스가 HEALTHY임을 확인한 뒤 해당 배포 프로세스에 TERM을 보내 lock을 정리했다. 따라서 백엔드 커밋 `2210acad`는 `origin/main`에 푸시 완료이나 API 컨테이너 이미지/프로세스 재기동은 미완료다. AADS health는 12:09:40 KST 기준 HEALTHY(DB latency 116ms, disk 62%, pipeline running 0)다. 기존 미커밋 변경은 배포 전 stash로 임시 보관 후 복원했으며, dashboard predeploy stash는 pop 과정에서 기존 dirty 파일이 복원되어 별도 stash 항목이 남지 않았다. server predeploy stash는 복원/병합 후 drop했다.
- AADS-SERVER-CANONICAL-UI-CLEANUP-20260819: 11:50-12:00 KST 기준 CEO 지시(`서버 신 명칭 contabo116, contabo14, cafe24_114`, `디스크 정리는 추후`)에 따라 서버 명칭/접속 정보 정리 후속을 수행했다. 백엔드 `app/services/server_registry.py`와 `app/core/project_config.py`는 이미 정규 서버 3대(`contabo116=5.104.86.116`, `contabo14=5.104.86.14`, `cafe24_114=114.207.244.86`) 기준으로 반영되어 있음을 재확인했고, 추가로 `app/api/project_docs.py`의 KIS/SF/NTV2 문서 스캔 호스트를 `contabo14`/`cafe24_114`로 교정했다. Runner 관련 스크립트와 systemd 샘플(`scripts/pipeline-runner.sh`, `scripts/pipeline-runner.sh.local`, `scripts/aads-pipeline-litellm-runner.211.service`, `scripts/aads-pipeline-litellm-runner.114.service`, `scripts/tg_approval_bot.py`)의 폐기 IP `68.183.183.11`, `211.188.51.113`, `116.120.58.155`를 신 IP로 교체했다. 대시보드에서는 레거시 `/server-status` 화면을 `/ops/servers`로 리다이렉트해 중복 서버 화면을 제거하고, 채팅 아티팩트 미니 서버 카드의 구 서버명/IP를 신 명칭으로 교정했다. 검증: KST DB 시간 조회 `2026-08-19 11:50:40`, AADS `health_check` HEALTHY(디스크 58%, DB latency 128ms), `python3 -m py_compile app/api/project_docs.py app/services/server_registry.py app/core/project_config.py` 통과, `git diff --check` 통과, AST 기반 `project_docs SERVER_CONFIG OK`, 대시보드 `npx eslint src/app/server-status/page.tsx src/components/chat/ArtifactDashboard.tsx` 통과, `npm run build` 통과. 미검증/미완료: 호스트에는 `pytest`/`structlog`가 없고 `.venv/bin/python`은 누락된 `/usr/local/bin/python3.11`을 가리켜 pytest는 미실행했다. 컨테이너 `/app`은 아직 배포 전 이미지 코드라 구 테스트가 보인다. `server_env_history` 최신 row는 2026-04-21 KST `unknown`으로 DB 스냅샷 수집 최신화는 별도 P1 작업으로 남겼다. 디스크 정리는 CEO 지시대로 제외했다.
- AADS-FOOD-PRO-TAX-MANAGEMENT-MOCKUP-20260819: 09:16-09:23 KST 기준 CEO 지시(`매장비서에 전문 세무관리기능 권장조치 즉시 반영하고 기획서 아키텍처 보완`)에 따라 공개 시안 `app/static/apps/yeoljeong-finance/mockup-v2.html#tax`를 단순 세무 일정 화면에서 전문 세무관리 화면으로 확장했다. 반영 범위는 부가세 매출·매입 대장(`vat-ledger`), 원천세·간이지급명세서·지급명세서 상세(`withholding-ledger`), 증빙 누락·불공제 검토(`evidence-gap`), 신고서 A4 미리보기(`tax-return-preview`), 납부서 A4 미리보기(`tax-payment-preview`), 세무대리인 검토·승인함(`tax-agent-approval`)이다. 국세청 홈택스 전자세금계산서/현금영수증/카드매입 수집, 부가세 대사, 원천세/지급명세서 제출, 수기증빙 OCR 보완, 대표 승인 후 세무대리인 읽기전용 공유 흐름을 시안 데이터 기준으로 반영했다. 디자인 기획서 `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`에는 전문 세무관리 아키텍처 섹션을 추가하고 P0 로드맵에 세무관리 원장·승인함을 보강했다. 기존 운영 `index.html`, API, DB, 배포 스크립트는 변경하지 않았다. 검증은 `git diff --check`, `python3 -m html.parser` 2개 HTML, Node 인라인 JS 파싱 1개, 공개 시안 URL HTTP 200/203,852 bytes 및 공개 기획서 HTTP 200/23,720 bytes, 공개 본문 grep, AADS `capture_screenshot`으로 수행했다. 최초 캡처는 timeout이었으나 전용 세션 재시도에서 `#tax` 화면 캡처 `https://aads.newtalk.kr/screenshots/screenshot_20260819_092334_f5b2b1.png`를 확보했다. AADS 컨테이너는 `docker ps` 기준 `aads-server`, `aads-server-green`, `aads-dashboard` healthy다. 커밋/푸시/정식 배포는 별도 지시 전 미수행한다.
- AADS-FOOD-INTEGRATION-BACKGROUND-SYNC-20260819: 10:12 KST 기준 CEO 지시(`우선 다음단계 진행하고 연동 수집은 백그라운드에서 처리될수 있게 조치`)에 따라 매장비서 판매채널 연동 수집을 요청-응답 대기형에서 백그라운드 큐 방식으로 전환했다. `POST /api/v1/yeoljeong-finance/accounts`의 판매채널 `auto_sync`와 `POST /api/v1/yeoljeong-finance/sync`의 `background=true` 요청은 `delivery_collection_status`에 `queued` 상태와 `delivery-sync-*` job id를 즉시 저장하고, FastAPI `BackgroundTasks`가 동일 run id를 `running` 후 최종 상태로 갱신한다. 운영 HTML은 `수집대기/처리중` 배지와 버튼 문구를 표시하고 20초 타임아웃을 10분 백그라운드 지연 안내로 조정했다. 후속 검증 중 쿠팡이츠/요기요/땡겨요 실패 안내가 배민 문구로 고정될 수 있는 버그를 확인해 서비스별 포털 라벨 메시지로 보정했다. 검증: 로컬 `python3 -m py_compile app/api/yeoljeong_finance.py app/services/yeoljeong_finance_service.py` 통과, 로컬 소스 마운트 컨테이너 `pytest tests/unit/test_yeoljeong_finance_api.py tests/unit/test_yeoljeong_finance_service.py tests/unit/test_yeoljeong_finance_print_static.py tests/unit/test_yeoljeong_delivery_collectors.py -q` 100 passed, Node 인라인 JS 파싱 `inline-script-ok 351929`, `git diff --check` 통과, active green 슬롯 `aads-server-green:8102` 헬스 200 및 외부 정적 URL 표식 확인. 운영 API 무인 curl은 인증 401로 차단되어 정상 보호 동작으로 판정했다.

## 2026-08-04
- AADS-PC-AGENT-AUTO-PAIRING-INSTALL-P0-20260804: PC Agent 신규 사용자 설치 UX를 수동 토큰 입력 기본값에서 자동 페어링 설치 기본값으로 전환했다. 백엔드 `app/api/kakao_bot.py`는 1회용 설치 티켓 발급/교환 API(`POST /api/v1/kakao-bot/agent/install-ticket`, `POST /api/v1/kakao-bot/agent/install-ticket/exchange`)와 EXE 다운로드 시 `install_ticket` 파일명 주입을 제공하고, `pc_agent/launcher.py`는 환경변수/인자/다운로드 파일명에서 티켓을 추출해 토큰으로 교환한 뒤 `config.json`에 저장한다. 대시보드 `src/app/kakaobot/agent/page.tsx`는 설치 버튼이 티켓 발급 후 `/download-exe?install_ticket=...`로 다운로드하도록 변경했고, 수동 토큰 UI는 자동 설치 실패 시 백업으로 낮췄다. 검증: `python3 -m py_compile app/api/kakao_bot.py pc_agent/launcher.py pc_agent/updater.py` 통과, 컨테이너 `docker exec aads-server python3 -m pytest tests/unit/test_pc_agent_download.py tests/unit/test_pc_agent_launcher_startup.py` 9 passed, 대시보드 `npx tsc --noEmit` 통과, 대상 페이지 eslint 통과, 운영 `POST /api/v1/kakao-bot/agent/install-ticket` 비로그인 401, 운영 `/api/v1/kakao-bot/agent/download-exe` HTTP 200/21,488,046 bytes 확인. 대시보드 자동 페어링 커밋 `1ed9ae6`, `bc76024`는 `origin/main` 및 `dashboard-write/main`에 반영 완료. 기존 `AADS-PC-AGENT-TOKEN-UI-P0-20260804`의 "대시보드 빌드 배포 예정" 메모는 이 항목으로 후속 완료 처리한다.
- AADS-PC-AGENT-TOKEN-UI-P0-20260804: P0 PC Agent 신규 사용자 설치 시 토큰 생성 UI 미표시 문제 수정. 확인된 원인: ①`launcher.py`가 config.json에 `agent_token` 없으면 설정창 진입 후 토큰 위치를 못 찾음, ②백엔드 `GET /agent/token`이 없어 설치 페이지에서 기존 토큰 조회 불가, ③설치 페이지(`/kakaobot/agent`)에 토큰 발급/복사 섹션이 없어 신규 사용자가 토큰 생성 위치를 찾지 못함. 조치: `app/api/kakao_bot.py`에 `GET /agent/token` (Depends(get_current_user), user_id+tenant_id 기준 최신 토큰 반환, 없으면 빈 token+안내 메시지)와 `kakao_pc_agent_tokens` 테이블 `ADD COLUMN IF NOT EXISTS user_id TEXT, tenant_id UUID` 마이그레이션 및 `idx_kakao_pc_agent_tokens_owner_recent` 인덱스를 추가했다(커밋 `a71e9f8f`). 대시보드 `src/app/kakaobot/agent/page.tsx`에 '에이전트 등록 토큰' 카드(GET 조회→없으면 발급 버튼, 있으면 토큰+복사+재발급) 추가, FAQ에서 잘못된 포트 8080 제거하고 토큰/방화벽/WebSocket 기준 안내로 교체했다(커밋 `8013586`). 설치 가이드 step 4의 '아래 에이전트 등록 섹션' 문구를 '위 에이전트 등록 토큰 섹션'으로 교정(토큰 카드가 가이드 상단에 위치), `settings/page.tsx` `handleGenerateToken`에 `!res.ok` 체크 추가 및 서버 오류 메시지 표시 보강(커밋 `c744d5d`). gksdk7466 판정: id=40/gksdk7466@gmail.com role=user(고객 워크스페이스 owner), id=df746609/gksdk7466@naver.com role=admin(internal). 특정 사용자 하드코딩 없음—현재 로그인 user_id+tenant_id 기준 동작. 검증: `python3 -m py_compile app/api/kakao_bot.py` 통과, `npx eslint src/app/kakaobot/agent/page.tsx settings/page.tsx` 경고 없음, `npx tsc --noEmit` exit=0, backend reload+health-check HTTP 200. 대시보드 배포는 후속 자동 페어링 커밋 `1ed9ae6`, `bc76024`와 함께 완료됐고, 2026-08-04 19:45 KST 재검증에서 운영 대시보드 Blue/Green 빌드 산출물 모두 `install-ticket`/`PC 에이전트 자동 설치`/`수동 백업` 문구를 포함한다.

## 2026-08-03
- AADS-RUNNER-ADMIN-MODEL-CLAIM-PATCH-20260803: 08:49 KST 기준 CEO 지시(`현재 러너 작업모델이 어드민 설정값으로 반영이 안되고 있는듯하다`, `즉시 권장 조치 진행하고 배포까지`)에 따라 Shell Pipeline Runner의 일반 claim 경로가 `worker_model`만 반환해 제출 단계에서 계산된 어드민 `model` 컬럼을 버리던 문제를 수정했다. `scripts/pipeline-runner.sh`와 `scripts/pipeline-runner.sh.local`의 일반 runner `model_return_expr`를 `COALESCE(worker_model, model, auto)`로 통일해, 직접 override가 없으면 `/admin/model-routing`의 `runner_model_config` 1순위 모델이 실행 모델로 내려간다. 회귀 테스트 `tests/unit/test_pipeline_runner_script_guards.py`에 일반 claim이 `model` 컬럼을 사용하는지 검증을 추가했다. 검증: `bash -n` 2개 스크립트 통과, `python3 -m py_compile app/api/pipeline_runner.py app/services/pipeline_runner_service.py` 통과, 컨테이너 `docker exec aads-server python3 -m pytest tests/unit/test_pipeline_runner_script_guards.py` 10 passed. 로컬 pytest는 미설치로 실패해 컨테이너 테스트로 대체했다. 08:55 KST `deploy.sh bluegreen` 배포 완료, active 슬롯은 `:8102/aads-server-green`, health/DB/채팅/LLM 검증 통과, 프론트 변경 없음으로 QA skip. 배포 후 active 컨테이너에서 `_get_model_for_size('M') = codex:gpt-5.6-luna` 확인. 단, 08:57 KST 기준 NTV2/GO100 러너 2건이 실행 중이라 host `aads-pipeline-runner.service` 재시작은 작업 중단 방지를 위해 보류했다.

## 2026-08-02
- AADS-OHVIS-REALTIME-RESPONSE-ORCHESTRATION-PLAN-20260802: 21:11 KST 기준 CEO 추가 지시(`별도 문서로 작업해서 보고`)에 따라 OHVIS 5초 즉답, 심층레이어, 맥락캐시, 백그라운드 진행 실시간 표시, 1차/2차/3차 질문 완료 순서 역전 시 응답 귀속/종합보고 정책을 별도 상세 기획서로 작성했다. 신규 문서 `docs/reports/20260802_OHVIS_REALTIME_RESPONSE_ORCHESTRATION_PLAN.md`는 현재 상태 실측(최근 7일 completed 169건, interrupted 31건, active 2건, streaming placeholder 1건, assistant 소요시간 데이터 192/334건, 컨테이너 healthy), 목표 UX, Chat Turn Gateway, Immediate Response Layer, Deep Work Layer, Context Cache Layer, Response Coordinator, SSE/API 이벤트, P0/P1/P2 구현 로드맵, 완료 기준, 롤백 범위를 포함한다. 기존 초안 `docs/reports/20260802_OHVIS_5SEC_IMMEDIATE_RESPONSE_ARCHITECTURE.md`는 보존했고, 이번 문서는 추가 지시 반영용 상세 별도 문서다. 검증: `date '+%Y-%m-%d %H:%M:%S %Z'`, `docker ps --format`, PostgreSQL SELECT 4건, `git diff --check -- docs/reports/20260802_OHVIS_REALTIME_RESPONSE_ORCHESTRATION_PLAN.md docs/HANDOVER.md` 수행. 커밋/푸시/배포는 별도 지시 전 미수행한다.

## 2026-07-31
- AADS-DIRECT-WORK-DEPENDENCY-POLICY-20260731: 12:03-12:18 KST 기준 CEO 지시(`상세 기획을 보고하고 운영 정책반영해서 보고`)에 따라 채팅 직접 코드/DB 작업의 의존성 운영정책을 문서와 프롬프트 DB 에셋으로 반영했다. 신규 문서 `docs/knowledge/DIRECT-WORK-DEPENDENCY-POLICY-v1.0.md`는 Runner 경로(`pipeline_jobs.depends_on`, `parallel_group`, `actual_changed_files`)와 채팅 직접 경로(`chat_workspace_change_ledger`, `git_project_lock`)의 차이를 명시하고, GREEN/YELLOW/RED/BLOCK 프리플라이트 판정, DB 직접 변경 허용 조건, Runner 전환 기준, 후속 자동화 백로그를 정의한다. `docs/knowledge/DEV-FLOW-v1.1.md`와 `docs/knowledge/DEV-FLOW-CHECKLIST-v1.0.md`에는 직접 작업 전 활성 runner/ledger/git status 확인과 동일 파일 충돌 시 Runner `depends_on` 전환 규칙을 추가했다. 운영 정책 DB 반영은 migration `migrations/117_direct_work_dependency_policy_prompt.sql`의 `global-direct-work-dependency-gate` L1 prompt asset으로 수행했다. 검증: `date '+%F %T %Z'` → `2026-07-31 12:17:17 KST`, DB SELECT 결과 `slug=global-direct-work-dependency-gate`, `layer_id=1`, `priority=15`, `enabled=true`, `content=1238 chars`; AADS `pipeline_jobs`는 queued/running/claimed/awaiting_approval 활성 작업 없음, AADS `chat_workspace_change_ledger`는 `dirty=1457`, `deployed=14`, `pushed=41`; 운영 컨테이너 `PromptCompiler.compile(workspace=AADS,intent=code_modify,model=gpt-5.6-sol,role=CTO)` 결과 해당 asset 적용 성공, compile error 없음, `applied_count=17`, `slug_index=2`, `system_prompt_chars=20745`; 공개 health check는 HTTP 200; `compiled_prompt_provenance`의 실제 persisted 채팅 turn 기록은 아직 0건이라 남은 관찰 항목으로 분리한다. `git diff --check` 통과. 커밋/푸시/배포는 별도 지시 전 미수행한다.
- AADS-FOOD-INTEGRATION-ADD-DRAWER-20260731: 10:54-11:05 KST 기준 CEO 지시(`운영에서 연동관리 페이지에 연동추가페이지가 디자인과 다르게 적용`)에 따라 `app/static/apps/yeoljeong-finance/index.html#auth-invite`의 `+ 연동 추가` 화면을 `mockup-v2.html#integrations` 시안과 같은 우측 드로어형 서비스 선택 페이지로 조정했다. 기존 중앙 대형 모달/구형 폼 진입을 우측 패널(`width min(560px)`, full-height drawer)로 바꾸고, 첫 화면에는 `판매사이트: 배민 · 쿠팡이츠 · 요기요 · 땡겨요`, `은행: 신한은행 기업 · 기업은행 기업`, `매입처: 쿠팡 · 마켓봄 · 뉴통 · 발주고 · 직접 추가`, `홈택스 · 전자세금계산서`, `주문 프로그램 없는 매입처 증빙 등록`, `추가 권장 연동 후보` 버튼만 노출한다. 실제 입력/저장은 별도 `connect-form` 상세 경로로 분리해 기존 `/accounts` Vault 저장 및 `/transactions/sync` 실행 흐름을 유지했다. 검증: `python3 -m html.parser app/static/apps/yeoljeong-finance/index.html` 통과, Node 인라인 JS 파싱 통과, `git diff --check -- app/static/apps/yeoljeong-finance/index.html docs/HANDOVER.md` 통과, 공개 URL `https://fb.newtalk.kr/static/apps/yeoljeong-finance/index.html` HTTP 200 및 `integration-add-list`/`connect-form`/은행 문구 확인. Browser Bridge 캡처는 `localhost:9333/json/version` 응답 없음으로 실패해 DOM/HTTP/API 검증으로 대체했다. 이 항목은 관련 파일만 격리 커밋·푸시·배포 완료보고로 마감한다.
- AADS-RUNNER-CONCURRENCY-GUARDS-20260731: 10:45:42 KST 기준 CEO 지시(`같은 프로젝트 여러 세션 동시 작업 의존성 문제 다음 단계 모두 진행`)에 따라 Pipeline Runner 동시성 보강 3건을 구현했다. `parallel_group` 값이 Shell Runner 클레임 결과와 `run_job()`까지 전달되며 Redis work lock은 `scope=parallel_group`을 받아 그룹별 키(`work_lock:{project}:{scope}`)로 분리된다. 모든 로컬 Git 작업은 기존처럼 `/tmp/aads-wt-{job_id}` clean worktree에서 실행하되, 로그에 `parallel_group`을 남긴다. Redis work lock 동시 실행 상한은 하드코딩 2에서 `MAX_CONCURRENT_PER_PROJECT` 및 `MAX_CONCURRENT_PER_PROJECT_{PROJECT}`로 통일했다. Runner 실행 후 실제 변경 파일은 `git diff --name-only` 기준으로 `pipeline_jobs.actual_changed_files` JSONB와 `logs` 이벤트(`actual_changed_files_recorded`)에 기록된다. 적용 파일: `app/services/deploy_lock.py`, `app/api/ops.py`, `app/api/pipeline_runner.py`, `scripts/pipeline-runner.sh`, `scripts/pipeline-runner.sh.local`, `migrations/112_pipeline_jobs_actual_changed_files.sql`, `tests/unit/test_pipeline_runner_script_guards.py`, `tests/unit/test_pipeline_runner_reliability.py`, `docs/HANDOVER.md`. 운영 DB에는 `actual_changed_files jsonb DEFAULT '[]'::jsonb` 컬럼과 `idx_pipeline_jobs_actual_changed_files_gin` 인덱스를 적용했고 `information_schema.columns` 재조회로 확인했다. 검증: `bash -n scripts/pipeline-runner.sh`, `bash -n scripts/pipeline-runner.sh.local`, `python3 -m py_compile app/services/deploy_lock.py app/api/ops.py app/api/pipeline_runner.py`, `docker exec aads-server python3 -m pytest tests/unit/test_pipeline_runner_script_guards.py tests/unit/test_pipeline_runner_reliability.py tests/unit/test_pipeline_runner_worktree_policy.py` 21 passed, `git diff --check` 통과. 로컬 `/usr/bin/python3 -m pytest ...`는 pytest 미설치로 실패해 컨테이너 pytest로 대체했다. 커밋/푸시/배포는 아직 수행하지 않았다.
- AADS-CHAT-STREAM-P0-RESUME-20260731: 10:40 KST 커밋 `b4295faf`. CEO 지시(세션 `8472efcb` "응답이 중단됨" 버블 멈춤 + stale running/retrying 정리 + Transport closed 도구오류를 모델 폴백과 분리)에 따라 3건 P0/P1 패치를 적용했다. ①`app/services/chat_service.py`의 `_AUTO_RESUME_PROCESS_INTERRUPTION_PREFIXES`에 `llm_first_response_timeout` 추가 — LLM 180초 무응답(`_FIRST_RESPONSE_TIMEOUT_SEC`) 중단이 자동 재개 대상에서 누락되어 터미널 버블로 멈추던 문제 해소. ②`cleanup_overlong_running_executions()`에 stale `retrying` 정리 블록 추가(`max(timeout//2, 300)`초 초과 시 `stale_retrying_cleanup_after_{age}s` 사유로 `interrupted` 전환, 빈 placeholder 삭제, 반환값에 `retrying_cleaned` 추가). ③`app/services/model_selector.py` codex_cli 경로에 `_codex_is_tool_error` 판정 추가 — `transport closed`/`mcp`/`tool_error`/`connection reset` 키워드가 에러 본문에 있으면 모델 폴백 체인(`_CODEX_FB`)을 타지 않고 `error_category="tool_transport_error"`로 즉시 반환. ④`app/services/loop_chat_handler.py`에 `loop_start_confirm` 인텐트와 `handle_loop_start_confirm()` 추가(START 판정에서 `_NON_COMMAND_HINT` 가드 제거, `_INTERVAL_START_RE` 도입). 검증: `python3 -m py_compile` 3파일 통과, `docker exec aads-server bash /app/scripts/reload-api.sh` 무중단 hot-reload, `https://aads.newtalk.kr/api/v1/ops/health-check` HTTP 200, 컨테이너 `/app/app/services/chat_service.py` 내 `llm_first_response_timeout` 4회 매치(호스트 동일), DB `chat_turn_executions` stale `retrying` 0건·`running` 4건 모두 age 600초 이내. 커밋 `b4295faf` + `origin/main` 푸시 완료. 대상 세션 `8472efcb` 실행 원장: `4ffb7045` interrupted(`llm_first_response_timeout_after_182s:heartbeat`, 10:05:48 KST), `8af8e6a7` completed(10:16:03), `4a832363` running(10:27:32, 현재 턴). 미완료: 자동 재개 실동작 E2E(다음 first_response_timeout 발생 시 관측 필요), 재시도 시 OAuth 슬롯 교대 로직(P1), Claude relay 측 동일 tool/model 오류 분리(P1), relay `client already closed` 고아 프로세스 정리(P2).
- AADS-LOOP-P0P1-CONFIRM-KEYWORD-FIX-20260731: 10:45 KST. 위 ④ 후속 결함 수정. `handle_loop_start_confirm()`이 출력하는 확인 프롬프트가 `'진행해'`로 승인하라고 안내했으나 `LOOP_CONFIRM_KW = ("루프 시작", "루프 진행", "루프 승인", "루프 생성")`에 `"진행해"`가 없어 CEO가 안내대로 응답해도 루프가 생성되지 않는 불일치가 있었다(안내문-판정 로직 불일치). 안내 문구를 `'루프 진행'`으로 교정해 실제 판정 키워드와 일치시켰고, 사용하지 않는 `import time`(ruff F401 유발)을 제거했다. 검증: `docker exec aads-server python3 -m py_compile app/services/loop_chat_handler.py` 통과, `detect_loop_intent` 실행 검증 10/10 통과(`완료시까지 진행하고 보고해`→`loop_start_confirm`, `매 10분마다 헬스체크해`→`loop_start_confirm`, `될 때까지 재시도해`→`loop_start_confirm`, `루프 진행`/`루프 시작`→`loop_start`, `루프 구현상태 보고해`/`아니 그냥 답해줘`/`P0 즉시 조치하고 P1은 되묻기로 우선 반영해서 조치해`→`None`, `루프 중지`→`loop_stop`, `루프 상태`→`loop_status`). 남은 리스크: `"루프 시작"`은 `LOOP_CONFIRM_KW`와 `LOOP_START_KW`에 모두 있고 `LOOP_CONFIRM_KW`가 먼저 평가되므로 확인 프롬프트 없이 즉시 생성된다(명시적 명령이므로 의도된 동작). 확인 프롬프트→승인→루프 생성 채팅 E2E는 미실행.

## 2026-07-30
- AADS-FOOD-DESIGN-CONFLICT-RECHECK-20260730: 09:46:15-09:49:30 KST 기준 CEO 재지시(`다른 세션에서 디자인파일을 건든 모양이다 확인하고 다시 반영하고 보고`)에 따라 완료 조건을 다시 실측했다. `git status -sb` 기준 `main...origin/main`이며 `HEAD`는 `f4e52972 fix: loop_executor 파라미터 수정 + OAuth auth_token 인증 수정`이다. 작업트리는 dirty 상태이고 이번 요청 직접 대상은 `app/static/apps/yeoljeong-finance/mockup-v2.html`, `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`, `docs/HANDOVER.md` 3개이다. 로컬 시안에서 `data-view="payroll"`, `documentPreviewOverlay`, `직원관리`, `급여관리`, `A4 210mm x 297mm`, `PDF 다운로드와 인쇄 전` 문구를 확인했고, 디자인 기획서에서 `직원관리/급여관리 분리`, `#payroll`, `PDF 다운로드와 인쇄 전 중앙 미리보기 모달`, `A4` 설계를 확인했다. 공개 시안 `https://fb.newtalk.kr/static/apps/yeoljeong-finance/mockup-v2.html?v=codex-recheck-20260730-0946`는 HTTP 200/215,183 bytes로 응답했고 공개 본문 핵심 키가 모두 확인됐다. 공개 기획서 `https://fb.newtalk.kr/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html?v=codex-recheck-20260730-0946`는 HTTP 200/20,880 bytes로 응답했고 핵심 키가 모두 확인됐다. 최초 `capture_screenshot`은 active Browser Bridge `localhost:9333` 응답 없음으로 실패했으나, `browser_connect(action=ensure_work_session, work_key=food-design-recheck-20260730)`로 전용 세션 `bb-b663b43a2e04`를 확보한 뒤 공개 캡처를 완료했다. 캡처: `#employees` https://aads.newtalk.kr/screenshots/screenshot_20260730_094844_16b5d3.png, `#payroll` https://aads.newtalk.kr/screenshots/screenshot_20260730_094930_45d653.png. 검증은 `git diff --check -- app/static/apps/yeoljeong-finance/mockup-v2.html app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html docs/HANDOVER.md`, Python 정적 문자열 검증(`static-checks=13 ok`), Node 인라인 JS 파싱(`inline scripts parsed 1`), 공개 URL HTTP/본문 확인, AADS `health_check(server=68)` HEALTHY로 수행했다. `python3 -m pytest tests/unit/test_yeoljeong_finance_print_static.py -q`는 `/usr/bin/python3: No module named pytest`로 미실행이다. 커밋/푸시/정식 `deploy.sh`는 수행하지 않았고, 기존 unrelated 미커밋/미추적 파일이 함께 남아 있어 커밋 시 대상 파일 선별이 필요하다.
- AADS-FOOD-DESIGN-CONFLICT-LEDGER-CLOSEOUT-20260730: 09:42:25-09:44:47 KST 기준 CEO 지적(`deploy_report_conflicts_with_ledger`, `document_report_conflicts_with_ledger`)에 따라 최종 완료보고 조건을 재실측했다. 현재 `HEAD`는 `f4e52972`이고 `git status --short` 기준 작업트리는 dirty 상태이며, 이번 요청 직접 대상은 `app/static/apps/yeoljeong-finance/mockup-v2.html`, `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`, `docs/HANDOVER.md`이다. 로컬 시안에는 `직원관리`/`급여관리` 좌측 메뉴 분리, 모바일 `급여` 메뉴, 신규 `#payroll` 뷰, `documentPreviewOverlay`, 작성된 계약서 A4 미리보기, 급여명세서 A4 미리보기, PDF/인쇄 전 중앙 모달, 계약서/급여명세서 문서 액션이 확인됐다. 공개 `https://fb.newtalk.kr/static/apps/yeoljeong-finance/mockup-v2.html?v=final-ledger-20260730-0942`는 HTTP 200/215,184 bytes로 응답했고 공개 본문에서 `data-view="payroll"`, `documentPreviewOverlay`, `직원관리`, `급여관리`, `작성된 계약서 미리보기`, `급여명세서 미리보기`, `A4 210mm x 297mm`를 확인했다. 공개 디자인 기획서 `https://fb.newtalk.kr/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html?v=final-ledger-20260730-0942`는 HTTP 200/20,879 bytes로 응답했고 `직원관리/급여관리 분리`, `#payroll`, `PDF 다운로드와 인쇄 전 중앙 미리보기 모달`, `A4` 설계가 확인됐다. 공개 캡처는 `#employees` https://aads.newtalk.kr/screenshots/screenshot_20260730_094354_c85d54.png, `#payroll` https://aads.newtalk.kr/screenshots/screenshot_20260730_094447_f453cc.png 로 확보했다. 검증은 `git diff --check -- app/static/apps/yeoljeong-finance/mockup-v2.html app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html docs/HANDOVER.md`, Node 인라인 JS 파싱(`scripts=1 ok`), 공개 본문 `rg`, Python 정적 문자열 검증(`static-checks=8 ok`), AADS `capture_screenshot`으로 수행했다. `python3 -m pytest tests/unit/test_yeoljeong_finance_print_static.py -q`는 `/usr/bin/python3: No module named pytest`로 미실행이다. 커밋/푸시/정식 `deploy.sh`는 수행하지 않았고, 기존 미커밋/미추적 파일이 함께 남아 있어 커밋 시 대상 파일 선별이 필요하다.
- AADS-FOOD-DESIGN-CONFLICT-FINAL-CLOSEOUT-20260730: 09:36:53 KST 기준 CEO 지적(`document_report_conflicts_with_ledger`)에 따라 완료조건을 재검증했다. 로컬 `app/static/apps/yeoljeong-finance/mockup-v2.html`에는 좌측/모바일 메뉴의 `직원관리`/`급여관리` 분리, 신규 `#payroll` 뷰, `documentPreviewOverlay`, 작성된 계약서 A4 미리보기, 급여명세서 A4 미리보기, PDF/인쇄 전 중앙 모달, `render(normalizeHash(), false)` 라우팅이 확인됐다. 공개 `mockup-v2.html?v=final-verify-20260730-0937`는 HTTP 200/215,184 bytes로 응답했고 공개 본문에서도 `data-view="payroll"`, `documentPreviewOverlay`, `직원관리`, `급여관리`, `작성된 계약서 미리보기`, `급여명세서 미리보기`, `A4 210mm x 297mm`가 확인됐다. 공개 캡처는 `#employees` https://aads.newtalk.kr/screenshots/screenshot_20260730_093942_2eb587.png, `#payroll` https://aads.newtalk.kr/screenshots/screenshot_20260730_094043_cda4bb.png 로 확보했다. 디자인 기획서 공개 URL은 HTTP 200이며 `직원관리/급여관리 분리`, `#payroll`, `PDF 다운로드와 인쇄 전 중앙 미리보기 모달`, `A4` 설계가 확인됐다. 검증은 `git status --short`, `rg` 로컬/공개 본문 확인, 공개 URL HTTP 확인, `git diff --check`, Node 인라인 JS 파싱, Python 정적 문자열 검증, AADS `capture_screenshot`으로 수행했다. `python3 -m pytest tests/unit/test_yeoljeong_finance_print_static.py -q`는 시스템 파이썬에 `pytest`가 없어 미실행했고 대체 정적 문자열 검증으로 보완했다. 커밋/푸시/정식 `deploy.sh`는 수행하지 않았다. 작업트리에는 이번 대상 외 기존 미커밋 변경이 남아 있어 커밋 시 파일 선별이 필요하다.
- AADS-FOOD-DESIGN-CONFLICT-FINAL-VERIFY-20260730: 09:31:43 KST 기준 CEO 지적(`최종 완료보고 조건 미충족`, `document_report_conflicts_with_ledger`)에 따라 `app/static/apps/yeoljeong-finance/mockup-v2.html`, 디자인 기획서 `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`, `docs/HANDOVER.md`를 재검증했다. 로컬 시안에는 좌측/모바일 메뉴의 `직원관리`/`급여관리` 분리, 신규 `#payroll` 뷰, `documentPreviewOverlay`, 작성된 계약서 A4 미리보기, 급여명세서 A4 미리보기, PDF/인쇄 전 중앙 모달이 존재한다. 공개 `https://fb.newtalk.kr/static/apps/yeoljeong-finance/mockup-v2.html?v=final-ledger-20260730`는 HTTP 200/209,328 bytes로 응답했고 공개 본문에서도 `data-view="payroll"`, `documentPreviewOverlay`, `직원관리`, `급여관리`, `작성된 계약서 미리보기`가 확인됐다. 로컬 파일 SHA-256은 `5258656d6d19d8a2456ce7f6361dbfeeec2349a69def0b943cdf49b1510d38f0`, 공개 응답 SHA-256은 `b458cc766a28e64bea89884cf8ff198aef35cc5ac117d0b87d1180167cc78a0c`로 다르지만, `diff` 결과 공개 응답 차이는 Cloudflare가 `<body>` 직후 숨김 링크와 `</body>` 직전 challenge script를 삽입한 것뿐이며 원본 기능 차이가 아니다. 검증은 `git diff --check` 통과, Node 인라인 script 파싱 1개 통과, Python 정적 문자열 검증 9개 통과, 디자인 기획서 공개 URL HTTP 200/20,912 bytes 및 공개 본문 grep으로 수행했다. `python3 -m pytest tests/unit/test_yeoljeong_finance_print_static.py -q`는 시스템 파이썬에 `pytest`가 없어 미실행했고, 대체 정적 문자열 검증으로 보완했다. 커밋/푸시/정식 `deploy.sh`는 수행하지 않았다. 작업트리에는 이번 대상 파일 외 기존 미커밋 변경이 남아 있어 커밋 시 대상 파일 선별이 필요하다.
- AADS-FOOD-DESIGN-CONFLICT-REAPPLY-20260730: 09:22:58 KST 기준 CEO 지시(`다른 세션에서 디자인파일을 건든 모양이다 확인하고 다시 반영`)로 `app/static/apps/yeoljeong-finance/mockup-v2.html`를 재감사했다. `docs/HANDOVER.md`와 디자인 기획서에는 직원관리/급여관리 분리, A4 문서 미리보기 모달 기록이 남아 있었지만 실제 `mockup-v2.html`은 `#payroll` 뷰와 `documentPreviewOverlay`가 빠지고 `#employees`가 다시 `직원·급여` 통합 화면으로 돌아간 상태였다. 좌측/모바일 메뉴에 `직원관리`와 `급여관리`를 분리 복구하고, `#employees`는 직원 직접 가입·입사서류 OCR·계약조건 입력·작성된 계약서 A4 미리보기·상황별 양식함·문서보관 중심으로 재반영했다. 신규 `#payroll`은 급여 대상/지급 예정/검토 예외/명세서 KPI, 급여 마감 워크플로우, 직원별 급여 상세표, 근태·수당 예외, 급여명세서 A4 미리보기, 대표 승인, 은행 지급예약 상세 드로어를 포함한다. 계약서/급여명세서 PDF 다운로드와 인쇄 버튼은 바로 실행하지 않고 중앙 `documentPreviewOverlay` 모달에서 A4 문서, 민감정보 마스킹, 권한, 최신 버전을 확인한 뒤 최종 실행 버튼을 누르는 흐름으로 복구했다. 기존 운영 `index.html`, API, DB는 변경하지 않았다. 검증은 `git diff --check`, Node 인라인 JS 파싱 1개 통과, 공개 `mockup-v2.html?v=reapply-20260730-0922` HTTP 200/209,327 bytes, 공개 본문 grep(`급여관리`, `data-view="payroll"`, `documentPreviewOverlay`, `작성된 계약서 미리보기`)로 수행했다. 공개 캡처: `#employees` https://aads.newtalk.kr/screenshots/screenshot_20260730_092926_2a8b5d.png, `#payroll` https://aads.newtalk.kr/screenshots/screenshot_20260730_093020_c890b7.png. 커밋/푸시/정식 배포는 수행하지 않았다.
- AADS-FOOD-EMPLOYEE-PAYROLL-SPLIT-20260730: 08:38:21 KST 기준 CEO 지시(`mockup-v2.html#employees 직원관리와 급여관리는 별도 메뉴로 분리하고 급여관리 상세페이지 디자인 반영`)를 `app/static/apps/yeoljeong-finance/mockup-v2.html`와 디자인 기획서 `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`에 반영했다. 좌측/모바일 메뉴의 `직원·급여`를 `직원관리`로 변경하고 신규 `급여관리` 메뉴 `#payroll`을 추가했다. `#employees`는 직원 회원가입, 입사서류/OCR, 계약조건 입력, 작성된 계약서 A4 미리보기, 전자서명, 문서보관, 계약 만료/보완 요청 중심으로 정리하고 급여 계산 상세는 제거했다. 신규 `#payroll`은 급여 대상/지급 예정/검토 예외/명세서 KPI, 급여 마감 워크플로우, 근태·수당 예외표, 지점별 지급 요약, 직원별 급여 상세표, 급여명세서 A4 미리보기, PDF/인쇄 전 모달, 대표 승인, 은행 지급예약 상세 드로어를 포함한다. 추가 액션 `attendance-detail`, `payroll-adjustment-detail`, `payroll-statement-detail`, `payroll-payment-detail`, `payroll-approval-detail`을 드로어 라우터에 연결했고, 직원 상세의 급여명세 CTA와 공제 예외 버튼도 급여관리 상세 드로어로 연결했다. 기존 운영 API/DB는 변경하지 않았다. 검증은 `git diff --check`, Node 인라인 JS 파싱, 공개 `mockup-v2.html#payroll` HTTP 200/222,448 bytes, 디자인 기획서 HTTP 200/20,911 bytes, 공개 본문 grep으로 수행했다. 공개 캡처: `#payroll` https://aads.newtalk.kr/screenshots/screenshot_20260730_084355_13d1fd.png, `#employees` https://aads.newtalk.kr/screenshots/screenshot_20260730_084431_ae5a37.png.
- AADS-FOOD-CONTRACT-LEGAL-A4-FIX-20260730: 08:35:10 KST 기준 CEO 지시(`한국 표준근로계약서와 프리랜서 계약서 등 양식 확인, A4 사이즈 적용, 법적 문제 없게 내용 반영`)를 재검토했다. 공식 기준은 고용노동부 `표준 근로계약서(서식 모음)` 게시본(근로조건 서면명시·교부, 표준근로계약서 5종), 고용노동부 `노무제공자를 위한 공통 표준계약서 및 활용가이드` 2023-12-26 게시본, 2026년 적용 최저임금 고시 시간급 10,320원 기준으로 확인했다. 운영 `app/static/apps/yeoljeong-finance/index.html#contracts`는 고용노동부 표준근로계약서 기준 참고, 근로기준법 제17조 필수 확인, 2026년 최저임금, A4 210mm x 297mm, 노무제공자 공통 표준계약서 기준, 프리랜서 오분류 방지, 고정 근무표·출퇴근·상시 교대 근무 표현 차단 문구를 포함하는 상태로 확인했다. 이번 직접 수정은 공개 시안 `app/static/apps/yeoljeong-finance/mockup-v2.html`의 계약조건 양식명을 `고용노동부 표준근로계약서`/`노무제공자 공통 표준계약서`로 정리하고, 문서 미리보기 `.paper-preview`를 A4 210mm x 297mm 비율로 고정하며, PDF/인쇄 전 A4 모달 문구와 프리랜서 검수·정산·해지·독립성 체크를 보강한 것이다. `tests/unit/test_yeoljeong_finance_print_static.py`에는 운영/시안 A4·공식기준 문자열 회귀 테스트를 추가했다. 검증은 `git diff --check`, Node `vm.Script` 인라인 JS 파싱(index/mockup 각 1개 script), Python 정적 문자열 검증 14개 통과로 수행했다. `python3 -m pytest ...`는 시스템 파이썬에 pytest가 없어 미실행했다. 커밋/푸시/배포는 수행하지 않았다.
- AADS-FOOD-DOCUMENT-PREVIEW-MODAL-20260730: 08:02:53 KST 기준 CEO 지시(`PDF다운, 인쇄 전 모달팝업으로 미리보기`)를 `app/static/apps/yeoljeong-finance/mockup-v2.html#employees` 직원·급여 시안에 반영했다. 기존에는 계약서/보고서/문서보관함/급여명세서 버튼이 우측 상세 드로어 또는 단순 상태변경 토스트로 끝나 실제 다운로드·인쇄 전 확인 UX가 부족했다. 공통 `documentPreviewOverlay` 중앙 모달을 추가하고, 계약서·급여명세서·CEO 경영보고서·문서보관함 보관본 타입별 A4형 프리뷰와 확인 체크리스트(누락 필드, 민감정보 마스킹, 권한, 버전/감사로그)를 표시하도록 했다. `PDF 다운로드`, `인쇄`, `명세서 PDF`, `명세서 인쇄`, 문서보관함 다운로드/인쇄 CTA는 모두 이 모달을 먼저 거치며, 모달 하단의 최종 실행 버튼에서만 완료 상태가 표시된다. 디자인 기획서 `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`에도 PDF/인쇄 전 중앙 미리보기 모달 기준을 반영했다. 기존 운영 `index.html`, API, DB는 변경하지 않았다. 검증은 `git diff --check`, Node 인라인 JS 파싱, 공개 URL HTTP 200/207,324 bytes, 디자인 기획서 URL HTTP 200/20,489 bytes, 공개 HTML 본문 grep, 문서 미리보기 액션 10곳 연결 확인, AADS 서버68 health `HEALTHY`로 수행했다. 공개 캡처: https://aads.newtalk.kr/screenshots/screenshot_20260730_080435_0f5bc8.png. 로컬 Chromium/Playwright 패키지는 없어 클릭 E2E는 DOM/JS 검증과 캡처로 대체했다.
- AADS-FOOD-EMPLOYEES-CONTRACT-PAYROLL-DETAIL-20260730: 07:25:58 KST 기준 CEO 지적(`mockup-v2.html#employees`에 직원 계약서 작성, 계약조건, 작성된 계약서 미리보기, 직원/급여 상세 페이지와 상황별 양식이 보이지 않음)을 재검토했다. 원인은 계약/서류 관련 일부 기능이 드로어 액션에는 있었지만 `#employees` 첫 화면에는 직원 목록, 입사서류, 간단 계약조건 폼 중심으로만 노출되어 실제 업무 화면에서 계약 작성·미리보기·급여 상세가 누락처럼 보이는 구조였다. `app/static/apps/yeoljeong-finance/mockup-v2.html`의 `#employees`를 KPI, 입사서류/OCR, 직원별 계약/서류/서명 상태, 계약조건 입력 워크벤치, 작성된 계약서 A4형 미리보기, 상황별 직원·계약 양식함, 급여·근태 상세 현황까지 한 화면에서 보이도록 확장했다. 추가 드로어로 `contract-condition-detail`, `contract-preview`, `form-library`, `payroll-detail`을 만들고 액션 라우팅 목록에 포함해 표준/단시간/기간제/수습/일용직/3.3%/외국인/미성년 양식 구분, 급여/수당/공제/명세서 미리보기, 퇴사정산 등 상황별 양식 흐름을 실제 버튼으로 확인할 수 있게 했다. 디자인 기획서 `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`에도 같은 직원·계약·급여 설계를 반영했다. 기존 운영 `index.html`, API, DB는 변경하지 않았다. 검증은 `git diff --check`, Node `vm.Script` 인라인 JS 파싱, 공개 URL HTTP 200/196,389 bytes, 공개 HTML 본문 grep, Browser Bridge `#employees` ARIA 스냅샷, 계약서 미리보기/급여 상세 버튼 클릭과 드로어 렌더 확인으로 수행했다. 캡처: https://aads.newtalk.kr/screenshots/screenshot_20260730_072936_f8184c.png.

## 2026-07-29
- AADS-FOOD-EMPLOYEE-SIMPLE-SIGNUP-SPLIT-20260729: 07:10:41 KST 기준 CEO 추가 지시에 맞춰 매장비서 공개 시안 `app/static/apps/yeoljeong-finance/mockup-v2.html#auth-invite`를 직원 간편 회원가입 화면으로 재정리했다. 첫 가입 화면은 이메일 아이디, 비밀번호, 소속, 휴대폰번호 4개 입력값만 받도록 축소하고, 이메일 인증은 가입 CTA 이후 발송/완료 상태로 표현했다. 로그인 후 직원 전용 첫 화면 `#auth-employee-home`을 추가해 기본정보 입력과 입사서류 등록을 분리 카드로 배치했다. `#auth-employee-profile`은 가입값인 이메일·소속·휴대폰을 자동 표시하고 이름, 생년월일, 주소, 긴급연락처, 급여계좌, 지점, 직무, 입사유형, 근무 가능 요일/시간을 입력·선택하는 화면으로 정리했다. `#auth-employee-documents`는 기본정보 저장 후 별도 입사서류 등록 화면으로 유지하며 필수 서류, OCR 자동 추출, 보완 요청, 관리자 계약 작성 연결을 표시한다. 디자인 기획서 `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`도 같은 흐름으로 갱신했다. 기존 운영 `index.html`, API, DB는 변경하지 않았다. 검증은 `git diff --check`, Node 인라인 JS 파싱, 공개 URL HTTP 200/186,162 bytes, 공개 HTML 본문 grep, 직접 해시 화면 캡처로 수행했다. 캡처: `#auth-invite` https://aads.newtalk.kr/screenshots/screenshot_20260729_071440_7a8f9b.png, `#auth-employee-home` https://aads.newtalk.kr/screenshots/screenshot_20260729_071536_a7366e.png, `#auth-employee-profile` https://aads.newtalk.kr/screenshots/screenshot_20260729_071722_c916d3.png, `#auth-employee-documents` https://aads.newtalk.kr/screenshots/screenshot_20260729_071628_087689.png.

## 2026-07-28
- AADS-FOOD-EMPLOYEE-AUTH-FIRST-VIEW-FIX-20260728: 08:13:39 KST 기준 CEO 지적 후 최종 재감사에서 공개 URL `mockup-v2.html#auth-invite`의 라우터와 DOM 렌더는 정상이나, 모바일 첫 뷰포트에서 직원 회원가입 입력 폼보다 공통 권한 설명 패널이 먼저 보여 "회원가입 부분이 안 보인다"는 UX 체감이 남을 수 있음을 확인했다. `app/static/apps/yeoljeong-finance/mockup-v2.html`의 `authShell()`에 선택형 `shellClass`를 추가하고, `auth-invite`, `auth-employee-profile`, `auth-employee-documents` 3개 직원 인증 화면에 `employee-auth` 레이아웃을 적용했다. 또한 `auth-invite` 필드 순서를 `사업장 코드 → 아이디·이메일 → 이메일 인증번호 → 비밀번호 → 비밀번호 확인` 우선으로 재배치하고 지점/이름/휴대폰/직무는 보조정보로 내렸다. 이 변형은 직원 화면에서 입력 카드가 권한 설명보다 먼저 렌더되게 하며, 모바일에서도 핵심 가입 입력값이 첫 화면에 바로 보이도록 한다. 검증은 `git diff --check`, Node `vm.Script` 인라인 JS 파싱, 공개 URL HTTP 200/본문 grep, Playwright 모바일 DOM 평가와 스크린샷으로 수행했다. 기존 운영 `index.html`, API, DB는 변경하지 않았다.
- AADS-FOOD-EMPLOYEE-AUTH-FINAL-LEDGER-20260728: 08:05:26 KST 기준 CEO 지적(`mockup-v2.html#auth-invite` 직원 회원가입 화면 미노출) 후속 완료 상태를 재감사했다. 최신 커밋 `ca7a89438a52b287ec633e8bce9e330a5d3c28d6`은 로컬 `HEAD`와 `origin/main`이 일치하며, 직전 기능 커밋 `ca7a8943`에는 `app/static/apps/yeoljeong-finance/mockup-v2.html`, `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`, `docs/HANDOVER.md`가 포함되어 있다. 공개 URL `https://fb.newtalk.kr/static/apps/yeoljeong-finance/mockup-v2.html?v=auth-final`은 HTTP 200/184,685 bytes로 응답했고, 공개 HTML 본문에서 `직원 회원가입`, `이메일 인증`, `비밀번호 설정`, `로그인 후 기본정보`, `입사서류 등록`, `normalizeHash`, `views[slug] || authScreens[slug] || views.home`을 확인했다. Browser Bridge 캡처는 `https://aads.newtalk.kr/screenshots/screenshot_20260728_080644_22c4e8.png`로 성공했다. 검증은 `git diff --check -- app/static/apps/yeoljeong-finance/mockup-v2.html docs/HANDOVER.md`, Node `vm.Script` 인라인 JS 파싱, `git ls-remote origin main`, 공개 HTML grep, 서버68 `health_check`로 수행했다. 대상 시안 파일과 기획 파일은 clean 상태이며, 작업트리에 남은 다른 dirty/untracked 파일은 이번 요청 범위 밖 기존 변경으로 분리했다. 정식 `deploy.sh`는 실행하지 않았고, 공개 정적 URL에서 최신 HTML 제공을 확인했다.
- AADS-FOOD-EMPLOYEE-AUTH-RENDER-FIX-20260728: 07:57:33 KST 기준 `#auth-invite` 직접 진입 화면 미노출 원인을 재검증했다. 1차 보정에서 `normalizeHash()`는 `authScreens` 키를 허용했지만, 실제 `render()` 함수가 `views[slug] || views.home`만 사용해 인증 화면 데이터(`authScreens["auth-invite"]`, `authScreens["auth-employee-profile"]`, `authScreens["auth-employee-documents"]`)를 렌더 소스로 쓰지 못하는 추가 버그가 확인됐다. `render()`를 `views[slug] || authScreens[slug] || views.home` 기준으로 수정해 공개 URL `mockup-v2.html#auth-invite` 직접 진입 시 직원 회원가입 화면이 실제 렌더되도록 보정했다. 검증은 `git diff --check`, Node 인라인 JS 파싱, 공개 URL HTTP 200, 공개 HTML 본문 grep, Browser Bridge 캡처로 수행했다. 기존 운영 `index.html`, API, DB는 변경하지 않았다.
- AADS-FOOD-EMPLOYEE-AUTH-HASH-ROUTE-FIX-20260728: 07:53:10 KST 기준 CEO가 `https://fb.newtalk.kr/static/apps/yeoljeong-finance/mockup-v2.html#auth-invite`에서 직원 회원가입 화면이 보이지 않는다고 지적한 건을 확인했다. 원인은 직원 회원가입 디자인 본문은 `authScreens["auth-invite"]`에 존재하고 공개 HTML에도 배포되어 있었지만, 초기 진입 라우터 `normalizeHash()`가 `views`만 허용하고 `authScreens` 키를 허용하지 않아 `#auth-invite`, `#auth-employee-profile`, `#auth-employee-documents` 직접 URL 진입 시 기본 `home` 화면으로 되돌아가는 버그였다. `app/static/apps/yeoljeong-finance/mockup-v2.html`의 `normalizeHash()`를 `views[slug] || authScreens[slug]` 기준으로 수정해 인증/회원가입 화면 해시 직접 진입을 허용했다. 검증은 `git diff --check`, Node 인라인 JS 컴파일, 공개 URL HTTP 200 및 본문 grep, 직접 라우트 문자열 검증, Browser Bridge 캡처 `https://aads.newtalk.kr/screenshots/screenshot_20260728_075611_c29912.png`로 수행했다. 운영 `index.html`, API, DB는 변경하지 않았다.
- AADS-FOOD-EMPLOYEE-SIGNUP-PROFILE-DOCS-MOCKUP-20260728: 07:36:44 KST 기준 CEO 추가 지시에 맞춰 매장비서 공개 시안 `app/static/apps/yeoljeong-finance/mockup-v2.html#auth-invite`를 직원 이메일 인증 회원가입 흐름으로 재정리했다. 직원 아이디는 이메일로 받고 이메일 인증번호, 비밀번호/확인, 사업장 코드, 휴대폰, 선택 초대 코드, 보조 본인인증을 입력한다. 가입 완료 CTA는 관리자성 `employees` 화면이 아니라 신규 `#auth-employee-profile` 화면으로 이동하도록 바꿨다. `#auth-employee-profile`에는 직원 로그인 후 주소, 생년월일, 긴급연락처, 급여계좌, 희망 지점, 직무, 입사유형, 근무 가능 요일/시간 입력·선택 디자인을 추가했고, `#auth-employee-documents`에는 직원 본인의 입사서류 파일 등록, OCR 자동 추출값, 제출/보완 상태, 관리자 계약작성 연결 디자인을 추가했다. 디자인 기획서 `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`도 이메일 인증 회원가입 → 첫 로그인 기본정보 입력·선택 → 입사서류 등록 → 관리자 계약작성 순서로 갱신했다. 기존 운영 `index.html`, API, DB, Docker/nginx 설정은 변경하지 않았다. 검증은 `git diff --check`, Node `vm.Script` 인라인 JS 컴파일, 공개 시안/기획서 URL HTTP 200, 공개 HTML 본문 grep, 07:48 KST Browser Bridge 캡처 `https://aads.newtalk.kr/screenshots/screenshot_20260728_074803_a26e50.png`로 수행했다.
- AADS-FOOD-EMPLOYEE-AUTH-MOCKUP-FINAL-VERIFY-20260728: 07:15:15 KST 기준 `app/static/apps/yeoljeong-finance/mockup-v2.html#auth-invite`와 디자인 기획서 `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`을 재검증했다. 공개 URL은 HTTP 200이고 공개 HTML 본문에서 `직원 회원가입`, `사업장 코드`, `비밀번호 확인`, `초대 코드 또는 링크 토큰`, `가입 후 제출할 입사서류 미리보기`, `관리자 계약서 작성 흐름`을 확인했다. Browser Bridge에서 제목 `매장비서 · 직원 회원가입 시안`, ARIA 스냅샷의 직원 회원가입 필드/CTA 노출, 필수값 입력과 동의 체크 후 `회원가입 후 입사서류 제출` 클릭 시 `직원·급여` 화면 전환을 확인했다. 캡처는 `https://aads.newtalk.kr/screenshots/screenshot_20260728_071114_d17fb4.png`다. 로컬 검증은 `git diff --check`와 Node `vm.Script` 인라인 JS 컴파일을 통과했다. 이번 재검증에서 기존 운영 `index.html`, API, DB, Docker/nginx 설정은 변경하지 않았다.
- AADS-FOOD-EMPLOYEE-AUTH-MOCKUP-20260728: 07:00:51 KST 기준 매장비서 공개 시안 `app/static/apps/yeoljeong-finance/mockup-v2.html#auth-invite`를 직원 초대 수락 보조 화면에서 직원 직접 회원가입 기본 화면으로 전환했다. 직원은 사업장 코드, 근무 지점, 이름, 휴대폰, 이메일, 비밀번호/확인, 입사 유형, 직무, 입사 예정일, 선택 초대 코드, 본인인증 방식을 입력하고 가입 후 입사서류 제출로 이동한다. 화면에는 가입요청 생성, 사업장 코드 기준 지점/권한 후보 자동 연결, 입사서류 업로드, 관리자 OCR 검토/계약서 작성, 직원 전자서명/문서보관 흐름을 단계형으로 배치했다. 로그인 화면과 프로필 계정 메뉴의 진입 라벨도 `직원 회원가입`으로 정리했고, 직원·급여 메뉴의 첫 단계도 `직원 직접 회원가입` 기준으로 맞췄다. 디자인 기획서 `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`도 같은 기준으로 갱신했다. 검증은 `git diff --check`, 인라인 JS `vm.Script` 컴파일, 공개 URL HTTP 200 및 공개 HTML 본문 grep, Browser Bridge 캡처/ARIA 스냅샷, 필수값 입력 후 제출 버튼 클릭으로 수행했고 `직원·급여` 화면 전환을 확인했다. 기존 운영 `index.html`, API, DB는 변경하지 않았다.
- AADS-FOOD-DESIGN-PLAN-SYNC-20260728: 06:53 KST 기준 매장비서 화면 시안과 디자인 기획서의 불일치를 확인했다. `app/static/reports/20260716_yeoljeong_store_assistant_architecture_design_plan.html`은 2026-07-16 기준 직원/계약/급여 중심 문서라 최신 CEO 지시인 FB 로그인/직원 직접 회원가입 우선, 판매사이트 ID/PW/2차인증, 은행 기업뱅킹 인증서/OTP, 홈택스/계산서 연동, 미연동 매입처 수기등록/OCR/승인 반영 흐름이 누락돼 있었다. 기획서에 최신 지시 반영 설계 섹션을 추가하고 화면 설계·디자인 기준·로드맵을 최신 시안과 맞췄다. 검증은 `git diff --check`, 핵심 문구 grep, 공개 정적 URL HTTP 200 및 공개 HTML 본문 확인으로 수행했다. 기존 운영 API/DB는 변경하지 않았다.

## 2026-07-27
- AADS-FOOD-EMPLOYEE-SIGNUP-FLOW-20260727: 08:03 KST 기준 매장비서 FB 운영 앱 `app/static/apps/yeoljeong-finance/index.html`의 직원관리/로그인 UX를 직원 직접 회원가입 우선 흐름으로 조정했다. 로그인 게이트 기본 탭을 직원 회원가입으로 바꾸고, 첫 화면에 `직원 회원가입 → 가입요청 자동 생성 → 입사서류 업로드 → 관리자 승인 후 계약` 4단계 흐름을 표시했다. 직원관리 메뉴는 `직원 가입 처리 흐름`과 `직원 회원가입/가입요청`을 상단으로 올리고, 기존 `직원 초대`는 `초대 링크(보조)` 카드로 낮춰 전화번호 기반 예외 흐름만 처리하도록 정리했다. FB 로그인/회원가입 모달 문구와 CTA도 `회원가입 후 입사서류 등록` 기준으로 맞췄다. 검증은 `git diff --check -- app/static/apps/yeoljeong-finance/index.html`, Node `vm.Script` 인라인 JS 컴파일, 공개 URL `https://fb.newtalk.kr/static/apps/yeoljeong-finance/index.html?v=employee-signup-flow` HTTP 200 및 신규 문구 grep, Browser Bridge 캡처 `https://aads.newtalk.kr/screenshots/screenshot_20260727_080402_49a6d1.png`로 수행했다. 기존 API/DB는 변경하지 않았다.
- AADS-FOOD-INTEGRATION-MOCKUP-FINAL-AUDIT-20260727: 07:50 KST 기준 매장비서 공개 시안 `app/static/apps/yeoljeong-finance/mockup-v2.html`의 연동관리와 FB 로그인 디자인 최종 완료 원장을 재검증했다. 변경 커밋 `36357761`은 로컬 `main` 및 `origin/main`과 동일하며, 시안 대상 파일 `mockup-v2.html`과 `docs/HANDOVER.md`에는 미커밋 diff가 없다. 공개 URL `https://fb.newtalk.kr/static/apps/yeoljeong-finance/mockup-v2.html#integrations`와 `#auth-login`은 HTTP 200이고 Browser Bridge에서 각각 `매장비서 · 연동 관리 시안`, `매장비서 · 로그인 시안` 제목으로 렌더링됐다. `#integrations`에는 판매사이트(배민, 쿠팡이츠, 요기요, 땡겨요)의 계정 ID, 비밀번호, 비밀번호 확인, 2차 인증, 매장코드, API 토큰, 권한 범위, 실패 시 CSV/수기 대체 흐름이 반영되어 있다. 은행은 신한은행 기업/기업은행 기업의 기업뱅킹 ID/PW, 계좌번호, 인증서 파일/PW, OTP/보안매체, 조회 전용 권한을 분리했고, 홈택스/계산서는 홈택스 ID/PW, 공동인증서/PW, 세무대리인 위임, 수집 범위, 신고 알림을 포함한다. 매입처는 쿠팡/마켓봄/뉴통/발주고 계정/API 연동과 주문 프로그램이 없는 거래처의 거래명세서·영수증 사진 OCR 수기등록, 반복 템플릿, 대표 승인 후 매입자료·재고·세무증빙 반영 흐름을 포함한다. 추가 권장 연동으로 POS/키오스크, 리뷰·CS, 노무·4대보험, 카드·PG 후보를 반영했다. `#auth-login`에는 대표자/점장/직원/외부 담당자 역할 기반 로그인, 조직 코드, 2단계 인증, 대표자 회원가입, 직원 초대 수락, 비밀번호 찾기, 사업자 등록 진입이 확인됐다. 검증은 KST 실측, `git diff --check`, 인라인 JS `vm.Script` 컴파일, 공개 URL 본문 grep, Browser Bridge ARIA 스냅샷과 캡처, 서버68 health check로 수행했다. 서버68은 HEALTHY, DB 연결 정상, pipeline stalled/running 0이며 디스크 사용률은 81%다. 현재 작업트리의 나머지 dirty 파일 20건은 이번 시안 대상이 아닌 기존 별도 변경으로 분리했다.

## 2026-07-26
- AADS-FOOD-INTEGRATION-MOCKUP-CREDENTIALS-20260726: 20:49:19 KST 기준 매장비서 `mockup-v2.html#integrations` 연동관리 시안을 CEO 지적사항에 맞춰 보강했다. 판매사이트(배민, 쿠팡이츠, 요기요, 땡겨요)는 계정 ID만으로 부족하므로 비밀번호, 2차 인증/OTP, 플랫폼 매장코드, API 토큰/정산 CSV fallback, 수집 범위, 권한 범위를 상세 입력값으로 추가했다. 은행(신한은행 기업, 기업은행 기업)은 기업뱅킹 ID/PW, 조회 계좌번호, 공동/금융인증서 파일, 인증서 비밀번호, OTP/보안매체, 조회 전용 권한을 분리했다. 홈택스/계산서는 홈택스 ID/PW, 공동인증서 파일/PW, 세무대리인 위임, 수집 범위, 인증 만료 알림을 추가했다. 매입처는 쿠팡/마켓봄/뉴통/발주고 계정/API 연동과 주문 프로그램이 없는 거래처의 수기 매입처 생성, 거래명세서/영수증 사진 OCR, 반복 템플릿, 대표 승인 후 매입자료·재고·세무 반영 흐름을 반영했다. 추가로 인증정보 Vault 보관 정책 상세 드로어를 추가했다. 검증은 인라인 JS `vm.Script` 구문 검사, `git diff --check`, 공개 URL HTTP 200 및 응답 HTML 문구 확인, Browser Bridge 렌더/드로어 클릭/캡처 성공으로 수행했다. 기존 운영 `index.html`, API, DB는 변경하지 않았다.
- AADS-FOOD-INTEGRATION-SETTINGS-R2-20260726: 20:04:49 KST 기준 매장비서 설정/연동관리 화면을 추가 보강했다. 판매사이트 4사(배민, 쿠팡이츠, 요기요, 땡겨요), POS, 신한은행 기업/기업은행 기업, 쿠팡/마켓봄/뉴통/발주고/기타 매입처, 거래내역서·영수증 사진/OCR, 홈택스, 계산서/증빙 업로드에 더해 카드사/PG 매출, 공과금 고지서, 세무대리인/회계프로그램 전달 채널을 추가했다. 프론트는 서비스 카탈로그 기반으로 공식/관리 URL, 권장 수집방식, 수집대상, 필수 확인값, 메모를 자동 프리셋하고 연동 현황 카드에 표시한다. `/api/v1/yeoljeong-finance/accounts`는 `category`, `data_scope`, `required_proof`, 기존 프론트가 보내던 `auto_sync` payload를 허용하도록 API 계약을 보강했다. 검증은 `python3 -m py_compile app/api/yeoljeong_finance.py app/services/yeoljeong_finance_service.py`, 인라인 JS `node --check`, `git diff --check` 통과. 로컬 `.venv/bin/python`은 깨진 symlink라 FastAPI import/pytest는 미실행했다. 커밋, 푸시, 배포는 아직 수행하지 않았다.
- AADS-FOOD-INTEGRATION-SETTINGS-20260726: 19:51:07 KST 기준 매장비서 운영 앱 `app/static/apps/yeoljeong-finance/index.html`의 설정/연동관리 화면을 판매사이트(배민, 쿠팡이츠, 요기요, 땡겨요, POS), 은행(신한은행 기업, 기업은행 기업), 매입처(쿠팡, 마켓봄, 뉴통, 발주고, 기타 주문프로그램), 주문프로그램 없는 매입처의 거래내역서/영수증 파일·사진 등록, 홈택스 및 계산서/증빙 업로드까지 등록 가능하도록 확장했다. 기본 연동 목록과 서비스 라벨, 수집 방식, 요약 카운트, 파일/사진 등록 CTA를 추가했고, 비배달 서비스의 `수집 실행`은 오류 호출 대신 설정 저장/파일 등록 흐름으로 분기한다. 백엔드 `app/services/yeoljeong_finance_service.py`는 `/api/v1/yeoljeong-finance/accounts` 저장 허용 범위를 배달 4사에서 전체 연동 서비스로 넓히고, 홈택스처럼 지점 공통인 연동도 사업자 기준으로 저장될 수 있게 범위 검증을 분리했다. 검증은 `python3 -m py_compile app/services/yeoljeong_finance_service.py app/api/yeoljeong_finance.py`와 Node `vm.Script` 인라인 스크립트 파싱 1건 통과로 수행했다. 커밋, 푸시, 무중단 배포는 아직 수행하지 않았다.

## 2026-07-25
- AADS-PIPELINE-RUNNER-KO-READONLY-GUARD-20260725: `runner-92eb71d8`가 "읽기 전용 확인만 수행" 지시를 수행하고도 Runner Guard의 영어 중심 read-only 판별에 걸리지 않아 `status='cancelled', phase='no_changes'`로 저장된 false cancellation을 확인했다. `scripts/pipeline-runner.sh`와 `scripts/pipeline-runner.sh.local`의 `is_read_only_instruction()`에 `읽기[[:space:]]*전용` 패턴을 추가해 한국어 읽기 전용 검증 작업이 diff 0건을 정상 완료(`done`)로 저장하도록 보강했다. 관련 단위 테스트는 `tests/unit/test_pipeline_runner_script_guards.py`에 추가했다.

## 2026-07-23
- AADS-FOOD-OPS-DETAIL-MOCKUP-FINAL-LEDGER-20260723: 2026-07-23 20:56 KST 최종 완료 원장을 재대조했다. 저장소와 GitHub `main`은 `9d8d3d7c` 기준 clean/ahead 0/behind 0이며, 운영 `aads-server`·`aads-server-green`의 `mockup-v2.html`은 저장소와 동일한 130,520 bytes 및 SHA-256 `dccdd2695f74ac690c9c062f1b5de49a5ac3e3c2420e91c4294acde3c468fa35`다. active 슬롯은 `aads-server-green:8102`, `/api/v1/health`와 공개 시안 URL은 HTTP 200이다. 공개 URL을 Chromium/Playwright로 1440x900·390x844에서 재검증해 메인 화면 15개, 업무별 검색 제출, 입사서류 OCR 검토, 근로계약서 작성, 사업자등록 OCR 원본대조, 메뉴 매출 행 상세, 권장 발주수량 산출 근거, 대표자 회원가입이 정상 동작했고 console/page error 0, horizontal overflow 0을 확인했다. Browser Bridge `capture_screenshot`은 SSH argument list too long 오류로 실패했으나 동일 공개 URL의 실제 Chromium 렌더와 HTTP/API 검증으로 대체했으며, 저장된 데스크톱·모바일 화면도 육안 검수했다. 변경 범위는 별도 정적 시안과 완료 기록이며 기존 운영 `index.html`, API, DB는 변경하지 않았다.
- AADS-PIPELINE-DEPLOY-PREFLIGHT-RECOVERY-COMPLETE-20260723: 2026-07-23 20:46 KST `runner-58cf579a`의 `deploy_preflight_git_state` 차단을 복구했다. 런타임 마커와 열정국밥 업로드·계약 데이터는 삭제하지 않고 Git 추적/검사 대상에서 분리했으며, 복구 커밋 `bb031157`을 `origin/main`에 푸시했다. `deploy.sh bluegreen`으로 API active 슬롯을 8100(`aads-server`)에서 8102(`aads-server-green`)로 무중단 전환했고, 필수 배포 검증·DB 컬럼/채팅 테이블·LLM 서비스 점검을 통과했다. 서버68 health는 `HEALTHY`, DB 연결 정상, active API `/api/v1/health` HTTP 200이며, 공개 `mockup-v2.html`도 HTTP 200 및 Browser Bridge 렌더를 확인했다. 배포 직후 main workdir과 `origin/main`은 clean 및 ahead/behind 0이다. 롤백 대상은 배포 전 8100 슬롯이며 기존 컨테이너를 healthy 상태로 유지했다.
- AADS-FOOD-OPS-DETAIL-MOCKUP-COMPLETION-20260723: 2026-07-23 20:46 KST 완료 원장을 재검증했다. 로컬·원격 Git은 `main` clean 및 `origin/main` ahead/behind 0이고, 운영 blue/green 컨테이너의 `mockup-v2.html`은 모두 130,520 bytes, SHA-256 `dccdd2695f74ac690c9c062f1b5de49a5ac3e3c2420e91c4294acde3c468fa35`로 저장소와 일치한다. 공개 URL은 HTTP 200이다. 공개 URL을 Playwright로 1440x900·390x844에서 각각 15개 항목 재검증해 직원 입사서류 OCR/계약서 작성, 사업자 OCR 원본대조, 메뉴 매출 행 상세, 권장 발주수량 근거, 대표자 회원가입, 업무별 검색 필터가 모두 동작했고 console/page error 0, horizontal overflow 0을 확인했다. Browser Bridge 캡처는 타임아웃이었으나 동일 공개 URL의 로컬 Playwright 렌더와 HTTP/API 폴백으로 검증을 완료했다. 기존 운영 `index.html`, API, DB, nginx는 변경하지 않았다.
- AADS-PIPELINE-DEPLOY-PREFLIGHT-RECOVERY-20260723: `runner-58cf579a` 배포가 AADS main workdir dirty 11건 때문에 `deploy_preflight_git_state`로 차단된 원인을 분류했다. blue/green 런타임 마커 `.active_container`·`.active_port`는 이미 ignore 대상이므로 Git 인덱스 추적을 해제하고, `.tmp-*` 및 열정국밥 계약·입사요청·온보딩 문서/업로드 런타임 경로를 ignore 대상으로 명시해 운영 데이터는 유지하면서 preflight 오염을 방지한다. GO100 직접 작업 변경기록과 현재 dashboard upstream source 상태는 별도 복구 커밋으로 보존한다. 검증 기준은 `git status` clean, `origin/main` ahead/behind 0, blue/green 배포 성공, 서버68 health 및 공개 정적 시안 HTTP 200이다. 롤백은 복구 커밋 revert와 배포 전 API 슬롯 재전환으로 수행한다.
- AADS-FOOD-OPS-DETAIL-MOCKUP-LEDGER-RECONCILIATION-20260723: 2026-07-23 20:17 KST 최종 대조에서 Pipeline Runner `runner-f7556238`의 배포 단계는 기존 dirty 11건 때문에 `deploy_preflight_git_state` 오류로 종료된 상태임을 재확인했다. 다만 시안 구현 커밋 `cc9fd12f`와 감사 커밋 `805c220b`는 `origin/main`에 포함되어 있고, nginx 원본 `mockup-v2.html`은 저장소 파일과 130,520 bytes 및 SHA-256 `dccdd2695f74ac690c9c062f1b5de49a5ac3e3c2420e91c4294acde3c468fa35`가 일치하며 공개 URL도 HTTP 200이다. 따라서 정확한 완료 상태는 “Runner 배포 단계 실패, 별도 정적 시안 공개 반영 및 검증 완료”다. Browser Bridge에서 직원 계약서 작성, 사업자 OCR 원본대조, 메뉴 매출 행 상세, 권장 발주 산출근거, 대표자 회원가입 화면을 다시 클릭 검증했다. 기존 운영 `index.html`, API, DB, nginx 및 사용자 dirty 파일은 변경하지 않았다.
- AADS-FOOD-OPS-DETAIL-MOCKUP-FINAL-AUDIT-20260723: 2026-07-23 19:30 KST 기준 `cc9fd12f`가 로컬 `HEAD`와 `origin/main`에 일치하고, 공개 `mockup-v2.html`은 HTTP 200으로 응답함을 재확인했다. Cloudflare 보안 스크립트 삽입분을 제외하면 배포 HTML은 저장소 원본과 동일하다. Browser Bridge에서 직원·급여(입사서류 OCR/계약조건), 사업자·지점(사업자등록증 OCR), 매출·정산(일자·사업자·지점·마켓·메뉴 필터/메뉴별 통계), 재고·발주(지점별 권장발주), 대표자 회원가입 화면과 드로어 동작을 재검증했다. 이번 감사 기록은 `docs/HANDOVER.md`만 변경하며 기존 운영 `index.html`과 API·DB는 변경하지 않는다.
- AADS-FOOD-OPS-DETAIL-MOCKUP-20260723: `app/static/apps/yeoljeong-finance/mockup-v2.html`을 직원 계약관리, 사업자등록증 OCR, 메뉴별 매출통계, 재고·발주예측, 통합 승인함, 알림센터, 원가·마진, 권한·감사로그, 문서보관함 상세 시안으로 확장했다. 기존 `index.html`은 수정하지 않았다.
- 직원 계약관리: 직원 회원가입/초대 수락, 입사서류 드롭존, 파일별 업로드/OCR/검토 상태, 민감정보 마스킹 안내, OCR 출처 배지, 관리자 수정 가능 필드, 계약조건 입력, 계약서 미리보기/전자서명/문서보관 드로어 흐름을 추가했다.
- 사업자 OCR: 사업자등록증 업로드, OCR 추출, 필드 매핑 검토, 저장/수정, 지점·계좌·배달플랫폼 연결 온보딩과 다사업자 카드, 주사업자, 수정이력, 권한, 재업로드, 검증상태를 추가했다.
- 메뉴 매출통계: 일자·사업자·지점·마켓·카테고리·메뉴 필터, KPI, 일별 추이, 채널 구성, TOP/BOTTOM 메뉴, 시간대·요일 히트맵, 메뉴별 상세표, CSV/Excel 및 비교기간 CTA, 행 상세 드로어를 추가했다.
- 재고·발주예측: 새 `재고·발주` 메뉴와 모바일 하단 메뉴를 추가하고, 현 재고·안전재고·사용속도·예상 소진일·발주점·권장 발주·공급사·리드타임·입고예정·재고금액, 산출근거, 자동발주 비활성 표시, 발주 제안→입고/검수 흐름을 구현했다.
- 추가 권장 화면: 통합 승인함, 알림센터, 원가·마진, 권한·감사로그를 실제 `data-view` 화면으로 추가하고 문서보관함은 기존 경영자료 라우트(`#documents`)를 확장해 입사·계약·사업자·세무 증빙 분류/만료 관리를 포함했다.
- 품질 기준: 모든 숫자·OCR·연동·예측은 시안 데이터/시뮬레이션임을 화면에 표시했다. CTA는 해시 라우트, 드로어, 상태 변경 중심으로 연결했고, ESC/배경 닫기/ARIA/label/button type 패턴과 모바일 내부 스크롤 테이블을 유지했다.
- 검증 기록: HTML/인라인 JS 구문 검증과 1440x900·390x844 Playwright 순회를 완료했다. 각 환경에서 업무 15개·인증 7개·핵심 상세 동작 9개, console/page error 0, horizontal overflow 0, 화면 전환 scrollY 0을 확인했다. `index.html` diff는 없으며 변경 파일은 `mockup-v2.html`과 `docs/HANDOVER.md`뿐이다.
- AADS-FOOD-DETAILED-MOCKUP: `app/static/apps/yeoljeong-finance/mockup-v2.html`을 10개 업무 화면과 7개 필수 인증 화면으로 확장했다. 각 업무 화면에 일자/기간, 사업자, 지점, 마켓·채널, 계좌, 담당자, 상태, 검색어 중 업무에 맞는 조건을 배치하고 초기화·검색·빠른 기간 선택 흐름을 추가했다.
- 상세 UX: 매출·정산, 계좌 거래, 직원·급여, 세무 신고, 사업자·지점, 경영자료의 행·카드·CTA에서 우측 상세 드로어를 열어 요약, 근거 수치, 처리 상태, 후속 액션을 확인할 수 있다. 경영자료 메뉴에는 생성·미리보기·PDF/Excel 내보내기 상태를 별도 설계했다.
- 인증 UX: 로그인, 대표자 회원가입, 본인인증, 비밀번호 찾기/재설정, 직원 초대 수락, 초기 사업자 등록 화면을 추가했다. 주 CTA는 회원가입→본인인증→사업자 등록, 비밀번호 찾기→재설정, 로그인/초대/등록 완료→홈으로 이어지는 해시 기반 시안 흐름을 제공한다.
- 변경 범위는 정적 UX 시안과 이 HANDOVER 기록뿐이며 운영 API·DB 스키마·실데이터는 변경하지 않는다. 화면의 금액·상태는 모두 시안 데이터다.
- AADS-FOOD-MENU-MOCKUP: `app/static/apps/yeoljeong-finance/mockup-v2.html` mockup-v2를 단일 HTML SPA 시안으로 확장했다. 통합 홈 회귀를 유지하면서 할 일·알림, 경영 리포트, 매출·정산, 입금·계좌, 직원·급여, 세무·회계, 사업자·지점, 연동 관리 총 9개 `data-view` 화면이 실제 메인 콘텐츠를 교체한다.
- UX 반영: 사이드바와 모바일 하단 메뉴 active 동기화, `#sales` 등 해시 직접 진입/새로고침, 제목/브라우저 타이틀 동기화, 주요 CTA/toast 상호작용, 1440px 및 390px 가로 overflow 방지 스타일을 적용했다. 모든 숫자는 시안 데이터 표기를 유지한다.
- 반영 커밋 `4ad1ce8e`를 `origin/main`에 푸시했다. 정적 호스트가 같은 파일을 즉시 제공해 컨테이너 재시작 없이 공개 URL HTTP 200을 확인했다.
- 공개 URL Playwright 검증: 9개 화면을 1440px·390px에서 직접 진입해 총 18건 HTTP 200, 해시·제목·active 동기화, `scrollWidth == innerWidth`, 콘솔/페이지 오류 0건, CTA toast 동작을 확인했다. 운영 API·DB 변경은 없다.

## 신규 러너 시작점
- 1차: `docs/knowledge/AADS-3STEP-SYSTEM-INDEX.md`
- 2차: `docs/knowledge/CTO-SYSTEM-MAP.md`
- 3차: 루트 `HANDOVER.md`
- 작업 전에는 인덱스의 금지사항과 읽기 순서를 먼저 확인한다.

## 2026-06-18
- Personal Assistant Hub P0 route fix: `assistant_router` was imported in `app/main.py` but not mounted, so unauthenticated `/api/v1/assistant/readiness` stopped at middleware 401 while authenticated internal-admin requests reached FastAPI 404. Mounted it with `app.include_router(assistant_router, prefix="/api/v1", tags=["assistant"])` and added a regression guard to `tests/unit/test_tenant_rbac_policy.py`.
- Verification: `pytest tests/unit/test_tenant_rbac_policy.py::test_jarvis_external_surfaces_are_internal_or_tenant_scoped tests/unit/test_tenant_rbac_policy.py::test_assistant_readiness_api_allows_internal_admin_and_blocks_customer tests/unit/test_voice_service.py` passed 7 tests; `python3 -m py_compile app/main.py app/api/assistant.py` passed; local app route listing includes `/api/v1/assistant/readiness`.
- Jarvis P0 follow-up: deleted/active SaaS user default tenant hygiene is closed in operating DB (`saas_users` total 44, active missing default tenant 0, removed/deleted-status missing default tenant 0). Core chat tenant-null counts are also 0 for `chat_sessions`, `chat_messages`, and `chat_artifacts`.
- Personal Assistant Hub regression guard added: `tests/unit/test_tenant_rbac_policy.py` now exercises `/api/v1/assistant/readiness` at HTTP level, verifying internal admin 200 with `personal_assistant` payload and customer 403 with the internal-admin-only error.
- Verification: `pytest -q tests/unit/test_tenant_rbac_policy.py tests/unit/test_voice_service.py tests/unit/test_admin_users_audit.py` passed 25 tests; `python3 -m py_compile app/api/assistant.py app/api/voice.py app/api/admin_users.py` passed. Plain `python -m py_compile` is not valid on this server because the default `python` binary cannot parse current Python 3.10+ type hints.
- Pipeline Runner CLI guard follow-up at 2026-06-18 14:35 KST: `scripts/pipeline-runner.sh` and `.local` already use Codex `exec --sandbox workspace-write --ephemeral -C "$workdir"` and omit Claude `--dangerously-skip-permissions` when running as root. The backend DB runner path in `app/services/pipeline_runner_service.py` was brought into line by replacing deprecated Codex `--full-auto` with `--sandbox workspace-write`.
- Regression coverage: `tests/unit/test_pipeline_runner_script_guards.py` now also checks the backend service path so future direct/chat runner invocations cannot reintroduce `--full-auto`. This is intended to reduce false runner failure fallback from deprecated Codex options and root-blocked Claude flags.
- Pipeline Runner read-only closeout follow-up at 2026-06-18 14:46 KST: smoke `runner-ec03a99d` proved the shell runner reached `NO_CHANGES_READ_ONLY` but its DB update still referenced `pipeline_jobs.completed_at` before every environment was schema-aligned, leaving the row in `running`. The read-only done update now writes only existing core columns, and migration `111_pipeline_jobs_completed_at.sql` keeps the API runner completion timestamp schema explicit.
- Jarvis/voice implementation ledger correction: voice backend is no longer only a plan. Server commits `3fd1ce0`, `294f8f2`, and `023f937` wired `/api/v1/voice/*`, tenant-scoped memory/session recall, and high-risk approval policy. Dashboard commit `9fb9046` added internal-admin-only voice input and Personal Assistant empty-state prompts in chat.
- Current verification basis: server targeted py_compile/pytest passed for voice, tenant RBAC, and memory/approval policy paths; dashboard targeted eslint passed and dashboard build passed in the prior verification. Browser microphone permission and real provider STT response remain a manual E2E follow-up.
- Runner caveat: not all Jarvis runners succeeded. The implementation state comes from direct main-branch correction commits, while several runner jobs ended as `error`, `rejected_done`, `dedup_blocked`, or `no_changes`.
- AADS Voice Command MVP는 기획 문서에서 백엔드 MVP 구현 상태로 갱신됐다. 현재 `/api/v1/voice/health`, `/api/v1/voice/transcribe`, `/api/v1/voice/speech`가 서버에 연결됐고, 대시보드 채팅 입력창에는 내부 관리자 전용 음성 입력 버튼이 추가됐다. 답변 TTS 자동 재생 UI와 브라우저 실사용 E2E는 후속 검증 대상이다.
- 신규 문서: `docs/plans/AADS-VOICE-COMMAND-MVP.md`.
- `runner-9dae6d37` 상태를 확인했다. 결과는 `error`이며 root 환경에서 `--dangerously-skip-permissions` 사용 차단으로 Claude Code 실행이 실패했다. `git_diff`는 비어 있어 코드 산출물은 없다.
- 권장 구현 흐름: `/api/v1/voice/transcribe`, `/api/v1/voice/speech`, `/api/v1/voice/health` 추가, 기존 JWT/tenant 인증 재사용, 음성 STT 결과는 기존 chat send 경로로 넘겨 도구 실행 정책을 유지한다.
- 구현은 직접 보정 커밋으로 수행됐다. 재투입 시에는 특정 Claude worker model 강제보다 Runner 기본 모델 설정 사용 또는 root 권한 플래그 문제 선조치가 필요하다.
- 검증: `date`, `git status --short`, `rg` 기반 구현 흔적 확인, `pipeline_runner_status(job_id=runner-9dae6d37, scope=all)` 확인.

## 2026-06-15
- AADS docs/static contract closeout: 법인 설립 서류 7종, 열정국밥 중화점 법인 부동산임대차계약서, 임대인동의서, 영업양수도계약서를 `app/static/docs/contracts/`에 공개 산출물로 정리했다.
- Generation scripts added/kept for reproducibility: `scripts/gen_incorporation_all.py`, `scripts/generate_yeoljeong_landlord_and_corp_lease.py`, `scripts/fix_duplicate_accumulate.py`.
- `app/services/self_evaluator.py` 중복 `_accumulate_experience()` 정의와 중복 호출 블록을 제거해 Experience Memory 축적 로직이 1회만 실행되도록 정리했다.
- Verification at 2026-06-15 13:36 KST: `python3 -m py_compile app/services/self_evaluator.py scripts/gen_incorporation_all.py scripts/fix_duplicate_accumulate.py scripts/generate_yeoljeong_landlord_and_corp_lease.py` passed. `curl -fsS http://127.0.0.1:8100/health` returned `status=ok`. Static DOCX URL for `주식회사 윤희에프엔비_정관.docx` returned HTTP 200 with DOCX content type.
- Public URL follow-up: Cloudflare returned 403 for percent-encoded Korean DOCX paths, while local static serving returned 200. ASCII aliases were added for all 10 contract files, including `yhfnb_articles.docx`, `yhfnb_incorporation_minutes.docx`, `yeoljeong_corp_lease_junghwa.docx`, `yeoljeong_landlord_consent_junghwa.docx`, and `yeoljeong_transfer_junghwa.docx`; public `https://aads.newtalk.kr/static/docs/contracts/yhfnb_articles.docx` returned HTTP 200.
- Commit scope note: `.active_container` and `.active_port` are runtime state files and must remain unstaged unless explicitly changing blue/green active slot metadata.

## 2026-06-12
- Codex usage bar recovery: `/api/v1/ops/codex-usage` now returns a stable `codex_cli` fallback limit when the Codex app-server relay responds with `ok=false` or `limits=[]`.
- Root cause: the chat `UsageBar` renders Codex only when `codex.ok` and `limits[0]` exist; the live relay currently returns HTTP 200 with an empty limits array, so the GPT/Codex bar disappeared while Claude still rendered.
- Fallback source: `oauth_usage_log` Codex/GPT-5.x rows for 5h/7d windows. When no Codex rows exist, it returns 0% usage with `fallback_reason=relay_empty_limits` so the UI remains visible and debuggable.
- Verification: `python3 -m py_compile app/api/ops.py` passed before commit/deploy.
- SaaS admin/customer isolation audit: operating DB has 1 active internal tenant, 34 active customer tenants, 33 active customer users, and 0 active non-deleted users without `default_tenant_id`. Core chat tables (`chat_workspaces`, `chat_sessions`, `chat_messages`, `chat_artifacts`, `e2e_credentials`) are tenant-scoped.
- Admin API lockdown: `/api/v1/admin/users/overview` and `/api/v1/admin/design/projects/AADS/screens` were verified as 403 for a customer token and 200 for an internal admin token after commit `e986cde`.
- Additional P0 exposure fixed: `/api/v1/llm-keys`, `/api/v1/image/*`, and `/api/v1/chat/drive*` could expose shared/internal resources to customer tokens. `app/api/llm_keys.py` and `app/api/image.py` now require `require_internal_admin`; chat drive routes now require tenant membership and verify the target workspace tenant before list/upload/download/delete.
- Verification: `python3 -m py_compile app/api/llm_keys.py app/api/image.py app/routers/chat.py app/services/chat_service.py` and `git diff --check` passed before deployment. Post-deploy customer-token HTTP checks must confirm `llm-keys=403`, `image/gallery=403`, internal workspace `chat/drive=404/403`, and own customer workspace `chat/drive=200`.
- Remaining SaaS hardening: add direct `tenant_id` or enforced join constraints for legacy link tables (`chat_files`, `chat_drive_files`, `chat_todo_items`, `compiled_prompt_provenance`, `media_generation_jobs`), resolve 19 orphan `compiled_prompt_provenance` rows, and split media jobs into tenant-scoped customer media vs internal gallery.

## 2026-06-11
- SaaS customer chat isolation closeout: active customer tenants missing chat workspaces were backfilled with default `[WORK] {tenant}` workspaces (`project_key=CUSTOMER`, `allowed_roles=["GeneralAssistant"]`, `role_routing_enabled=false`). Customer tenant login/onboarding/invite acceptance now ensures a default workspace exists.
- Customer users are forced to `GeneralAssistant` in customer workspaces, including when the client sends an internal role key, and internal project mentions are ignored for customer tenants so AADS/KIS/GO100/SF/NTV2/NAS context does not bleed into customer chats.
- Blueshop verification: `objgood@naver.com` in tenant `블루샵` now sees one workspace, one available role (`GeneralAssistant`), can create a session, and an API smoke message completed with SSE `done=true`; the user and assistant messages were stored only under the Blueshop tenant.
- Sleep-Time Agent auth routing fix: `app/core/memory_gc.py` no longer calls `get_client().messages.create()` directly for project insights or quality optimization. It now uses `call_llm_with_fallback()` so invalid Anthropic credentials fall through the central R-AUTH chain instead of repeatedly logging `invalid x-api-key`.
- Yeoljeong transfer contract refresh: `scripts/generate_yeoljeong_transfer_contract.py` now explicitly states `사업자등록 완료 전 폐업신고 금지` in Article 5 and the cooperation table. Regenerated `영업양수도계약서_열정국밥_중화점.docx` in both `exports/contracts/` and `app/static/docs/contracts/`.
- Download fix: regenerated DOCX initially inherited SELinux `admin_home_t` and returned 403; static copy was corrected to `httpd_sys_content_t`. Verified URL `https://aads.newtalk.kr/static/docs/contracts/영업양수도계약서_열정국밥_중화점.docx?v=20260611-active-coop4` returns HTTP 200, size 45,208 bytes, SHA256 `fef45709bcd56cc2e717764ac1e7980b3515ec60171d9566bda7727962dd9ed4`.
- AI review git diff classification DB closeout: Pipeline Runner scripts already had synchronized `pre_exec_sha` committed/uncommitted diff capture, zero-diff approval blocking, and `INVALID_GIT_DIFF` guards; Chat-Direct review capture already had AADS local `git diff` fallback in `app/services/tool_executor.py`.
- Live DB gap fixed: `migrations/041_code_review_flag_classification.sql` was applied to add `code_reviews.flag_category`, `failure_stage`, and `needs_retry`; `checkpoint_migrations(v=41)` is now recorded.
- Verification: `POST /api/v1/review/code-diff` with `fatal: not a git repository` returned `FLAG`, `GIT_DIFF_FAILURE`, `git_diff_capture`, and `needs_retry=true`. `bash -n scripts/pipeline-runner.sh scripts/pipeline-runner.sh.local`, `pytest -q tests/unit/test_pipeline_runner_script_guards.py tests/unit/test_code_reviewer_flag_classification.py`, and `python3 -m py_compile app/services/tool_executor.py app/services/code_reviewer.py app/api/ceo_chat_tools.py` passed.
- No code deploy/restart was needed for this closeout; only the live DB migration and HANDOVER docs changed. Existing unrelated dirty files were left untouched.
- Blue-green deploy reporting correction: `deploy.sh` now removes the duplicated pre-switch active-stream drain wait and reports `active 전환 완료` separately from old-slot standby sync. Standby rebuild remains a background drain-following task and emits its own complete/skip log, so final reports no longer imply standby sync is finished when it is only scheduled.
- Verification: `bash -n deploy.sh` passed. Runtime check at 2026-06-11 10:37 KST showed `.active_port=8102`, `.active_container=aads-server-green`, both `8100` and `8102` health endpoints OK, and both API containers healthy. Existing unrelated dirty/static files were left untouched.

- NTV2/V1 신뉴톡 `pick.newtalk.kr/root/members` AADS AI chat 운영 검수: 실제 운영 경로 `/home/newpigup3` 기준으로 관리자 공통 head include, `v1_new` service routing, `/aads-chat/config`, `/aads-chat/session`, `/aads-chat/sessions/{id}/messages` 흐름을 검증했다.
- V1 신뉴톡 프록시 `application/controllers/Aads_chat.php`의 message payload가 `response_mode='fast'`로 AADS 자동 라우팅을 우회하던 문제를 `response_mode='auto'`로 수정했다. 2026-06-11 12:08 KST 관리자 세션 E2E에서 `members_status=200`, `data-service="v1_new"`, `config_status=200`, `session_status=201`, message `status=200`, stream `model=GPT-5.5 (Codex CLI)`, `errors=[]`를 확인했다.
- 검증 보안: 비로그인 `/aads-chat/config?service=v1_new` 호출은 `Unauthenticated`로 차단됐고, `auth_code=80` 테스트 세션의 `/aads-chat/config?service=v1_new`는 `403 Forbidden`으로 차단됐다. 임시 `PHPSESSIDaads*` 테스트 세션은 검증 후 삭제했고 잔여 0건을 확인했다.
- 운영 주의: `/home/newpigup3` 레거시 저장소는 기존 대량 dirty 상태이며, `routes.php`/`head.php` diff에는 이번 AADS chat 외 기존 입점상담/푸시/카카오 가드 변경이 섞여 있어 선별 커밋은 보류했다. `application/config/aads_chat.php`는 토큰 가능성이 있어 커밋 대상에서 제외해야 한다.

## 2026-06-12
- External Chat Gateway 범용화: 뉴톡 전용 `Literal["newtalk"]` / `v1_old|v1_new|v2` 제한을 제거하고, `AADS_EXTERNAL_CHAT_ALLOWED_SERVICES` CSV 또는 `AADS_EXTERNAL_CHAT_SERVICE_REGISTRY` JSON으로 외부 서비스별 `provider:service`를 등록할 수 있게 했다. 미등록 서비스는 `external_chat_service_not_allowed`로 403 차단된다.
- 등록 서비스별 워크스페이스명, 시스템 프롬프트, 세션 제목 prefix, 색상, 아이콘, `admin_only` override를 지원한다. 기본 뉴톡 3개 서비스는 기존 호환성을 유지한다.
- API 보강: `/api/v1/external/chat/services`가 토큰/HMAC 인증 후 등록된 외부 서비스 목록을 반환하고, `/config`, `/sessions`, `/messages`는 범용 provider/service 문자열을 받되 안전한 key 패턴과 등록 검증을 통과해야 한다.
- 검증: `python3 -m py_compile app/services/external_chat_gateway.py app/api/external_chat.py`, `python3 -m pytest tests/unit/test_external_chat_gateway.py -q` 12개 통과. 운영 컨테이너 `aads-server-green`에서 `sf:ops` 등록 config import 검증 통과. 컨테이너 내부 pytest는 tests 디렉터리 미마운트 상태라 기존 컨테이너 내 8개 테스트만 실행됨.

## 2026-06-10
- Chat completed-execution terminal-only repair: session `efccec7c-0788-4564-a2cf-265c63d075f0` showed a completed execution (`0a5a3a4a-2164-4b1b-89fa-8c9a22a1cb3a`) whose visible assistant row remained `interrupted_partial`, so the chat bubble changed from completed to interrupted. `app/services/chat_service.py` now repairs completed executions by keeping one final assistant row and archiving duplicate `streaming_placeholder/interrupted_partial/interruption_notice` siblings as `_archived_partial` instead of deleting them.
- 2026-07-27 07:36 KST: FOOD 매장비서 시안 `app/static/apps/yeoljeong-finance/mockup-v2.html`의 `#integrations`와 인증 화면을 보강했다. 판매사이트 연동 설정은 계정 ID만으로 불가하다는 UX를 명시하고 비밀번호, 비밀번호 확인, 2차 인증, 인증 담당자, 인증 만료일, API 토큰, 권한 범위, 실패 시 CSV/수기 대체 흐름을 드로어에 추가했다. 은행/홈택스/매입처 필수 입력값 표를 8개 범주로 확장했고, 주문 프로그램이 없는 매입처는 거래명세서·영수증 사진 OCR 수기등록 보드와 승인 후 매입자료·재고·세무증빙 반영 흐름으로 연결했다. FB 공개 로그인 시안은 대표자/점장/직원/외부담당 역할 분리, 조직 코드, 2단계 인증, 직원 초대 후 입사서류 제출, 사업자등록증 OCR 온보딩을 반영했다. 검증 예정: HTML 인라인 JS 컴파일, `git diff --check`, 공개 URL HTTP/화면 동작 확인.
- 검증 완료: `git diff --check -- app/static/apps/yeoljeong-finance/mockup-v2.html docs/HANDOVER.md` 통과, Node `vm.Script` 인라인 JS 구문 검사 통과, 공개 URL `https://fb.newtalk.kr/static/apps/yeoljeong-finance/mockup-v2.html#integrations` HTTP 200 및 `Last-Modified: 2026-07-27 07:36 KST` 확인, Browser Bridge 스냅샷에서 `#integrations` 필수 입력값/서비스별 설정 판단표/미연동 매입처 수기 운영과 `#auth-login` 역할 기반 로그인 화면 확인. 캡처: `/screenshots/screenshot_20260727_073918_3b3a51.png`, `/screenshots/screenshot_20260727_074215_816732.png`.
- Live DB backfill: 26 completed executions that had only terminal placeholder/interrupted rows and no final assistant row were repaired; 5 sibling terminal rows were archived; follow-up verification returned `remaining_completed_execs_with_terminal_only=0`. Target session `efccec7c...` now has `assistant_message_id=47338bf2-d6b1-42c9-b570-03a37fcd79ab`, `intent=NULL`, `model_used=gpt-5.5`.
- Verification: `PYTHONPATH=/root/aads/aads-server JWT_SECRET_KEY=test-secret-key pytest tests/unit/test_chat_service.py -q` passed with 46 tests, and `python3 -m compileall app/services/chat_service.py app/routers/chat.py` passed.
- Chat auto-default routing follow-up: `auto-default-llm` and legacy `qwen-turbo` are now treated as automatic DB-default sentinels in both `chat_service.send_message_stream()` and `model_selector.call_stream()`, including when the frontend sends `model_override=auto-default-llm`.
- Verification: active API SSE test session `5697dc0c-8389-4668-a8a2-b462ef69ab4c` completed with stream model and DB assistant `model_used=GPT-5.5 (Codex CLI)`. Targeted regression tests passed: `tests/unit/test_chat_service.py::test_send_message_stream_applies_db_default_over_auto_routed_models` and the two DB-default model selector tests.
- Chat large-session follow-up: dashboard dormant `useChatSSE` stream-resume URL now uses the canonical `/api/v1` base without duplicating `/api/v1`, shared artifact API helpers accept bounded `limit/offset`, and chat artifact filtering/count calculations are memoized to reduce repeated work in large sessions.
- Verification: `npx eslint src/app/chat/page.tsx src/hooks/useChatSSE.ts src/services/chatApi.ts --quiet` passed, and `npx tsc --noEmit --pretty false` passed. Full targeted lint including `src/lib/api.ts` still fails on pre-existing `no-explicit-any` debt in that file.
- Chat window precision audit follow-up: historical same-execution final/interrupted duplicate bubbles were hidden with `intent='_deleted_duplicate'` under the safe condition that a final assistant message already existed for the same `execution_id`; verification returned `duplicate_executions=0` and `orphan_untagged_7d=0`. Dashboard lint blockers in `ChatInput` and `SessionSummaryCard` were fixed by removing synchronous state-reset effects and replacing Web Speech `any` types with local interfaces. `supervisord.conf` now routes `aads-api` stdout/stderr to `/dev/fd/1` and `/dev/fd/2` so structured `stream_producer_exit` and completion guard logs are visible through Docker logs after the next deploy/restart.
- Chat window audit follow-up: completed-execution interrupted sibling bubbles are now archived automatically after completion, and a DB backfill archived 5 historical sibling interruption bubbles where a final assistant message already existed for the same execution. Four historical `execution_id IS NULL` interrupted bubbles were not hidden because no final execution linkage could be proven; they were tagged with `quality_details.interruption_reason='orphan_placeholder_no_execution'` for traceability.
- Verification: `JWT_SECRET_KEY=test-secret-key pytest tests/unit/test_chat_service.py -q` passed with 42 tests, and `python3 -m compileall app/services/chat_service.py` passed. Follow-up DB checks returned 0 same-execution final/interrupted duplicates after the backfill.
- Chat large-session freeze mitigation: `/api/v1/chat/artifacts` now supports bounded `limit/offset` and defaults to 60 artifacts, while the dashboard chat page fetches at most 61 records and renders the latest 60. This prevents sessions such as `266ab3aa-b0fd-46bb-8c54-01e4852c956f` from loading hundreds of artifact payloads on every session entry or SSE completion refresh.
- Verification: `JWT_SECRET_KEY=test-secret-key pytest tests/unit/test_tenant_rbac_policy.py tests/unit/test_chat_service.py -q`, `python3 -m compileall app/routers/chat.py app/services/chat_service.py`, and `npx eslint src/app/chat/page.tsx` passed for the touched paths. Full dashboard `npm run lint` still fails on pre-existing unrelated lint debt.
- Pipeline Runner AI review git diff guard was synced into `scripts/pipeline-runner.sh.local`, not only the primary `scripts/pipeline-runner.sh`. Local/recovery runner launches now share the same zero-diff block, git diff shape precheck, `INVALID_GIT_DIFF` flag, and retry recommendation path.
- Regression coverage added in `tests/unit/test_pipeline_runner_script_guards.py` to keep the primary runner and local template byte-for-byte synchronized and to require the diff precheck guard strings in both files.
- Chat completion false-interrupt guard tightened: `app/services/output_validator.py` and `app/services/response_completion_contract.py` now allow a structured final report to end with a `→ 다음 단계` recommendation without being reclassified as `PROGRESS_ONLY_RESPONSE` / `final_report_missing`.
- Verification target: `bash -n scripts/pipeline-runner.sh scripts/pipeline-runner.sh.local`, `pytest -q tests/unit/test_pipeline_runner_script_guards.py`, and `pytest -q tests/unit/test_response_completion_contract.py tests/unit/test_output_validator.py`.

## 2026-05-19
- Pipeline Runner API stale guard 보강: `app/api/pipeline_runner.py`가 AADS 로컬 runner PID만 `/proc`로 검증하고, KIS/GO100/SF/NTV2 원격 runner PID는 로컬 dead PID로 오탐하지 않도록 분리했다.
- AADS 로컬 `running/claimed` 작업의 runner PID가 죽었고 120초 이상 지난 경우, 새 작업 제출 전 `process_died` error로 정리해 dedup/lock이 막히는 재발을 줄인다.
- Contabo sync 개선: `scripts/sync-to-contabo.sh` lockfile PID 재사용 오탐을 줄이고, dashboard blue-green deploy는 `AADS_DASHBOARD_QA_STRICT=true`로 실행해 QA `UNKNOWN/ERROR`가 단순 성공 sync로 기록되지 않게 했다.
- 운영 cron 정리: root crontab의 존재하지 않는 `scripts/launch-sync.sh` 호출을 제거했고, `/etc/cron.d/contabo-sync`의 실제 `sync-to-contabo.sh` 5분 주기만 남겼다.
- 검증: `python3 -m py_compile app/api/pipeline_runner.py`, `bash -n scripts/sync-to-contabo.sh`, `pytest -q tests/unit/test_pipeline_runner_reliability.py` 통과.

## 2026-05-14
- Codex CLI quota 안내 중 `You've hit your limit · resets 3am (Asia/Seoul)` 고정시각 패턴이 `_parse_quota_reset_seconds()`에서 다음 03:00 KST까지의 초 단위로 파싱되는지 회귀 테스트를 추가했다.
- 검증: `pytest tests/unit/test_model_selector_dynamic_routing.py -q` 20개 통과. 운영 컨테이너 `aads-server`에서도 `resets 3am (Asia/Seoul)`이 다음 03:00 KST 기준 복구 시간으로 파싱됨을 확인했다.

## 2026-05-13
- 2026-05-13 KST: CEO PC 로컬 양자화/멀티모달 모델 브릿지 준비를 추가했다. `scripts/local_model_install_queue.json`을 canonical queue로 두고 `app/services/local_model_manager.py`가 queue/status/single-item install-test 라우팅을 담당한다.
- PC Agent에 `local_model_queue_status`, `local_model_install_test`, `local_model_media_job` 안전 핸들러를 추가하고 `pc_agent/VERSION`을 `1.0.26`으로 올렸다. `local_model_install`과 `local_media_job` lease 동시성은 1로 제한해 대형 모델 병렬 설치를 막는다.
- `app/api/local_models.py` REST API와 채팅 도구 `local_model_queue_status`, `local_model_install_test`, `generate_music`, `generate_three_d_asset`, `media_job_status`를 추가했다. `local_image`, `local_video`, `local_music`, `local_3d`는 async media job으로만 준비되며 기본 채팅 모델 라우팅은 변경하지 않았다.
- DB 준비: `093_pc_ollama_quantized_model_pack.sql`, `094_pc_ollama_backend_correction.sql`, `095_local_multimodal_model_bridge.sql`을 추가했다. `095`는 `media_generation_jobs.kind`에 `music`, `model_3d`를 허용하고 `pc_local` 모델 레지스트리를 queued/prepared 상태로 seed한다.
- 운영 주의: PC Agent가 offline이거나 새 capability를 아직 받지 못한 경우 설치 완료로 간주하지 않는다. 응답은 queued/prepared 또는 offline/not-updated 상태로만 보고해야 한다.
- 2026-05-13 14:11 KST: PC Agent `1.0.27` 준비. Ollama `/api/chat`의 `think` 값을 top-level로 전달하도록 보정하고, 로컬 모델 smoke test가 `content`가 비어 있을 때 `thinking/reasoning`을 fallback으로 기록하도록 수정했다. `self_update` 명령은 성공했지만 이후 WebSocket 재연결이 아직 확인되지 않아 모델 설치/테스트 재개는 PC Agent 재실행 후 진행해야 한다.

## 2026-05-12
- 2026-05-12 15:20 KST: `app/services/chat_service.py`에 응답 누락 지적 속 인용 지시 승격 로직을 추가했다. `"마지막 대화버블에 '...조치하고 보고해'가 남아 있는데 왜 응답을 못하나"` 유형은 인용된 실제 CEO 지시를 이번 턴 실행 대상으로 시스템 프롬프트에 다시 붙인다.
- 검증: `python3 -m py_compile app/services/chat_service.py`, `.venv/bin/python -m py_compile app/services/chat_service.py`, `.venv/bin/python` 직접 헬퍼 호출 검증 통과. 현재 `.venv`에는 `pytest`가 없어 추가 단위 테스트 실행은 미수행.
- Browser Bridge 업무 키 전용 세션 매니저 적용: `ntv2-sinsang-registration`, `ntv2-china-sourcing-admin`, `ntv2-vvic-scrape` 같은 업무 키로 세션을 확보한다.
- `ntv2-sinsang-registration`은 보호 세션이다. 중국상품소싱 관리자 검수나 VVIC 수집 자동화는 이 세션을 공유하지 말고 `browser_work_key`를 지정해 별도 세션을 사용한다.
- 사용 예시: `browser_connect(action="ensure_work_session", work_key="ntv2-china-sourcing-admin")`, 이후 `browser_navigate(url="...", browser_work_key="ntv2-china-sourcing-admin")`.
- 상태 확인: `browser_connect(action="status")` 또는 `GET /api/v1/browser-bridge/work-sessions`에서 label, storage, leased, last_used_at, work_key/protected 매핑을 확인한다.
- AADS/vault 자동 로그인은 분리 세션(`browser_work_key` 또는 `browser_session_id` 명시)에서만 수행한다.
- 2026-05-12 13:43 KST: `app/services/model_selector.py`에서 Codex relay와 Claude CLI relay 재시도 정책을 기본 `2초 간격 x 30회`로 통일했다. 환경변수 `AADS_RELAY_RETRY_INTERVAL_SECONDS`, `AADS_RELAY_RETRY_MAX_RETRIES`로 조정 가능하다.
- Claude CLI relay는 partial 응답이 있으면 재시도 요청에 직전 assistant 초안을 붙여 같은 모델이 이어 쓰도록 보강했다. 명시적 quota/결제/인증/`You've hit your limit ... resets` 계열은 재시도하지 않는다.
- 검증: `python3 -m py_compile app/services/model_selector.py` 통과. `pytest -q tests/unit/test_model_selector_dynamic_routing.py::test_stream_cli_relay_retries_same_model_before_returning_done tests/unit/test_model_selector_dynamic_routing.py::test_stream_codex_relay_retries_same_model_before_returning_done tests/unit/test_model_selector_dynamic_routing.py::test_relay_retry_policy_defaults_to_two_seconds_thirty_retries` 3개 통과.

## 2026-05-11
- Pipeline Runner dashboard-target guard applied in `scripts/pipeline-runner.sh`: AADS jobs that reference `/root/aads/aads-dashboard`, `aads-dashboard`, or chat/dashboard frontend paths now run against `/root/aads/aads-dashboard` instead of the backend workdir.
- Runner now blocks `awaiting_approval` when the actual target repo has zero git diff, preventing read-only/tmp-copy failures from being reported as successful work.
- Approval/reject/deploy cleanup now resolves the same target workdir from the stored job instruction, so dashboard worktrees are committed, pushed, reverted, or removed in the dashboard repo rather than `aads-server`.
- AADS dashboard-target deploy skips backend reload/bluegreen and runs the dashboard deploy path; dashboard rollback also redeploys the dashboard path instead of the backend path.

## 2026-05-09
- Project Change Promoter implemented: completed runner jobs and raw `memory_facts` change events can now be promoted into `architecture_decision`, `feature_change`, `api_contract`, and `data_model_change` facts.
- `workspace_preloader` now prioritizes those strategic change categories in a `최근 중요 변경 자동 인지` block so future sessions can recognize important architecture/function/API/DB changes automatically.
- Canary applied on live DB: 4 CEO workspace changes were promoted with embeddings; duplicate prevention uses `project_change_promoter` source tags.
- Operational note: scheduler registration is coded as `project_change_promoter` every 30 minutes, but the running API process must be restarted/deployed before that scheduler is active.

## 2026-05-06
- Pipeline Runner audit/remediation applied: `db_exec()` now suppresses empty `UPDATE ... RETURNING` command tags, preventing `UPDATE 0` from being misread as job ids and producing repeated `invalid session_id` logs.
- Failure semantics hardened: runtime/watchdog/token/restart/shutdown/deploy-lock/rollback failures now use `status='error'` with explicit `phase/error_detail` instead of `cancelled/superseded`.
- Actual model trace hardened: runner now records Codex effective model after CLI normalization/fallback; API job detail now exposes `model`, `worker_model`, `actual_model`, `size`.
- Chat-direct AI review diff capture hardened: `tool_executor` now quotes git paths, strips `run_remote_command` wrappers, and falls back to local `git -C ... diff` for AADS when SSH/host bridge git diff capture fails.
- DB queue cleanup performed: 6 stale queued jobs, 1 stale rolling_back job, 1 invalid `failed` row, and 6 mismatched `rejected_done` phases normalized.
- Server sync: 68 and 114 runner scripts updated and restarted. 211 script updated on disk only; do not restart until active GO100 job `runner-d59beba6` finishes.
- Technical record added: `docs/reports/20260506_RUNNER_AUDIT_REMEDIATION.md`.

## 2026-05-04
- Android Agent Galaxy Z Fold6 remote-control follow-up: `runner-4f922625` code implementation is committed in `aads-server` as `05c7dc7`; follow-up sensor JSON hardening skips non-finite values during `JSONArray` serialization.
- Chat visibility fix: `runner_response` assistant messages are no longer hidden by backend/dashboard message filters, so saved AI review/status reports remain visible in the main chat timeline.
- Pipeline Runner docs/config: AADS per-project concurrency is documented/configured as `MAX_CONCURRENT_PER_PROJECT=6`; global limit remains 10.
- Technical record added: `docs/reports/20260504_ANDROID_AGENT_CHAT_VISIBILITY_TECHNICAL.md`.
- Caution: `runner-4f922625` has finalize/deploy timeout history. Future deploy-complete reports require fresh health/API/APK download verification.

## 2026-04-24
- Phase 1-C: claude_md_merger ready, /api/v1/ops/claude-md endpoint live

## 2026-07-26 API liveness follow-up
- AADS chat replay/stale-runtime 배포 후 Docker health가 `/api/v1/health`의 종합 점검 지연으로 10초 timeout에 걸리는 문제를 확인했다. 컨테이너 liveness 전용 `/health/live`, `/api/v1/health/live`를 추가하고 API blue/green healthcheck를 `/health/live`로 전환했다. 기능 health는 기존 `/api/v1/health` 유지, 컨테이너 생존 판정은 경량 경로로 분리한다.

## 2026-08-02 OHVIS 5초 즉답 아키텍처 기획
- CEO 요청으로 5초 즉답, 심층 작업 레이어, 맥락 캐시, 백그라운드 진행상태 실시간 표시, 1차/2차/3차 질문 완료 순서 역전 시 취합 정책을 문서화했다.
- 신규 문서: `docs/reports/20260802_OHVIS_5SEC_IMMEDIATE_RESPONSE_ARCHITECTURE.md`.
- 현재 실측: 최근 7일 `chat_turn_executions`는 `completed=169`, `interrupted=31`, `running=2`; 30분 초과 running/retrying stale은 0건; 최근 7일 assistant 소요시간 보유는 192/334건.
- 구현 권장: P0는 additive migration + backend SSE/job 원장 + dashboard 진행 UI가 필요한 L 규모 작업이므로 Pipeline Runner로 `AADS-OHVIS-5SEC-IMMEDIATE-RESPONSE-P0-20260802`를 제출하는 방식이 적절하다.

## 2026-08-04 10:39 KST - NTV2/V1 구뉴톡 이미지정렬 `{GoodsCode_15}` 토큰 제외

- 요청: 구뉴톡 상품 이미지정렬 후 생성 HTML에 `{GoodsCode_15}`가 노출되는 문제 제거.
- 원인: 정렬 저장 컬럼에는 토큰이 없었고, 기존 `goods_detail.DanharooDescription` 262건 끝부분에 `<br />` 뒤 `{GoodsCode_15}` 템플릿 토큰이 남아 있었다.
- 조치:
  - `/home/danharoo/www/application/controllers/products.php`에 정렬 이미지 공통 필터 `_get_existing_sort_images()`를 추가하고, 정렬 화면/API/MO/Skin8 상세 생성에서 `{}` 포함 값, 경로 값, 실제 썸네일 파일이 아닌 값을 제외하도록 했다.
  - `/home/danharoo/www/application/views/products/goods_img_sorting_test1.php`의 저장/상세저장 JS에 `getValidSortImageTitle()`을 추가해 클라이언트에서도 `{GoodsCode_15}` 같은 토큰을 hidden sort list에 싣지 않게 했다.
  - DB `goods_detail.DanharooDescription`에서 literal `{GoodsCode_15}` 잔여 토큰 262건을 제거했다. 정렬 컬럼 `GoodsSortImg1~4`는 변경 대상 0건.
- 백업:
  - `/home/danharoo/www/application/controllers/products.php.bak_aads_20260804_goodscode15_filter`
  - `/home/danharoo/www/application/views/products/goods_img_sorting_test1.php.bak_aads_20260804_goodscode15_filter`
- 검증:
  - `php -l /home/danharoo/www/application/controllers/products.php` 성공.
  - `php -l /home/danharoo/www/application/views/products/goods_img_sorting_test1.php` 성공.
  - DB 잔여 `{GoodsCode_15}` count: `GoodsSortImg1~4=0`, `Description=0`, `DanharooDescription=0`.
  - `https://newtalk.kr/products/goods_img_sorting_test1/set1600k18` 응답에서 `{GoodsCode_15}` 미검출.
- 상태: 운영 파일 직접 핫픽스 반영. 커밋/푸시/정식 배포 없음.

## 2026-08-18 21:10 KST - AADS 채팅 CEO 지시 버블 사라짐 보강
- 대상 세션 `77174902-a5e6-4705-9131-70e699bd0ee7`에서 총 메시지 735건 중 자동/러너성 메시지 357건이 확인됐다.
- P0 러너 `runner-a79eebaa` 승인 diff에 `app/data/yeoljeong_finance/platform_accounts.json` 등 작업 범위 밖 변경이 섞여 반려했다.
- 직접 보강: `app/services/chat_service.py`의 메인 메시지 조회 제외 필터에 `pipeline_runner`, `runner_notification` intent를 추가해 LIMIT 전에 자동 runner 이벤트를 제외한다.
- 직접 보강: `aads-dashboard/src/app/chat/page.tsx`의 runner 판별에 `pipeline_runner`, `runner_notification` intent를 추가해 메인 대화 버블이 아니라 로그 레인 대상으로 분리한다.
- 기존 정책 유지: `runner_response`는 2026-05-04 결정에 따라 저장된 검수/상태 보고가 보이도록 숨김 대상에 넣지 않는다.
- 검증: `python3 -m py_compile app/services/chat_service.py app/routers/chat.py` 성공, `npm run build` 성공. 배포/푸시는 별도 상태 확인 필요.

## 2026-08-19 11:52 KST - 열정국밥 중화점 배민 PC Agent 자동수집 반영
- 요청: 연동설정에 저장된 중화점 판매채널 계정으로 즉시 수집을 실행하고 정상 수집되게 조치.
- 조치: `app/services/yeoljeong_finance_service.py`에서 배민 PC Agent 로그인 세션 홈 대시보드의 주문금액, 입금예정금액, 리뷰 스니펫을 원장 레코드로 반영하도록 보강했다.
- 조치: `app/services/yeoljeong_delivery_collectors.py`에서 포털 HTML 파싱 중 `None` 컬럼 키가 섞여도 source id 생성이 실패하지 않게 수정했다.
- 조치: 쿠팡이츠 등 비배민 포털 보안 차단 시 배민 메시지로 표시되던 상태 메시지를 서비스명 기준으로 보정했다.
- 실측 결과: PC Agent 배민 세션 로그인 후 중화점 배민 수집 성공, 응답 기준 매출 1건/정산 1건/리뷰 288건 수집. 원장 병합 후 중화점 배민 원장에는 매출 3건, 정산 1건, 리뷰 167건이 존재한다.
- 남은 이슈: 쿠팡이츠는 포털 보안 차단, 땡겨요는 추가 인증 요구, 요기요는 인증 후 조회기간 내 0건 상태다. 각 포털별 PC 로그인 세션 또는 인증 처리가 필요하다.
- 검증: `pytest tests/unit/test_yeoljeong_finance_service.py::test_baemin_dashboard_records_extracts_home_summary tests/unit/test_yeoljeong_finance_service.py::test_sync_delivery_uses_baemin_pc_agent_session_without_password tests/unit/test_yeoljeong_delivery_collectors.py::test_normalize_record_tolerates_none_header_key -q` 컨테이너 기준 3 passed.

## 2026-08-21 13:02 KST - PC Agent 관리형 브라우저 종료 안전 가드
- 요청: 프로젝트 자동화 브라우저 정리 시 CEO가 직접 사용하는 일반 Chrome 창은 닫지 않도록 보강.
- 확인: `browser_launch`는 work_key 재사용과 `new_window=false`가 이미 반영되어 있었지만, `browser_close_session`은 등록된 세션의 프로필 루트가 PC Agent 관리형인지 검증하지 않았다.
- 조치: `pc_agent/commands/browser_auto.py`에서 `%LOCALAPPDATA%/KakaoBot/cdp-profile` 하위 관리형 프로필만 탭/프로세스 종료를 허용하고, 일반 사용자 프로필 또는 출처 불명 세션은 `UNMANAGED_BROWSER_PROFILE`로 거부하도록 변경했다.
- 검증: `python3 -m py_compile pc_agent/commands/browser_auto.py tests/unit/test_cdp_session_manager.py` 성공. 현재 소스를 마운트한 컨테이너에서 `pytest tests/unit/test_cdp_session_manager.py tests/unit/test_pc_agent_routing_leases.py tests/test_pc_agent_command_builder.py -q` 69 passed.

## 2026-08-21 13:40 KST - PC Agent 자동 cleanup 서버 측 브라우저 보호 보강
- 확인: 운영 PC Agent 배포 파일은 `1.0.61`로 갱신됐지만, CEO PC 실행 프로세스는 구형 EXE가 남아 있어 `browser_close_session` 내부 가드가 즉시 메모리에 로드됐다고 단정할 수 없었다.
- 조치: `app/services/pc_agent_manager.py`에서 브라우저 명령 타임아웃/완료 후 자동 cleanup의 기본값을 `close_browser=false`, `close_tabs=false`로 변경했다. 명시 파라미터가 없는 자동 정리는 세션 해제만 요청하고 창/탭 종료를 지시하지 않는다.
- PC 조치: CEO PC 설치 폴더의 Agent ZIP 동기화를 수동 실행해 `VERSION=1.0.61` 및 `commands/browser_auto.py`의 `UNMANAGED_BROWSER_PROFILE` 가드 존재를 확인했다. 일반 Chrome 또는 관리형 Chrome 프로세스는 종료하지 않았다.
- 검증: `python3 -m py_compile app/services/pc_agent_manager.py` 성공. 현재 소스를 마운트한 컨테이너에서 `pytest tests/unit/test_pc_agent_routing_leases.py -q` 17 passed.

## 2026-08-21 14:23 KST - PC Agent 판매채널 자동수집 기획서 및 재사용 스킬 추가
- 요청: PC Agent를 이용한 판매채널 데이터 자동수집 기획문서 존재 여부를 확인하고, 없으면 상세 기획/아키텍처를 작성하며 모듈화/스킬화해 다른 곳에서도 사용할 수 있게 정리.
- 확인: `docs` 목록과 관련 검색에서 판매채널 자동수집 전용 기획서는 없었다. 구현은 `app/services/yeoljeong_finance_service.py`, `app/services/yeoljeong_delivery_collectors.py`, `scripts/yeoljeong_auto_collect.py`, `app/static/apps/yeoljeong-finance/index.html`에 분산되어 있었다.
- 조치: `docs/plans/20260821_SALES_CHANNEL_PC_AGENT_COLLECTION_ARCHITECTURE.md`를 추가해 현재 구현, 목표/비목표, PC Agent 중심 아키텍처, payload/result/status 계약, 모듈화 기준, 운영 실행서, 수용 기준, 개선안을 정리했다.
- 조치: `.claude/skills/sales-channel-collector/SKILL.md`를 추가해 수집 실행, 진단, 신규 채널 추가, 타 프로젝트 이식 절차를 반복 사용 가능한 스킬로 정리했다.
- 조치: `docs/ARCHITECTURE-INDEX.md`에 FOOD 판매채널 자동수집 섹션을 추가해 정본 문서와 스킬 진입점을 등록했다.
- 추가 조치: 전체 finance service 테스트 중 드러난 기존 땡겨요 CAPTCHA 경로의 fallback 클릭 판정을 보정했다. `page.evaluate()` fallback은 브라우저 boolean `true`일 때만 클릭 성공으로 인정해 문자열 반환 mock 또는 비표준 adapter 응답을 오인하지 않는다.
- 검증: `git diff --check` 성공. 현재 저장소 전체를 `aads-server-aads-server` 이미지에 `/work`로 마운트한 일회성 컨테이너에서 `python3 -m py_compile app/services/yeoljeong_delivery_collectors.py app/services/yeoljeong_finance_service.py scripts/yeoljeong_auto_collect.py app/services/yeoljeong_bank_browser_connector.py` 성공, `pytest tests/unit/test_yeoljeong_delivery_collectors.py tests/unit/test_yeoljeong_finance_service.py -q` 129 passed, `pytest tests/unit/test_bank_browser_connector.py -q` 38 passed. 스킬 형식 검증 `quick_validate.py .claude/skills/sales-channel-collector`는 `Skill is valid!`로 통과.

## 2026-08-24 07:12 KST - 열정국밥 은행 브라우저 자동수집 P0/P1 보강
- 요청: 은행 데이터 수집 자동화 구현 상태 확인 후 가능한 자동화를 모두 반영하고 즉시 데이터 수집 실행.
- 조치: `--until-complete` 자동수집에서 은행 계좌가 누락되지 않도록 수정했고, 은행 브라우저 커넥터에 포털 상태 분류, OTP/CAPTCHA/인증서/본인확인 감지, 안전한 input focus, 거래내역/입출금/조회 버튼 자동 탐색, 거래 테이블 재파싱, no-records 정상 완료 처리를 추가했다.
- 조치: PC Agent Browser Bridge의 sidecar timeout을 은행 포털용으로 완화하고, Playwright식 `goto/evaluate(timeout=...)` 인자를 실제 command timeout에 반영했다. 동일 work_key의 반복 복구 루프를 제한하고 브라우저 전체 수집 deadline 초과 시 `BANK_BROWSER_PC_AGENT_TIMEOUT`을 반환하도록 했다.
- 운영 실측: `/app/yeoljeong-data` 기준 신한은행 기업 브라우저 계좌 1개가 `active/auto_sync`로 등록되어 있으나, `oby-ceo`와 `DESKTOP-ICU55HK` PC Agent 모두 신한은행 페이지 browser launch/evaluate 단계에서 timeout이 발생했다. `2026-08-01`~`2026-08-24` 즉시 수집 결과는 `BANK_BROWSER_PC_AGENT_TIMEOUT`, imported_rows=0, 거래 원장 `bank_transactions` count=0.
- 검증: `python3 -m py_compile app/browser_bridge/service.py app/services/auth_challenge_orchestrator.py app/services/yeoljeong_bank_browser_connector.py app/services/yeoljeong_finance_service.py scripts/yeoljeong_auto_collect.py` 성공. 현재 소스를 `aads-server-aads-server` 이미지에 마운트해 `pytest tests/unit/test_yeoljeong_bank_browser_connector.py tests/unit/test_yeoljeong_auto_collect.py tests/unit/test_auth_challenge_orchestrator.py -q` 실행, 66 passed.
- 남은 이슈: 실거래 적재는 은행 포털 세션이 PC Agent에서 안정적으로 열리고 인증/조회 화면까지 도달해야 가능하다. 현재는 자동화 코드가 아니라 PC Agent 브라우저 route timeout이 차단 원인이다.

## 2026-08-24 07:21 KST - 은행 브라우저 세션 재사용 및 간편조회 로그인 연결 보강
- 요청: 판매채널 PC Agent 자동수집과 은행 수집의 관계를 확인하고, 은행 자동수집이 브라우저를 반복 새창으로 여는 문제와 은행 간편조회 이용자ID 로그인 처리 여부를 즉시 조치.
- 확인: 판매채널과 은행은 같은 PC Agent/Browser Bridge 기반이지만 work_key를 분리한다. 문제는 판매채널용 `force_recreate_portal_sessions`가 은행 수집 payload의 `force_recreate_browser`로 전파되어 은행 세션까지 강제 재생성될 수 있던 점이다.
- 조치: `scripts/yeoljeong_auto_collect.py`에서 판매채널 세션 재생성 옵션을 은행 브라우저에 전파하지 않도록 분리했다. 은행 세션 강제 재생성은 명시적 `force_recreate_bank_browser`에만 반응한다.
- 조치: `app/services/yeoljeong_bank_browser_connector.py`에서 기존 은행 탭 URL이 같은 은행 도메인이면 `goto()`를 생략하고 기존 탭을 재사용하도록 했다. 여러 탭이 있으면 같은 은행 포털 탭을 우선 선택한다.
- 조치: `app/services/yeoljeong_finance_service.py`에서 `bank_accounts` 계좌 원장과 `platform_accounts` 은행 간편조회 연동설정 원장을 사업자/지점/은행코드로 매칭해 저장된 이용자ID, 로그인 비밀값, 계좌번호, 계좌 비밀값, 사업자번호를 커넥터에 전달하도록 연결했다.
- 조치: 커넥터는 로그인 화면 감지 시 저장 자격으로 ID/비밀값/계좌정보를 브라우저 DOM에 자동 입력하고 조회/로그인 버튼을 누른 뒤 거래 테이블을 재확인한다. diagnostics에는 값과 `password` 필드명을 남기지 않고 `secret` 존재 여부만 기록한다.
- 실수집 재시도: 로컬 프로세스에서 신한은행 계좌 1개에 대해 `2026-08-01`~`2026-08-24` 수집을 호출했으나 active AADS API route DNS/localhost 연결 실패 후 `BANK_BROWSER_PC_AGENT_TIMEOUT`, imported_rows=0으로 종료됐다. 이는 이번 로컬 실행 환경의 PC Agent 라우팅 문제이며, 코드 회귀 테스트는 통과했다.
- 검증: `.venv-playwright/bin/python -m pytest tests/unit/test_yeoljeong_bank_browser_connector.py tests/unit/test_yeoljeong_auto_collect.py -q` 66 passed. `python3 -m py_compile app/services/yeoljeong_bank_browser_connector.py app/services/yeoljeong_finance_service.py scripts/yeoljeong_auto_collect.py` 성공. 대상 파일 `git diff --check` 성공.
- 남은 이슈: 변경분은 아직 커밋/푸시/배포되지 않았다. 운영 컨테이너와 CEO PC Agent에 반영하려면 선별 커밋, 배포, PC Agent 업데이트/재시작 후 같은 은행 work_key로 재수집해야 한다.

## 2026-08-24 07:26 KST - 최종 완료보고 보정 전 회귀 검증
- 보정: 판매채널 브라우저 세션 생성 URL 계약이 은행 작업 중 바뀌어 기존 테스트 4건이 실패했다. 판매채널은 기존처럼 세션 재생성 때만 실제 URL을 넘기고 평시에는 `about:blank`를 쓰도록 되돌렸다. 은행 브라우저 탭 재사용/로그인 자동입력/은행 강제재생성 분리 변경은 유지했다.
- 검증: `.venv-playwright/bin/python -m pytest tests/unit/test_auth_challenge_orchestrator.py tests/unit/test_yeoljeong_bank_browser_connector.py tests/unit/test_yeoljeong_auto_collect.py tests/unit/test_bank_browser_connector.py -q` 107 passed.
- 검증: `.venv-playwright/bin/python -m pytest tests/unit/test_yeoljeong_finance_service.py tests/unit/test_yeoljeong_finance_api.py -q` 138 passed.
- 검증: `.venv-playwright/bin/python -m py_compile app/browser_bridge/service.py app/services/auth_challenge_orchestrator.py app/services/yeoljeong_bank_browser_connector.py app/services/yeoljeong_finance_service.py pc_agent/commands/browser_auto.py scripts/yeoljeong_auto_collect.py` 성공. `git diff --check` 성공.

# 2026-08-24 — GO100-WAVE-CHART-OVERLAY-DIRECT

- 작업: GO100 차트에서 `go100_wave_decisions` 기반 파동 판단을 일봉 차트 W1~W5 마커로 표시하도록 직접 구현했다.
- 변경 파일: `backend/app/routers/v4_chart.py`, `frontend/src/lib/api/chart.ts`, `frontend/src/components/market/StockChart.tsx`, `frontend/src/go100/components/chart/StockChartWorkspace.tsx`, `docs/HANDOVER.md`.
- 조치: `/api/v4/chart/strategy-signals/{stock_code}`가 `strategy=ma_wave` 요청 시 파동 판단을 하루 1개 최신 마커로 반환하게 했고, 프론트 차트는 파동 번호/phase/action을 W마커 색상·위치로 변환한다. 일봉 차트에만 표시해 분봉 시간축 NaN 마커를 방지했다.
- 검증: `python3 -m py_compile backend/app/routers/v4_chart.py` 성공, `npm --prefix frontend run lint` 성공, `./node_modules/.bin/tsc --noEmit` 성공, `git diff --check` 성공, `curl http://127.0.0.1:8002/health` 200 확인, `curl http://127.0.0.1:3001/go100/chart?code=005930` 보호 라우트 로그인 리다이렉트 확인.
- 빌드: `npm run build`는 컴파일/타입확인/정적 페이지 85개 생성까지 통과했으나 마지막 `Collecting build traces` 단계에서 exit 143으로 종료되어 전체 빌드 완료 판정은 보류한다. 기존 React hook warning 8건은 변경 범위 밖이다.
- 배포/커밋: 서비스 재시작, 커밋, 푸시는 수행하지 않았다. 기존 dirty 파일(`wave_lgbm.pkl`, `LimitupTrackerPage.tsx`, `WaveTrainingPage.tsx`, `artifacts/` 등)은 보존했다.
- 영향 분리: GO100 차트 API/프론트 표시만 변경했다. KIS 주문·매매 로직과 DB 스키마는 변경하지 않았다.

## 2026-08-24 08:41 KST - FOOD 은행 브라우저 수집 보강 푸시/배포

- 요청: 은행 브라우저 수집 보강 커밋을 다음 단계로 진행.
- 조치: `main`의 ahead 3커밋(`be782858`, `83b5a55a`, `c2873512`)을 `origin/main`에 푸시했다. 핵심 은행 커밋은 `c2873512 fix(food): stabilize bank browser collection reuse`.
- 배포: `bash /root/aads/aads-server/deploy.sh`로 AADS blue-green 배포를 실행했다. 첫 시도는 backup target `8100`의 active-stream 보호로 중단됐고, 이후 target 슬롯 재빌드가 진행되어 active 포트가 `8102`에서 `8100`으로 전환됐다.
- 검증: `curl http://127.0.0.1/api/v1/health -H 'Host: aads.newtalk.kr'` 및 `curl http://127.0.0.1:8100/api/v1/health` 모두 `status=ok`, `graph_ready=true`.
- 운영 재검증: 배포된 `aads-server:8100`에서 신한 기업 브라우저 계좌 1개를 `2026-08-01`~`2026-08-24` 범위로 수집 재시도했다. 이전 `BANK_BROWSER_PC_AGENT_TIMEOUT`은 재현되지 않았고, 신한 기업 페이지 `https://bizbank.shinhan.com/main.html`까지 도달했다.
- 현재 결과: 수집 상태는 `BANK_BROWSER_OPERATOR_ACTION_REQUIRED`, `imported_rows=0`, `collected_rows=0`. 같은 browser work_key `yeoljeong-bank-browser-25b8c525d84799c2`, session `bb-343beb2ff19d`에서 OTP/CAPTCHA/본인인증 등 운영자 입력 후 재수집 필요.
- 남은 이슈: 거래 원장 `bank_transactions` 적재는 아직 완료되지 않았다. PC Agent는 페이지 도달까지 정상화됐으나 은행의 추가 인증 단계가 자동화 차단점이다.

## 2026-08-24 17:34 KST - FOOD 신한 WebSquare 거래 테이블 파서 보정 배포

- 요청: 신한은행 브라우저 수집 보강 변경을 커밋/푸시/배포까지 완료.
- 조치: `app/services/yeoljeong_bank_browser_connector.py`에서 신한 WebSquare 헤더의 `오름차순 정렬` 문구와 `(원)` 단위를 정규화하도록 보정했다. 거래 헤더만 있고 거래행이 없는 화면은 파서 실패가 아니라 거래 0건으로 판정하도록 `transaction_header_found` 진단값을 추가했다.
- 검증: `.venv-playwright/bin/python -m py_compile app/services/yeoljeong_bank_browser_connector.py` 성공, `.venv-playwright/bin/python -m pytest tests/unit/test_yeoljeong_bank_browser_connector.py -q` 46 passed, `git diff --check -- app/services/yeoljeong_bank_browser_connector.py tests/unit/test_yeoljeong_bank_browser_connector.py` 성공.
- 배포: 커밋 `f6f54da9 fix(finance): parse Shinhan sortable transaction headers`를 `origin/main`에 푸시했고, 17:42 KST AADS blue-green 배포로 active 슬롯이 `8102`에서 `8100`으로 전환됐다. 배포 스크립트 Health/DB/Chat/LLM 검증 통과, 외부 nginx 경로와 `8100` 직접 health 모두 `status=ok` 확인.

## 2026-08-24 17:58 KST - FOOD 언니냉면 연동설정 중복 지점 정리

- 요청: 언니냉면 사업자정보 입력 후, 다른 세션에서 미리 생성된 연동설정 중복을 정리.
- 조치: `biz-eonni-naengmyeon` 사업자 아래 active 지점이 `언니냉면`, `성신여대역점` 2개로 보여 연동/수집 원장의 지점값을 `성신여대역점`으로 정규화했다.
- DB 반영: `yeoljeong_platform_accounts` 4건, `yeoljeong_delivery_sales` 92건, `yeoljeong_delivery_settlements` 92건, `yeoljeong_delivery_reviews` 369건, `yeoljeong_delivery_collection_status` 24건을 `언니냉면`에서 `성신여대역점`으로 변경했다. `yeoljeong_branches.id='branch-eonni-naengmyeon'`은 `inactive` 및 `deleted_at` 소프트 삭제 처리했다.
- 파일 반영: `app/data/yeoljeong_finance/platform_accounts.json`의 언니냉면 연동 4건도 `성신여대역점` 지점/라벨로 보정했다.
- 롤백 백업: 컨테이너 `/app/tmp/yeoljeong_eonni_branch_cleanup_20260824_1753KST.json`에 변경 전 DB row와 JSON row를 저장했다.
- 검증: active DB 기준 `언니냉면` 지점 참조 0건, `성신여대역점` 참조는 계정 4건/매출 92건/정산 92건/리뷰 369건/수집상태 24건. 서비스 `_read('platform_accounts')` 기준 active 계정 30건, 중복 그룹 0건. 언니냉면 연동 4건은 배민/쿠팡이츠/요기요/땡겨요 모두 `business_id=biz-eonni-naengmyeon`, `branch=성신여대역점`으로 확인했다.
- 배포/커밋: DB/JSON 운영 정리 작업이며 서비스 재시작, 커밋, 푸시는 수행하지 않았다. 기존 작업트리 dirty 파일은 보존했다.

## 2026-08-24 18:06 KST - FOOD 언니냉면 삭제 후 지점목록 재노출 원인 차단

- 요청: `https://fb.newtalk.kr/static/apps/yeoljeong-finance/index.html` 사업자·지점 화면에서 지점목록 5개 중 `언니냉면` 1개를 삭제했는데 사라지지 않는 문제 확인 및 조치.
- 원인: `yeoljeong_branches.id='branch-eonni-naengmyeon'` row는 이미 `deleted_at` 소프트 삭제 상태였지만, `app/services/yeoljeong_finance_service.py`의 `CANONICAL_BRANCHES`에 `언니냉면`이 고정 기본 지점으로 남아 있어 `/settings` 정규화 시 삭제된 지점이 다시 병합될 수 있었다. 또한 설정 저장 시 화면에서 빠진 사업자/지점을 DB에서 소프트삭제하는 후처리가 없어 DB 잔존 row가 재노출될 위험이 있었다.
- 조치: `CANONICAL_BRANCHES`에서 `branch-eonni-naengmyeon`을 제거했다. `save_settings_persisted()`가 저장 payload에 포함되지 않은 `yeoljeong_branches`, `yeoljeong_businesses` active row를 `deleted_at=NOW()`로 소프트삭제하도록 보강했다.
- 검증: 운영 DB active 지점 조회 결과 `중화점`, `성신여대점`, `성신여대역점`, `열정국밥_미아점` 4개만 노출됐다. 컨테이너 내부 `python -m py_compile /app/app/services/yeoljeong_finance_service.py` 성공. `_canonicalize_ui_settings()` 단위 확인 결과 `branch-eonni-naengmyeon` 재삽입 여부가 `False`로 확인됐다. 공개 `/settings`는 인증 필요로 HTTP 401이 정상 응답이다.
- 배포/커밋: `deploy_safe(mode=reload)`로 API hot-reload를 실행했다. 사전/사후 `/api/v1/ops/health-check` 모두 정상이고 hot-reload 결과 `재로드=72개`로 확인됐다. 커밋, 푸시는 수행하지 않았다.

## 2026-08-24 19:05 KST - FOOD 언니냉면 연동설정 활성 지점 재연결

- 요청: 삭제 처리 후 언니냉면 연동건을 다시 사업자/지점에 연결.
- 조치: 운영 컨테이너의 `yeoljeong_finance_service._read/_write('platform_accounts')` 경로로 판매사이트 4건을 재저장했다. 대상은 배민셀프서비스, 쿠팡이츠, 요기요, 땡겨요이며 모두 `business_id=biz-eonni-naengmyeon`, `branch=성신여대역점`, `status=credential_registered`로 고정했다.
- 유지 정책: 삭제 요청된 지점 row `branch-eonni-naengmyeon`/`언니냉면`은 `inactive` 상태를 유지했다. 지점목록 재노출을 막기 위해 연동은 현재 active 지점 `성신여대역점`으로 연결했다.
- 검증: `yeoljeong_platform_accounts` DB 조회에서 4건 모두 `business_id=biz-eonni-naengmyeon`, `branch=성신여대역점`으로 확인했다. `app/data/yeoljeong_finance/platform_accounts.json`도 동일 값과 `updated_at=2026-08-24T19:05:16+09:00`로 확인했다.
- 배포/커밋: 운영 DB/JSON 원장 보정 작업이다. 서비스 재시작, 커밋, 푸시는 수행하지 않았다.

## 2026-08-24 19:25 KST - FOOD 배민 orders/history 주문상세 collector P0 구현

- 요청: 배민 `https://self.baemin.com/orders/history` 페이지의 주문건별 모든 정보, 정산자료, 리뷰 수집을 위한 다음 단계 즉시 구현 및 DB 수집 필드 리스트 정리.
- 조치: `app/services/baemin_order_history_collector.py`를 추가해 로그인된 PC Agent 배민 페이지에서 `orders/history`로 이동하고 주문번호 기준으로 목록/펼침 상세/주문 추가 정보 텍스트를 파싱한다. 저장 스키마는 `schema_version=baemin_order_history.v1`이며 기존 `yeoljeong_delivery_sales`, `yeoljeong_delivery_settlements`의 `payload JSONB`에 주문/정산 상세를 보관한다.
- 조치: `app/services/yeoljeong_finance_service.py`의 배민 PC Agent 수집 경로가 신규 collector를 먼저 호출하도록 연결했다. 상세 주문 row가 있으면 홈 대시보드 요약 row보다 우선 저장하고, status diagnostics에 `order_history_orders_seen`, `order_history_orders_saved`, `order_history_detail_failed`, `order_history_settlement_pending`을 남긴다.
- 문서: `docs/plans/20260824_BAEMIN_ORDER_HISTORY_FULL_PARSER_PLAN.md`에 DB 물리 컬럼과 `payload` 수집 필드 리스트를 추가했다. 리뷰는 `orders/history`가 아니라 리뷰관리 화면 전용 collector가 필요하다고 분리 기록했다.
- 검증: `python3 -m py_compile app/services/baemin_order_history_collector.py app/services/yeoljeong_finance_service.py` 성공, `pytest tests/unit/test_baemin_order_history_collector.py -q` 3 passed. 호스트 전체 delivery collector 테스트는 `structlog` 미설치로 collection 단계 실패했고, 운영 컨테이너는 배포 이미지 기준이라 신규 미커밋 테스트 파일을 보지 못했다.
- 남은 이슈: 운영 PC Agent로 실제 배민 로그인 세션에서 최근 7일 E2E 수집을 돌려야 한다. 리뷰 전체 수집은 아직 별도 `baemin_review` collector 미구현 상태다.
- 배포/커밋: 이번 변경은 아직 커밋/푸시/배포하지 않았다.

## 2026-08-25 08:09 KST - FOOD 사업자별 중복 연동설정 정리

- 요청: 모든 사업자별 지점 중복 연동건을 확인하고, 정상 입력값이 있는 계정은 삭제하지 않도록 비교 후 1건만 유지하고 나머지 정리.
- 기준: `business_id + branch + service`가 같은 활성 연동을 중복으로 판정했다. 유지 후보는 `credential_registered` 상태, payload 필드 수, 암호화/마스킹 필드 존재, 최근 동기화 필드 존재 여부를 비교했다.
- 유지: `biz-junghwa/중화점/baemin`은 `83c5b12f-0b3d-46b6-bcbe-b5c00dc0fd51` 1건을 유지했다. `biz-junghwa/중화점/ibk_business`는 `7c392171-9f1a-46c0-a390-34249702afd1` 1건을 유지했다.
- 정리: DB `yeoljeong_platform_accounts`에서 중복 5건(`31309922-2104-430f-9f64-d416f894f3f9`, `5cde6a62-254b-4eac-bb3d-26029276b594`, `482e4bff-41dd-4be5-a1ed-4c1ba82dfe84`, `acct-baemin`, `f1e4bfd0-6175-4a86-88bc-efe896921256`)을 `deleted_at=NOW()`로 소프트삭제했다.
- 파일 원장: `app/data/yeoljeong_finance/platform_accounts.json`에서도 같은 5건을 제거해 서비스 `_read('platform_accounts')`가 소프트삭제 row를 재활성화하지 않도록 맞췄다. 파일 원장은 30건에서 25건으로 감소했다.
- 보호 확인: 언니냉면(`biz-eonni-naengmyeon`) 판매사이트 4건은 모두 `성신여대역점`에 `credential_registered` 상태로 유지했다.
- 검증: DB 활성 중복 그룹 0건, 파일 원장 중복 그룹 0건, 서비스 `_read('platform_accounts')` 병합 결과 25건/중복 0건, 운영 컨테이너 내부 파일 25건/중복 0건을 확인했다. `yeoljeong-finance` 컨테이너는 healthy, 공개 `index.html`은 HTTP 200이다.
- 롤백: DB는 위 5개 row_id의 `deleted_at`을 NULL로 되돌리면 복구된다. 파일 원장은 이번 변경 diff에서 제거된 5개 JSON 객체를 복원하면 된다.
- 배포/커밋: 운영 DB와 런타임 파일 원장 정리 작업이다. 서비스 재시작, 커밋, 푸시는 수행하지 않았다.

## 2026-08-25 08:19 KST - FOOD 신한 개인사업자 실은행 수집 진행

- 요청: 미아점 신한 실은행 데이터 수집까지 구현 진행.
- 조치: 신한 개인사업자 간편조회 전용 work_key(`yeoljeong-bank-shinhan-individual-00e6447fd39dad84`)와 상태머신을 보강했다. 로그인 안내 확인, 계좌조회 화면 이동, 계좌번호/계좌비밀번호/조회기간 입력, 조회 제출 단계의 진단값을 남기도록 했다. 은행 수집 API payload에는 `browser_timeout_seconds`를 추가해 운영 호출에서 장시간 무응답을 제한한다.
- 조치: PC Agent `browser_eval` timeout 시 Browser Bridge 세션 재생성 루프가 수집 응답을 장시간 붙잡지 않도록 은행 커넥터 경로에서 local-agent 자동 복구를 비활성화했다.
- 운영 확인: `biz-mia/branch-gangbuk-mia` 은행계좌 1건(`9aa33cb9-5597-4f0d-ab0d-43146c35f318`)과 신한 quick-service 자격증명 1건이 존재하고, 사업자 유형은 `individual`이다.
- 검증: `docker exec aads-server pytest -q tests/unit/test_yeoljeong_bank_browser_connector.py tests/unit/test_yeoljeong_auto_collect.py` 74 passed. `curl http://127.0.0.1:8100/health`는 `status=ok`, `graph_ready=true`.
- 실수집 결과: `2026-08-25` 범위로 `bb-b0a676c8f3e6`/port `59960` 세션에서 수집 API를 실행했으나 `BANK_BROWSER_PC_AGENT_TIMEOUT`, `imported_rows=0`, `collected_rows=0`으로 종료됐다. 운영 파일 원장 `bank_transactions.json`은 0건이다.
- 원인: PC Agent는 `system_info`, `browser_tabs`, `browser_screenshot`, `browser_health`에는 응답하지만, 같은 CDP 세션의 `browser_eval`은 `1+1`, `document.title`, `location.href` 같은 단순 표현도 504 timeout이다. 은행 화면 자체보다 PC Agent Runtime.evaluate 경로 장애로 판정한다.
- 남은 조치: PC Agent의 `browser_eval` 런타임을 복구하거나 해당 명령의 cleanup/timeout 처리를 보강한 뒤 같은 세션/work_key로 재수집해야 한다. 실제 은행 거래 적재는 아직 완료되지 않았다.
- 배포/커밋: AADS API 컨테이너/프로세스를 재시작해 코드 로드는 반영했다. 커밋, 푸시는 수행하지 않았다.

## 2026-08-25 09:17 KST - FOOD 매장비서 로그인 페이지 단순화

- 요청: `https://fb.newtalk.kr/static/apps/yeoljeong-finance/index.html` 로그인 전 화면에 기능 설명과 회원가입 안내가 복잡하게 노출되어 있어 로그인만 남기는 방향으로 개선.
- 조치: `app/static/apps/yeoljeong-finance/index.html`의 로그인 게이트를 `매장비서 로그인` 중심으로 단순화했다. 첫 화면에서 운영 범위 요약 카드, 직원 가입 4단계, 체크리스트, 회원가입 탭, 직원 회원가입 CTA, 보안 설명 스트립을 숨기거나 제거했다.
- 조치: 상단 subtitle을 `열정국밥 운영관리`로 축약하고, 로그인 카드에는 이메일/비밀번호/로그인 버튼만 보이게 했다. 직원 회원가입과 초대 수락 기능 자체는 기존 모달/해시 흐름에 남겨 운영 기능 삭제 없이 첫 화면 노출만 정리했다.
- 검증: HTML 내 inline script 추출 후 `new Function()` 문법 검증 성공, `git diff --check -- app/static/apps/yeoljeong-finance/index.html` 성공. 공개 URL cache-bust 응답에서 `매장비서 로그인`, `운영관리 로그인` 문구가 확인됐고 기존 첫 화면 문구 `직원은 먼저 회원가입하고...`, `직원 회원가입 먼저 하기`, `기존 관리자는 로그인하고...`는 공개 본문 grep에서 미노출 확인했다.
- 배포/커밋: 정적 파일 변경이며 공개 URL은 최신 HTML을 제공 중이다. 커밋, 푸시, 정식 `deploy.sh`는 수행하지 않았다. 기존 작업트리 dirty 파일은 보존했다.

## 2026-08-25 12:21 KST - FOOD 배민 4개 매장 전체 백필 구현 계획 저장

- 요청: 배민 4개 매장 모두 주문건별 모든 정보, 리뷰, 광고현황을 리소스에 무리 없이 전체 백필하도록 추가 구현/보강 기획을 문서로 저장하고 즉시 구현 준비.
- 문서: `docs/plans/20260825_BAEMIN_ALL_STORES_FULL_BACKFILL_IMPLEMENTATION_PLAN.md`를 추가했다. 주문/정산/리뷰/광고 payload 필드 계약, 4개 매장 범위, `orders/history` 최신순 백필 순서, 리뷰/광고 별도 collector, checkpoint/status payload, 리소스 제한, 완료 기준, Runner 구현 지시서를 포함했다.
- 실측: DB 기준 배민 원장은 sales 199건, settlements 196건, reviews 955건, ads 0건이다. 파일 원장 기준 sales는 미아점 184건, 중화점 7건, 성신여대점 4건, 성신여대역점 3건이고, `baemin_order_history.v1` 주문상세는 미아점 180건뿐이다. reviews는 미아점 240건, 중화점 316건, 성신여대점 215건, 성신여대역점 184건이나 `review_text` 채움은 중화점 2건뿐이다.
- 판정: 현재 배민 자동수집은 미아점 중심 주문상세 부분 수집 단계이며, 4개 매장 주문상세/리뷰상세/광고현황 전체 백필 완료 상태가 아니다. 구현은 `baemin_order_history_collector.py` v2 보강, 신규 `baemin_review_collector.py`, 신규 `baemin_ads_collector.py`, `collection_mode=baemin_full_backfill` 연결이 필요하다. 실제 백필 실행 전 배민 queued/running stale 후보 7건도 정리해야 한다.
- 배포/커밋: 문서 저장과 HANDOVER 기록만 수행했다. 커밋, 푸시, 배포는 수행하지 않았다. 기존 작업트리 dirty 파일은 보존했다.

## 2026-08-25 12:59 KST - DESIGN 단하루 명함 파일 즉시 다운로드 링크 조치

- 요청: 채팅에서 안내한 파일을 클릭해도 열 수 없으므로 바로 다운로드 가능하게 재보고.
- 원인: `/api/v1/files/download`는 공개 도메인에서 401 인증 차단되고, `https://aads.newtalk.kr/static/...` 및 `/exports/...` 대시보드 경로는 Next.js 인증/라우팅 때문에 클릭 다운로드에 부적합했다.
- 조치: 단하루 명함 산출물(`namecard_danharoo.svg`, `namecard.html`, JPG 시안 4개)을 `/var/www/certbot/exports/danharoo-namecard-download.zip`으로 묶어 Nginx 공개 export 경로에 배치했다. 향후 호스트 정적 산출물도 파일 API가 찾을 수 있도록 `app/api/files.py` 허용 루트에 `/root/aads/aads-server/app/static`과 하위 fallback 경로를 추가했다.
- 검증: `https://aads.newtalk.kr/exports/danharoo-namecard-download.zip` HEAD/GET 모두 HTTP 200, `content-type=application/zip`, `content-length=150176` 확인. 운영 컨테이너 내부 `_resolve('app/static/namecard_danharoo.svg')`도 `/app/app/static/namecard_danharoo.svg`, 3,628 bytes로 정상 해석 확인.
- 배포/커밋: Nginx 공개 정적 파일 배치라 재시작 없이 즉시 반영됐다. 커밋, 푸시, 정식 배포는 수행하지 않았다. 기존 작업트리 dirty 파일은 보존했다.

## 2026-08-25 13:17 KST - FOOD 배민 전체 백필 구현 직접 보강

- 요청: 이전 단계에서 차단된 배민 4개 매장 주문상세/리뷰/광고 전체 백필 구현의 다음 단계 진행.
- Runner 처리: 기존 `runner-f6427e3e`는 `origin/main`보다 local main이 1커밋 앞선 상태라 `deploy_preflight_git_state`로 error였다. 해당 ahead 커밋은 pre-push 훅 서명 만료로 1차 push가 실패했고, `git commit --amend --no-edit`로 훅 서명을 갱신해 `ca1c37a5 fix(browser-bridge): resolve Playwright browsers path by glob before driver start`를 `origin/main`에 push했다. 이후 재제출한 `runner-3a60e651`은 로그 0건/dead PID로 스톨 판정되어 terminate 처리했으나 뒤늦게 `awaiting_approval`로 올라왔다. Runner diff가 직접 구현본과 달라 충돌/회귀 위험이 있어 최종 거부 처리했다.
- 조치: Runner 재시도 대신 직접 보강했다. `baemin_order_history_collector.py`를 `schema_version=baemin_order_history.v2`로 올리고 날짜 window, 주문상세 클릭 jitter, 페이지 스크롤, checkpoint diagnostics, `BackfillLimits`, `collect_order_history` alias를 추가했다. `baemin_review_collector.py`, `baemin_ads_collector.py`를 신규 추가해 리뷰/광고 payload 계약을 별도 수집한다.
- 조치: `yeoljeong_finance_service.py`에서 `mode=full_backfill`이면 63일 제한을 해제하고 4개 canonical 매장을 순차 scope로 잡도록 연결했다. 배민 PC Agent 경로는 full backfill일 때 주문상세+리뷰+광고 collector를 호출하며, `yeoljeong_delivery_collection_status.payload`에 checkpoint, metrics, 리소스 제한값을 저장한다. `scripts/trigger_delivery_sync.py`는 `--service baemin --mode full_backfill --from --to --max-orders --branch all` CLI를 지원한다.
- 검증: `.venv-playwright/bin/python -m py_compile app/services/baemin_order_history_collector.py app/services/baemin_review_collector.py app/services/baemin_ads_collector.py app/services/yeoljeong_finance_service.py scripts/trigger_delivery_sync.py` 성공. `.venv-playwright/bin/python -m pytest tests/unit/test_baemin_order_history_collector.py tests/unit/test_baemin_review_collector.py tests/unit/test_baemin_ads_collector.py tests/unit/test_yeoljeong_finance_service.py -q` 결과 126 passed. `git diff --check` 대상 파일 통과.
- 남은 이슈: 실제 배민 PC Agent 로그인 세션에서 E2E 백필은 아직 실행하지 않았다. 배민 보안/추가 인증 또는 CEO PC 세션 연결 상태에 따라 `portal_action_required`가 발생할 수 있다.
- 배포/커밋: `ca1c37a5` Browser Bridge 선행 커밋만 push 완료했다. 이번 FOOD 백필 직접 보강 변경은 기존 미커밋 FOOD/은행/문서 변경과 같은 파일에 섞여 있어 아직 별도 커밋, 푸시, 배포하지 않았다.

## 2026-08-25 13:35 KST - FOOD 배민 자동수집 런타임 검증 보강

- 요청: 배민 데이터 자동수집을 빠르게 구현 완료.
- 조치: 배민 주문내역 collector의 실제 브라우저 수집 함수가 파서 결과 `records`를 checkpoint diagnostics보다 먼저 확정하도록 보강된 상태를 확인했다. DOM 후보 추출 정규식은 `T...` 주문번호뿐 아니라 `B...` 등 알파벳 시작 주문번호도 수집하도록 일반화했다. `tests/unit/test_baemin_order_history_collector.py`에는 비-T 주문번호를 가진 fake Playwright page로 `collect_baemin_order_history()`를 직접 호출해 checkpoint와 저장 row를 검증하는 테스트를 추가했다.
- 검증: `.venv-playwright/bin/pytest -q tests/unit/test_baemin_order_history_collector.py tests/unit/test_baemin_review_collector.py tests/unit/test_baemin_ads_collector.py` 결과 9 passed. `.venv-playwright/bin/pytest -q tests/unit/test_yeoljeong_finance_service.py` 결과 118 passed. `.venv-playwright/bin/pytest -q tests/unit/test_yeoljeong_finance_api.py` 결과 23 passed. `.venv-playwright/bin/python -m py_compile app/services/baemin_order_history_collector.py app/services/baemin_review_collector.py app/services/baemin_ads_collector.py app/services/yeoljeong_finance_service.py app/api/yeoljeong_finance.py scripts/trigger_delivery_sync.py` 성공.
- 남은 이슈: 실제 배민 PC Agent 로그인 세션을 사용한 운영 E2E 백필 실행은 아직 수행하지 않았다. 배포/재시작/푸시도 CEO 명시 승인 대상이라 이번 턴에서는 수행하지 않았다.
- 배포/커밋: 커밋, 푸시, 배포 없음. 기존 작업트리 dirty 변경은 보존했다.

## 2026-08-25 13:39 KST - FOOD 배민 자동수집 API/CLI full_backfill 계약 보강

- 요청: 배민 데이터 자동수집을 빠르게 구현 완료.
- 발견: `scripts/trigger_delivery_sync.py`는 `--mode full_backfill`을 만들고 있었지만, 운영 API `SyncPayload`가 `mode`, `all_businesses`, `max_orders`, `max_reviews`, `checkpoint`를 선언하지 않아 `/api/v1/yeoljeong-finance/sync` 경유 실행에서는 해당 옵션이 Pydantic 모델에서 버려질 수 있었다. 또한 canonical 운영 CLI `scripts/yeoljeong_auto_collect.py`도 full_backfill 옵션을 payload/child process argv에 전달하지 않았다.
- 조치: `app/api/yeoljeong_finance.py`의 `SyncPayload`에 배민 전체 백필과 PC Agent/은행 자동수집에서 쓰는 옵션 필드를 추가했다. `scripts/yeoljeong_auto_collect.py`에는 `--mode full_backfill`, `--max-orders`, `--max-reviews`를 추가하고 timeout child collector 실행에서도 옵션이 유지되도록 했다.
- 조치: `tests/unit/test_yeoljeong_finance_api.py`에 API가 full_backfill 옵션을 서비스 레이어로 보존하는 테스트를 추가했다. `tests/unit/test_yeoljeong_auto_collect.py`에는 payload와 child argv가 full_backfill 옵션을 유지하는 테스트를 추가했다. `tests/unit/test_yeoljeong_finance_service.py`에는 `sync_delivery()`가 배민 PC Agent 수집기로 full_backfill context를 전달하고 status payload에 checkpoint/metrics를 남기는 테스트를 추가했다.
- 검증: `.venv-playwright/bin/python -m pytest -q tests/unit/test_baemin_order_history_collector.py tests/unit/test_baemin_review_collector.py tests/unit/test_baemin_ads_collector.py tests/unit/test_yeoljeong_finance_api.py tests/unit/test_yeoljeong_finance_service.py tests/unit/test_yeoljeong_auto_collect.py` 결과 181 passed.
- 남은 이슈: 실제 배민 PC Agent 로그인 세션에서 운영 E2E 백필은 아직 실행하지 않았다. 배민 추가 인증/보안 차단 시 `portal_action_required`가 정상 반환되어 CEO PC에서 인증 후 재시도해야 한다.
- 배포/커밋: 커밋, 푸시, 배포 없음. 기존 FOOD/은행/문서 dirty 변경과 같은 파일에 섞여 있어 별도 커밋은 수행하지 않았다.

## 2026-08-26 10:29 KST - FOOD 배민 백필 stale/timeout 분리 및 자동 재시도 큐 보강

- 요청: 배민 4개 매장 전체 백필이 stale/timeout으로 멈추지 않도록 기준을 백필용으로 분리하고, 매장별·일자별·최대 주문 수 단위로 쪼개 재시도 큐가 계속 이어지게 보강. 광고 수집기는 별도 저장 경로가 0건이라 우선 보강.
- 조치: `yeoljeong_finance_service.py`에 `DELIVERY_BACKFILL_STALE_AFTER` 기본 60분, `DELIVERY_BACKFILL_MAX_ATTEMPTS` 기본 3회를 추가해 일반 수집 15분 stale 기준과 분리했다. full_backfill은 `window_days=1`, `max_backfill_runs=1`, `max_orders/max_reviews` 제한 단위로 `delivery_collection_status`에 queued row를 만들고, 완료 후 다음 날짜 또는 timeout/stale 재시도 row를 자동 생성한다.
- 조치: `main.py`의 배민 full_backfill 스케줄 기본값을 `max_orders=80`, `max_reviews=80`, `window_days=1`, `max_backfill_runs=1`로 낮췄다. `delivery_auto_collect_pc_agent_catchup`은 queued 백필이 있으면 실행하고 running일 때만 중복 실행을 막도록 수정했다.
- 조치: `baemin_order_history_collector.py`는 checkpoint `last_order_no` 이후 주문만 저장하도록 보강하고, 주문번호 segment가 이전 주문번호를 중복 포함하지 않도록 경계를 수정했다. `baemin_ads_collector.py`는 광고 URL 후보를 순차 시도하고 우리가게클릭/오픈리스트/울트라콜 상품명 기반 파싱과 diagnostics를 추가했다. `scripts/trigger_delivery_sync.py`는 `--window-days`, `--max-backfill-runs` 옵션을 받는다.
- 검증: `python3 -m py_compile app/services/yeoljeong_finance_service.py app/services/baemin_order_history_collector.py app/services/baemin_ads_collector.py app/api/yeoljeong_finance.py app/main.py scripts/trigger_delivery_sync.py` 성공. `pytest -q tests/unit/test_baemin_ads_collector.py tests/unit/test_baemin_order_history_collector.py tests/unit/test_yeoljeong_delivery_scheduler_contract.py` 결과 11 passed. 운영 컨테이너 의존성 기준 `/tmp/aads-check` 복사본에서 `python3 -m pytest -q ...` 결과 22 passed, 112 deselected. `git diff --check` 대상 파일 통과.
- 남은 이슈: 실제 배민 PC Agent 로그인 세션을 사용하는 운영 E2E 백필 실행은 아직 수행하지 않았다. 배포/재시작도 수행하지 않아 운영 프로세스에는 미반영 상태다.
- 배포/커밋: 이번 변경은 아직 커밋, 푸시, 배포하지 않았다. 기존 무관 dirty 변경 `docs/CHANGELOG-go100-direct.md`는 보존했다.

## 2026-08-26 13:53 KST - FOOD 배민 단일 매장 백필 재시도 scope 보정

- 요청: `delivery-sync-c0f9b8a45532` 추적 후 timeout이면 해당 매장만 `max_orders`를 더 작게 쪼개 재시도하는 다음 단계를 완료.
- 확인: DB `yeoljeong_delivery_collection_status`에서 `delivery-sync-c0f9b8a45532`는 성신여대점 2026-08-26 window가 succeeded였고, continuation인 성신여대점 2026-08-25 window가 `BACKGROUND_SYNC_STALE` failed로 정리됐다. 이후 `delivery-sync-3eae1d8c36d9` 재시도는 `--business-id biz-sungshin --branch 성신여대점` 의도였으나 서비스 내부 `full_backfill` scope 확장으로 4개 매장 queue/action_required row를 만들었다.
- 조치: `yeoljeong_finance_service.py`의 `_delivery_sync_scopes()`가 full_backfill이어도 `all/전체`가 아닌 명시 사업자·지점은 단일 scope만 반환하도록 수정했다. `tests/unit/test_yeoljeong_finance_service.py`에는 명시 매장 full_backfill은 단일 scope, `all/전체` full_backfill은 4개 매장 scope를 반환하는 계약을 분리했다.
- 검증: 호스트 `python3 -m py_compile app/services/yeoljeong_finance_service.py tests/unit/test_yeoljeong_finance_service.py` 성공. 운영 컨테이너 `python -m py_compile app/services/yeoljeong_finance_service.py scripts/trigger_delivery_sync.py scripts/yeoljeong_auto_collect.py tests/unit/test_yeoljeong_finance_service.py tests/unit/test_yeoljeong_auto_collect.py` 성공. 운영 컨테이너에서 `_delivery_sync_scopes()` 직접 실행 결과 명시 성신여대점은 `[('biz-sungshin', '성신여대점')]`, 전체 scope는 4개 매장을 반환했다.
- 검증 제한: 컨테이너 pytest는 `tests/`가 bind mount가 아니라 이미지 내부 구버전 테스트를 실행해 1개 실패했다. host pytest는 `fastapi/structlog` 미설치로 collection 실패했다. 따라서 실제 수정 테스트는 py_compile과 컨테이너 함수 계약 검증으로 대체했다.
- 배포/커밋: 이 항목은 커밋/푸시/배포 진행 대상으로 남긴다. 기존 무관 dirty 변경 `docs/CHANGELOG-direct-edit.md`, `docs/CHANGELOG-go100-direct.md`, `scripts/yeoljeong_auto_collect.py`, `tests/unit/test_yeoljeong_auto_collect.py`, `.bank_auto_collect.lock`은 보존한다.

## 2026-08-27 08:25 KST - FOOD 은행 PC Agent 전용 lock 배포 보정

- 요청: `runner-6c71a443` 승인 후 신한은행 은행수집 PC Agent 전용화와 CDP 재개 보강을 배포하고 검증.
- 조치: 러너 승인 후 `aads-server-green`이 active upstream으로 전환됐으나, 운영 회귀 테스트에서 배민 공유 work_key/보안차단/orphan tab cleanup 로직이 퇴행한 것을 확인했다. 은행 전용 lock 및 신한 resume 보강은 유지하고 `yeoljeong_finance_service.py`의 배민 보호 로직을 복구했다.
- 검증: 운영 이미지 의존성 + 현재 소스 bind mount 기준 `python -m pytest tests/unit/test_yeoljeong_delivery_scheduler_contract.py tests/unit/test_yeoljeong_auto_collect.py -q` 42 passed, `python -m pytest tests/unit/test_yeoljeong_finance_service.py tests/unit/test_yeoljeong_bank_browser_connector.py -q` 187 passed. 대상 파일 `py_compile`과 `git diff --check` 통과.
- 운영 상태: active port는 `8102`, active container는 `aads-server-green`, `/health`는 `status=ok`, `graph_ready=true`. PC Agent는 blue API `8100`에 2대 온라인이고 green local agent 목록은 아직 0건이라 은행 수집은 peer fallback 또는 명시 agent id로 실행해야 한다.
- 남은 이슈: 최근 1개월 신한 실수집 `imported_rows > 0` 검증은 배포 보정 커밋/재배포 후 다시 실행해야 한다. 기존 무관 dirty 변경과 원장 이벤트 파일은 커밋 대상에서 제외한다.

## 2026-08-27 11:17 KST - CHAT 완료 판정 및 완료 알림 중복 방지 보강

- 요청: 채팅 응답 완료 여부가 진행 로그를 완료로 오판하는 문제와 새로고침 후 완료 알림이 2~3회 반복되는 문제를 직접 구현으로 개선.
- 조치: `response_completion_contract.py`의 진행형 꼬리 판정 단어셋을 최종 저장 가드와 맞춰 `호출`, `우회`, `대조`, `비교`, `연결`, `재시도`, `정리` 등 운영 확인 표현도 최종 완료보고 미충족으로 잡도록 보강했다. 이에 따라 "DB 메시지와 대조하겠습니다" 같은 응답은 completed 저장 전 자동 이어쓰기/보존 대상으로 판정된다.
- 조치: `aads-dashboard/src/app/chat/page.tsx`에서 `completion_token` ack를 30분 TTL로 `localStorage`에 저장하고, 새로고침 후 `streaming-status` 호출에도 ack를 전달하도록 했다. React ref가 초기화되어도 같은 완료 토큰의 `just_completed`/토스트/음성 안내가 반복 발화되지 않게 했다.
- 검증: 운영 컨테이너 `python -m pytest -q tests/unit/test_response_completion_contract.py tests/unit/test_chat_service.py -q` 75 passed. 서버 `python3 -m py_compile app/services/response_completion_contract.py app/services/chat_service.py app/routers/chat.py` 성공. 대시보드 `npx eslint src/app/chat/page.tsx` 오류 0개, 기존 경고 20개. 대상 파일 `git diff --check` 통과.
- 남은 이슈: 브라우저 실제 새로고침 E2E는 배포 후 운영 URL에서 확인해야 한다. 서버 repo의 FOOD/GO100 관련 기존 dirty 파일은 이번 채팅 변경 커밋 대상에서 제외한다.

## 2026-08-27 11:26 KST - FOOD 배민 보안차단 hard-stop 및 45분 쿨다운 보강

- 요청: 배민 보안차단 발생 즉시 전체 배민 run을 hard-stop하고, 45분 쿨다운 동안 스케줄러가 배민을 다시 태우지 못하게 직접 구현.
- 조치: `yeoljeong_finance_service.py`에 `BAEMIN_SECURITY_BLOCK_CODES`, `BAEMIN_SECURITY_BLOCK_COOLDOWN_MINUTES=45` 계약과 `cooldown_until` 계산을 추가했다. 배민 수집 결과가 `BAEMIN_SECURITY_BLOCKED`, `PORTAL_BLOCKED`, `SECURITY_BLOCKED`이면 남은 배민 scope를 실행하지 않고 status diagnostics/event에 `hard_stop_remaining_baemin_scopes`, `cooldown_minutes`, `cooldown_until`을 기록한다.
- 스케줄러: `app/main.py`는 기존 `DEFAULT_BAEMIN_SECURITY_BLOCK_COOLDOWN_MINUTES = 45`와 `_delivery_auto_collect_security_block_cooldown_active()`로 최근 배민 차단 원장이 있으면 `delivery_auto_collect_skip: baemin_security_block_cooldown`을 기록하고 배민 서비스를 제외한다. 이번 검증에서 최근 차단은 active, 46분 경과 차단은 expired로 판정됨을 확인했다.
- 검증: `python3 -m py_compile app/services/yeoljeong_finance_service.py app/main.py tests/unit/test_yeoljeong_finance_service.py tests/unit/test_yeoljeong_delivery_scheduler_contract.py` 성공. 운영 컨테이너 `python -m pytest tests/unit/test_yeoljeong_finance_service.py tests/unit/test_yeoljeong_delivery_scheduler_contract.py -q` 결과 140 passed. `/health`는 `status=ok`, `graph_ready=true`.
- 남은 이슈: 이번 턴에서는 운영 브라우저로 배민 보안차단을 재현하지 않았다. 실제 보안차단 재현 E2E는 배민 계정 차단 위험이 있어 API/단위 계약 검증으로 대체했다. 배포/재시작은 아직 수행하지 않았고, 기존 무관 dirty 파일은 보존했다.

## 2026-08-31 09:44 KST - CHAT 미완료 응답 premature completed 재방지 보강

- 요청: 응답이 실제 완료가 아닌데 completed로 닫히는 현상을 즉시 조치하고 결과 보고.
- 조치: `app/routers/chat.py`의 stale execution recovery가 최근 partial 또는 도구 진행이 있는 실행을 너무 빨리 recovered/completed로 정리하지 않도록 grace를 확대했다. 도구 진행 실행은 120초, 도구 없는 실행은 60초, empty no-runtime은 90초 이상일 때만 복구 정리 대상으로 본다.
- 조치: `app/services/chat_service.py`에서 heartbeat도 25초 간격으로 DB 중간 저장해 blue-green 배포 중 `updated_at`이 오래돼 보이는 문제를 줄였다. 메모리 streaming state의 orphan 완료 판정도 도구 진행 시 180초, 일반 실행 90초로 늘려 완료 신호가 먼저 나가지 않게 했다.
- 검증: 운영 컨테이너 소스에서 `recovery_grace`, heartbeat 저장, orphan threshold 반영을 확인했다. `python3 -m py_compile app/routers/chat.py app/services/chat_service.py tests/unit/test_tools_and_pipeline.py` 성공. 운영 이미지 의존성 + bind mount 기준 `pytest tests/unit/test_chat_service.py tests/unit/test_tools_and_pipeline.py -q` 결과 135 passed, 1 warning.
- 배포/커밋: 서버 코드 커밋 `6a69660f fix: prevent premature completion during tool execution`이 생성됐다. 본 HANDOVER와 회귀 테스트 기대값 보정은 후속 커밋으로 정리한다. 대시보드는 `d42481a88d90 fix(chat): require final message before completion ui`까지 배포됐으나 원격 push가 아직 남아 있어 후속 단계에서 정리한다.
