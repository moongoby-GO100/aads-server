# AAG V11-4 / AO4-B coverage evidence

- Baseline: `origin/main` at `5c8c0216f02eba19ee4ee9dcb28163c0354fcbe3`.
- Scope: authoritative AAG v2 brief, analyzer identity, inventory coverage,
  risk-weighted coverage, unobserved scope, miss-review action.
- Safety: `not_detected`, `outside_scope`, `partial`, `stale`, `fallback`,
  `mismatch`, and `truncated` all return `negative_assertion_allowed=false`
  and at least one required follow-up action.
- Golden set: 31 parameterized contract cases plus API integration cases.
- AO4-B boundary: this evidence expands #310 analysis only after a pinned
  snapshot shows complete inventory and risk coverage. Missing or partial
  coverage is a reason to inspect more code, never proof of no impact.
- Rollback: revert the V11-4 commit. The new read endpoint and service are
  additive and introduce no migration or blocking gate.
