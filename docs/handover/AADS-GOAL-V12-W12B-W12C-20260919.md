# AADS W-12b/W-12c handover

- Task: `AADS-GOAL-V12-W12B-W12C-20260919`
- Status: implementation and disposable-DB validation complete; independent review pending
- Commit: this document's Git commit (record the final SHA in DB handover)
- Production migration: 미적용

## Delivered

The additive migration completes the ten PRD 14.8 responsibilities. It hardens
`work_item_dependencies`, `work_item_evidence`, and `goal_workflow_outbox`, and
adds the seven missing canonical stores. It includes composite tenant/project
foreign keys, partial/idempotent uniqueness, monotonic kill-switch and lease
epochs, append-only ledgers, decision/hash/version diagnostics, safe masking
pointers, review separation/SLA/override fields, and execution reconciliation
states. Existing M12/M14 migration files were not edited.

ADR-021 selects PostgreSQL RLS. All ten stores are `FORCE ROW LEVEL SECURITY`
with missing-context default deny. Runtime identity must be a non-owner
`NOSUPERUSER NOBYPASSRLS` role separate from the migration role. Repositories
must set `app.current_tenant_id` transaction-locally before first access.

## Compatibility and rollback

The existing M14 outbox writer remains accepted: the trigger derives its payload
hash, sequence, and publish state. No existing rows are deleted or rewritten.
Rollback is intentionally non-destructive and application-first. The rollback
SQL retains schema, audit data, append-only triggers, and RLS; a later retirement
would require a separately approved archive migration.

## Verification evidence

- Focused pytest: 12 passed, 0 skipped.
- Disposable PostgreSQL integration: passed against a newly-created empty
  `goal_policy_test` database on PostgreSQL 15. It applied M12/M14 and W-12b,
  repeated W-12b, executed verification and non-destructive rollback SQL, raced
  uniqueness, and exercised all ten stores under a `NOSUPERUSER NOBYPASSRLS`
  role for tenant-A visibility, missing-context deny, tenant-B non-disclosure,
  and cross-tenant mutation `DELETE 0`.
- The integration harness contains no schema/table drop or truncate. It refuses
  a non-empty test database and strips only legacy idempotent trigger drops from
  the empty-database bootstrap; W-12b executes byte-for-byte.
- Python compile: passed for both added test modules.
- Diff whitespace check: passed.
- AAG baseline: 835 nodes, 1281 edges, 2 findings, no baseline increase.
- Pre-commit and repository commit hooks: required before push; never bypassed.
- Original Runner AI review was infrastructure-HOLD. A fresh independent review
  is required for the corrected candidate before milestone acceptance.
- Build: 승인 후 Runner 빌드 검증 대상.

## Remaining gate

The independent AI review and commit hooks must produce PASS before acceptance.
Production migration remains blocked until W-13 transaction-local tenant context
and W-14F runtime-role verification are integrated; enabling FORCE RLS before
those writers are ready would fail closed and interrupt governed writes.
