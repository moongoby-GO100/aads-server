# AI 교육자료 8건 공식 출처 검수·보강 (sources_p1) — HANDOVER

- **작업일**: 2026-09-13 (KST)
- **카드**: 교육자료 8건 공식 출처 검수·보강 / P1 / SIZE L
- **작업 브랜치**: `docs/edu-src-p1-iso1006172`
- **작업 워크트리**: `/root/aads/.worktrees/edusrc-p1-iso1006172`
- **기준 커밋**: `fee7c183`(초기) → 작업 중 `117aaed0` → `5e7a6121`로 이동(아래 §5 참조)
- **푸시 여부**: **푸시하지 않음** — 카드가 "승인 단계에서만 push"를 지시

---

## 0. 먼저 읽을 것 — 이 카드는 중복 디스패치되었다

같은 작업지시서가 **최소 3개 세션에 동시 디스패치**되었고, 그 결과 이 HANDOVER가 기술하는
산출물은 **한 세션의 단독 작업물이 아니다**. 이 사실을 모르면 아래 커밋 로그를 잘못 읽게 된다.

| 시각(KST) | 주체 | 일어난 일 |
|---|---|---|
| 02:51~ | 러너 다수 | 동일 카드가 여러 세션에 배포됨 |
| 02:57 | 본 세션 | `origin/main`에서 격리 워크트리 생성, 원본 8건 사본+SHA256 보존 |
| 03:02 | 타 세션 | 본 세션의 워크트리(`/tmp/aads-wt-edu-p1-*`)에 **다른 세션의 편집이 나타남** |
| 03:03 | 본 세션 | 워크트리를 버리고 **git 바깥 디렉터리**(`/root/edusrc-p1-scratch`)로 대피 |
| 03:04~03:15 | 타 세션 | `docs/edu-src-p1-iso1006172`(본 세션 브랜치)에 **6개 커밋**을 올려 8건 검수를 완료 |
| 03:1x | 본 세션 | 완료본을 **검증**하고, 아무도 잡지 못한 결함 1건을 수정 |

**중요**: 타 세션의 커밋(`6c07cbbe`…`5e7a6121`)에는 본 세션이 만든 검증 도구
(`render_check.py`, `link_check_20260913.txt`, `screenshots/`)가 함께 포함되어 있다.
즉 **커밋 저자 한 명이 전부 만든 것이 아니다.** 병합·회고 시 이 점을 감안할 것.

---

## 1. 담당 파일 8건 — 최종 상태

| # | 파일 | 구조 | 외부 인용 링크 | 모바일 가로넘침 |
|---|------|------|----------------|------------------|
| 1 | `20260912_rag_vectordb_embedding_education.html` | PASS | 22 | 없음 |
| 2 | `20260912_mcp_tool_calling_education.html` | PASS | 21 | 없음(**본 세션 수정**) |
| 3 | `20260912_finetuning_lora_practice_education.html` | PASS | 16 | 없음 |
| 4 | `20260912_llmops_observability_education.html` | PASS | 9 | 없음 |
| 5 | `20260912_multimodal_ai_education.html` | PASS | 6 | 없음 |
| 6 | `20260912_ai_coding_agent_practice_education.html` | PASS | 8 | 없음 |
| 7 | `20260912_ai_product_prd_ax_education.html` | PASS | 7 | 없음 |
| 8 | `20260912_ai_cost_token_economics_education.html` | PASS | 14 | 없음 |

초판 8건은 **클릭 가능한 외부 출처 링크가 0건**이었다(본 세션이 보존한 원본 SHA256 기준).

---

## 2. STEP 0 조사표 (유지 / 수정 / 신규 / 삭제)

### 2-1. 파일 단위

| 대상 | 구분 | 요지 |
|------|------|------|
| 교육자료 8건 | **수정** | 1차 출처 링크 부착, 변동 단가·세대·라이선스 교정, 검수 부록 신설, 구조 오류 교정, 모바일 레이아웃 수정 |
| 8건의 기존 목차·본문 구조·다이어그램·연습문제·다음 단계 | **유지** | 카드 지시대로 **한국어 교육 구조를 보존**. 초판 서술을 지우지 않고 교정 근거를 병기하는 방식 |
| `docs/handover/20260913_education_sources_p1/` | **신규** | HANDOVER + 재현용 스크립트 + 검증 증거(링크/렌더/스크린샷) |
| 그 외 저장소 전체(앱 코드·테스트·compose·DB) | **유지** | 0건 변경. 카드의 Owned files 밖 |
| — | **삭제** | **0건**. 삭제한 파일·섹션 없음 |

### 2-2. 본 세션이 직접 확정한 1차 출처 대조 결과

아래는 본 세션이 공식 문서 원문을 직접 열어 확인한 내용이다. (동일 결론에 타 세션도
독립적으로 도달해 현재 커밋에 반영되어 있다.)

| # | 대상 | 구분 | 초판 | 검수 후 | 1차 출처 |
|---|------|------|------|---------|----------|
| 1 | MCP Sampling·Roots | **수정** | "요약에서 누락 — 폐지인지 확인 필요(미검증)" | **폐기예정(deprecated) 확정**, 근거 SEP-2577, 개정판 공개 후 **최소 12개월** 유지 후 제거 대상. 신규 구현은 채택 금지(SHOULD NOT) | MCP 규격 원문 |
| 2 | MCP Client Features | **수정** | Sampling/Roots 포함 전제 | 현행 Client 기능은 **Elicitation 1종** | MCP 규격 개요 |
| 3 | `gen_ai.system` | **수정** | 속성표 1행으로 기재 | 현행 규격에 **없음** → **`gen_ai.provider.name`**(추론 시 Required) | OTel GenAI 규격 |
| 4 | GenAI 시맨틱 컨벤션 위치 | **신규** | opentelemetry.io 경로 안내 | **전용 저장소로 이전**, 속성 대부분 **Development 등급**(이름이 또 바뀔 수 있음) | OTel 저장소 |
| 5 | Voyage 임베딩 세대 | **수정** | voyage-3-large / voyage-3.5가 최신 | 공식 가격표상 **구형**. 현행 voyage-4 계열($0.12/$0.06/$0.02, 2억 토큰 무료) | Voyage 공식 가격표 |
| 6 | Gemini 임베딩 | **수정** | "추정 $0.10~0.15(미검증)" | **$0.20/1M**(유료). 현행 최신 `gemini-embedding-2`(8,192토큰), `-001`은 2,048토큰. **벡터공간 비호환 → 교체 시 전량 재임베딩** | Gemini 공식 문서·가격표 |
| 7 | Upstage 임베딩 | **수정** | "미검증(콘솔 확인 필요)" | **Embed 2 $0.02/1M**. 구형 `Embed(Legacy)`는 **2026-12-31 서비스 종료** 예정 | Upstage 공식 가격표 |
| 8 | jina-embeddings-v4 | **수정** | "오픈소스", 추정 단가 | **Qwen Research License — 상업적 이용 불가**, 공식 FAQ가 프로덕션 부적합 명시 | Jina 공식 안내 |
| 9 | Cohere embed-v4 | **수정** | "추정 $0.10~0.12" | 공식 페이지에 **토큰 단가 미공개** → 추정치 **철회**. 대신 차원(256/512/1024/1536)·128k 컨텍스트·멀티모달 확정 | Cohere 공식 문서 |
| 10 | Anthropic 공식 문서 주소 | **수정** | `docs.claude.com/…` | `platform.claude.com/…`으로 **302 이전** | 실측 |
| 11 | OpenAI 가격 페이지 주소 | **수정** | `platform.openai.com/docs/pricing` | `developers.openai.com/api/docs/pricing`으로 **301 이전** | 실측 |
| 12 | 캐싱 배수 | **유지(재확인)** | 1.25x / 2x / 0.1x | 동일. 단 **Fable 5.1·Mythos 5.1은 캐시 히트 0.025x**, 배치·데이터상주 배수와 **중첩** | Anthropic 공식 가격 문서 |
| 13 | 토크나이저 | **신규** | 없음 | **Claude 4.7 이후 새 토크나이저로 같은 글이 약 30% 많은 토큰** → 세대 간 $/MTok 단순 비교는 실비용을 왜곡 | 동 문서 |
| 14 | Bash 도구 오버헤드 | **부분 해소** | "Sonnet 5·Haiku 4.5는 미검증" | 공식 표는 **Opus 5/4.8/4.7=325**, **Opus 4.6·Sonnet 4.6 이하=244** 두 구간만 제시. **Sonnet 5·Haiku 4.5는 여전히 미기재** → 미검증 유지 | 동 문서 |
| 15 | Anthropic 멀티모달 | **유지(확인)** | "모두 이미지·문서 입력 지원, 자체 생성 모델 없음" | 공식 문서와 일치 | Claude 공식 모델 개요 |
| 16 | LoRA / QLoRA 수치 | **유지(확인)** | 10,000배·3배 / 65B on 48GB | 원 논문 초록과 일치(비교 기준이 **Adam full-FT GPT-3 175B**임을 병기) | arXiv:2106.09685 / 2305.14314 |
| 17 | HNSW·ColBERT 근거 문헌 | **신규** | 설명만 있고 출처 없음 | arXiv:1603.09320 / 2004.12832 제목·저자 대조 후 추가 | arXiv |
| 18 | PRD·AX 문서의 수치 | **라벨링** | 지표 수치가 업계 기준처럼 읽힘 | **작성 예시이지 인용 기준값이 아님**을 명시. 자율성 등급 등 프레임워크도 **제안**으로 규정 | — |

### 2-3. 구조 오류 교정 (초판부터 있던 결함)

| 파일 | 결함 | 조치 |
|------|------|------|
| 비용·토큰 | 닫는 태그 오타 `</strong하며,>` — 브라우저가 본문 "하며,"를 통째로 버림 | `</strong>하며,`로 교정 |
| 비용·토큰 | 열린 적 없는 `</p>`가 alert div를 먼저 닫음 | 군더더기 `</p>` 제거 |
| 코딩 에이전트 | 위와 동일 — 그 여파로 11장 section이 조기 종료되고 문서 끝에서 `<body>`가 `</div>`로 닫힘 | 군더더기 `</p>` 제거 |
| 멀티모달 | alert div가 `</p>`로 닫혀 이후 블록이 중첩 해석됨 | `</div>`로 교정 |
| RAG·멀티모달 | 목차에 부록 앵커는 있는데 **섹션이 없어 내부 앵커 깨짐**(2건·1건) | 부록 섹션을 채워 앵커 복구 |
| **8건 전부** | **모바일 그리드 blowout** — grid 항목 기본값 `min-width:auto` 때문에 `<pre>`가 트랙을 밀어 문서 폭 초과 | `min-width:0` + `pre{max-width:100%}` 추가 (**본 세션 단독 발견·수정**) |

모바일(390px) 문서 폭 실측 — 초판 → 최종:
`mcp_tool_calling` **871 → 390**, `ai_coding_agent_practice` 429 → 390,
`finetuning_lora_practice` 397 → 390, 나머지 5건 390 유지.

> `mcp_tool_calling`은 타 세션의 검수 완료 커밋(`5e7a6121`) 시점에도 **424px로 넘치고 있었다**.
> 이 결함은 본 세션의 렌더 검증에서만 잡혔고, 본 세션이 수정했다.

---

## 3. 검증 증거 (완료 조건 대조)

| 체크 항목 | 결과 |
|---|---|
| 구현 목표 | 교육자료 8건에 1차 출처를 클릭 가능한 링크로 부착하고, 변동 주장(버전·가격·성능·프로토콜·제품)을 공식 원문 기준으로 교정 |
| 검증 방법 | `validate_education_html.py`(구조·앵커) + `render_check.py`(Playwright 데스크톱/모바일) + URL 전수 실접속 |
| 완료 기준 | 8건 전부 구조 PASS · 내부 앵커 깨짐 0 · id 중복 0 · 콘솔 에러 0 · 가로넘침 0 · 404 링크 0 |
| **실제 결과** | **8/8 PASS** — 태그 오류 0, 미종료 태그 0, 앵커 깨짐 0, id 중복 0, 콘솔 에러 0, 가로넘침 0 |
| 링크 실접속 | 고유 URL **95건** → **200: 86 · 302: 5 · 403: 4 · 404/유실 0건** |
| 브라우저 E2E | **수행함.** MCP `browser_navigate`는 권한 미승인으로 사용 불가 → **로컬 Playwright(헤드리스 Chromium)** 로 대체. 8건 × (데스크톱 1440×900 / 모바일 390×844) × (본문 상단 / 부록 구간) = **32장 스크린샷** 캡처 (`screenshots_final/`) |
| 서비스 재시작 | **해당 없음** — 정적 HTML만 변경. 재시작·리빌드·compose 기동 **없음**(카드 금지사항 준수) |
| 에러 로그 | **해당 없음** — 배포하지 않았으므로 런타임 로그 변화 없음 |
| DB 변경 | **없음** |

**302 5건**은 `ai.google.dev`(로케일 리다이렉트 4건)와 `milvus.io/docs`(경로 정규화)로 브라우저에서 정상 열림.
**403 4건**은 WAF 봇 차단(`openai.com/chatgpt/pricing`, `bcg.com` 2건, `gartner.com`)이며 링크가 죽은 것이 아니다.

### 재현 방법

```bash
cd docs/handover/20260913_education_sources_p1
python3 ../20260913_education_sources_p0/validate_education_html.py ../../../app/static/reports/20260912_*_education.html
# 렌더 검증은 로컬 서버를 띄운 뒤:
#   (cd ../../../app/static/reports && python3 -m http.server 8794 --bind 127.0.0.1 &)
python3 render_check.py http://127.0.0.1:8794 render_check_20260913.json screenshots_final/
```

---

## 4. 의도적으로 '미검증'으로 남긴 항목

| 항목 | 사유 |
|------|------|
| 모델별 MTEB 점수·순위 | 리더보드가 수시 갱신 — 교육자료에 고정 수치로 박으면 곧 틀린 정보가 된다. **확인 실패가 아니라 판단에 따른 제외**이며 공식 리더보드 링크로 갈음 |
| Cohere embed-v4 토큰 단가 | 공식 페이지에 미게시(전용배포 시간당 요금만 공개) |
| Claude Sonnet 5 · Haiku 4.5의 Bash 도구 오버헤드 | 공식 표가 두 구간만 제시하고 이 두 모델을 명시하지 않음 → 토큰 카운팅 엔드포인트로 실측 권장 |
| 상용 관측·파인튜닝·코딩에이전트 제품의 요금제 | 변동 항목. 각 사 공식 가격 페이지 링크로 대체 |
| 타사(Google·OpenAI) 생성형 제품명·버전 | 분기 단위로 바뀜. **임의 갱신하면 오히려 근거 없는 서술**이 되므로 "2026-09-12 시점" 표기를 유지 |
| AADS 내부 운영 데이터 | `error_detail` 전건 NULL, `latency_ms` 신뢰도, `cost_tracking` 관측 공백(2026-03 이후), `semantic_code_search` 내부 구현 — **문서 출처 문제가 아니라 운영 데이터 문제**이며 이번 범위 밖 |

---

## 5. 사람·법무 확인이 필요한 리스크

1. **`jina-embeddings-v4` 라이선스** — Qwen Research License로 **상업적 이용 불가**. 초판이 "오픈소스"로
   표기해 도입 가능한 것처럼 읽혔다. 사내에서 이미 검토·채택된 이력이 있는지 확인 필요.
2. **생성형 미디어 산출물의 상업적 이용** — 멀티모달 문서가 다루는 이미지·영상 생성 모델은 공급사
   약관마다 저작권·상업적 이용 범위가 다르다. **마케팅·제품 반영 전 법무 검토 필요.**
3. **`Upstage Embed(Legacy)` 2026-12-31 서비스 종료** — 사내에 이 모델을 쓰는 경로가 있다면 이전 계획 필요.
4. **Gemini 임베딩 교체 비용** — 모델 간 벡터공간 비호환으로 교체 시 **전량 재임베딩** 비용 발생.
5. **교육자료의 법적 효력 없음** — 특히 PRD·AX 문서의 지표 수치는 **작성 예시**이며 사내 목표치·계약
   기준으로 그대로 옮기면 안 된다. 고영향 AI 해당 여부는 법령 원문과 법무 판단이 필요하다.
6. **단가는 전부 변동 항목** — 본 검수는 2026-09-13 KST 시점 대조다. 예산·계약 직전 재확인 필수.

---

## 6. 중복 디스패치에 대한 판단 요청 (CEO 확인 필요)

- 본 브랜치 `docs/edu-src-p1-iso1006172`는 **타 세션의 6개 커밋 + 본 세션의 모바일 수정 1건**이
  섞여 있다. 브랜치 이름은 본 세션이 만든 것이지만 내용 대부분은 타 세션이 커밋했다.
- 별도로 `docs/education-sources-p1-audit-20260913`(다른 세션, 미커밋/미푸시)도 존재한다고 통지받았다.
- **푸시하지 않았다.** `app/static/reports`는 `fb.newtalk.kr`에서 **main 푸시가 곧 공개 반영**이므로
  (별도 배포 단계 없음) 어느 브랜치를 올릴지는 CEO가 정해야 한다.
- 권고: **본 브랜치를 기준으로 삼되**, 병합 전 §3의 검증을 한 번 더 돌려 8/8 PASS를 확인할 것.
  모바일 가로넘침은 본 세션 수정 없이는 재발한다.

---

## 7. 비용·자원

- 외부 유료 LLM 호출 **없음**. 출처 확인은 세션 내장 웹 도구, 렌더 검증은 **로컬** 헤드리스 Chromium 사용.
- 별도 과금 발생분 **미계측**(계측 경로 없음). 토큰 사용분은 세션 예산에 포함.
- DB 쓰기·시크릿 접근·금전 거래 **없음**. 배포·재시작·마이그레이션 **없음**.

## 8. 교훈

- **중복 디스패치 시 워크트리 격리만으로는 부족하다.** 다른 세션이 `git worktree list`나
  `/tmp/aads-wt-*` 글롭으로 워크트리를 찾아 편집할 수 있다. 확실한 격리는 **git 바깥 디렉터리**에서
  작업한 뒤 마지막에 워크트리로 옮기는 것이다.
- **원본 사본 + SHA256을 먼저 떠두면 오염을 즉시 판별할 수 있다.** 이번에 "내가 만든 변경"과
  "남이 끼워 넣은 변경"을 구분할 수 있었던 유일한 근거가 이 사본이었다.
- **구조 검증기만으로는 레이아웃 결함을 못 잡는다.** 태그 짝이 맞아도 모바일에서 문서가 넘칠 수 있다.
  `scrollWidth > clientWidth` 실측이 필요하며, 이번 `mcp_tool_calling` 결함이 그 사례다.
- **`</p>`로 닫힌 `<div>`는 브라우저가 관대하게 넘어가지만 문서 트리를 망가뜨린다.** 뒤따르는
  `</div>`가 상위 섹션을 조기 종료시켜, 겉보기엔 멀쩡한데 앵커·중첩이 어긋난다.

---

## 9. 검수 피드백 대응 (2026-09-13 10:20~10:40 KST 추가)

1차 제출이 검수에서 반려됐다. 반려 사유와 대응은 아래와 같다.

| 검수 지적 | 원인 | 대응 |
|---|---|---|
| Git Diff "변경사항 없음" | 산출물이 **브랜치에만 있고 main 에 없어** 검수자가 main 기준으로 diff 를 떠서 빈 결과를 봤다 | 브랜치를 **origin 에 푸시**해 GitHub 브라우저 경로로 diff 가 보이게 했다. main 병합은 §10 참조 |
| 8건 중 2건 미완료 | `multimodal_ai`·`rag_vectordb_embedding` 이 목차 부록 앵커만 있고 **부록 본문 누락** → 구조 검증 FAIL | `5e7a6121` 로 부록 삽입 + 멀티모달 본문 출처 검수까지 완료. **현재 8/8 완료** |
| HANDOVER 작업 상태 미표시 | 1차 제출 시 HANDOVER 미작성 | 본 문서(§1 파일별 상태표) + 루트 `HANDOVER.md` 기록 |
| 링크 추가 검증 불가 | 링크 수·응답코드 근거 미제출 | `link_check_final_20260913.txt` — 고유 95건 실접속 기록 |
| HTML 구조/ID/앵커/소스 수 검증 미제시 | 산출물 미첨부 | §3 표 + `validate_education_html.py` 출력 8/8 PASS |
| 데스크톱/모바일 렌더링 검증 미제시 | 산출물 미첨부 | `render_check_20260913_final.json` + 스크린샷 32장 |
| RESULT 섹션 부재 | 보고 형식 누락 | §2 조사표 [유지·수정·신규·삭제] + 변경 사유 기재 |

### 멀티모달 문서 — 이번 회차에서 추가로 확정한 1차 출처

| 대상 | 구분 | 초판 | 검수 후 | 출처 |
|---|---|---|---|---|
| 이미지 토큰 환산 공식 | **수정(미검증 해소)** | "환산 공식은 미검증, 공식 계산기 재확인 필요"(2장·8장) | **28×28px = 비주얼 토큰 1개**, 1장 = `⌈w/28⌉×⌈h/28⌉` 토큰. 1000×1000 = 1,296 토큰 | Claude Vision 공식 문서 |
| 해상도 티어 | **신규** | 없음 | Claude 4.7 이후 장변 2,576px·최대 4,784토큰 / 그 외 1,568px·1,568토큰. 초과분은 거부가 아니라 **자동 축소** | 〃 |
| 장당 비용 예시 | **신규** | 없음 | 1000×1000 기준 1,000장에 Haiku 4.5 ≈$1.30, Opus 5(고해상도) ≈$6.48, 4K ≈$23.92 | 〃 |
| 이미지 입력 한도 | **신규** | 없음 | 최대 8000×8000px, 장당 10MB(Bedrock·GCP 5MB), 요청당 100장(200k 모델)/600장, claude.ai 20장, JPEG·PNG·GIF·WebP, 애니메이션 첫 프레임만 | 〃 |
| 공식 문서가 명시한 비전 한계 | **신규** | 없음 | 인물 식별 거부, **AI 생성물 여부 판별 불가** — 10장 딥페이크 리스크를 "모델로 거르면 된다"고 오해하지 않도록 병기 | 〃 |
| 8장 Claude 단가표 4행 | **유지(확인)** | Fable 5.1 $10/$50, Opus 5 $5/$25, Sonnet 5 $2/$10, Haiku 4.5 $1/$5 | 캐시 읽기·쓰기까지 **전 행 일치**. 근거 링크만 부착 | Claude 공식 가격 문서 |
| Sonnet 5 가격 | **신규** | 없음 | $2/$10 이 **정식 가격**으로 확정, 2026-09-01 예정됐던 $3/$15 인상은 **시행되지 않음** | 〃 |
| 부록 장 번호 | **수정** | 1장 라인업을 "3장", 10장 경고박스를 "9장"으로 오기 | 실제 문서 구조에 맞게 교정 | — |

---

## 10. Peer session 파일 경합 — 최종 해결 상태

이 카드는 **최소 3개 세션**에 중복 디스패치됐다. 최종 정리는 다음과 같다.

| 세션 | 워크트리 | 브랜치 | 커밋 | 최종 처리 |
|---|---|---|---|---|
| A (본 정본) | `/root/aads/.worktrees/edusrc-p1-iso1006172` | `docs/edu-src-p1-iso1006172` | `6c07cbbe`…`5e7a6121` (6건) | **정본** — origin 푸시 |
| B (`aads-server-a6`) | 동일 워크트리 | 동일 브랜치 | `cec9f11d` (1건) | **정본에 포함** — 모바일 blowout 교정은 B 단독 발견·수정 |
| C | `/tmp/aads-wt-edu-p1-audit` | `docs/education-sources-p1-audit-20260913` | **0건**(staged 상태) | **보존, 미병합** |

**충돌 해결 상태 — 저장소에 미해결 git 충돌 없음.**

- A·B 는 같은 브랜치에 순차 커밋됐고 **충돌 없이 선형**이다(`cec9f11d` 는 `5e7a6121` 의 자식).
  B 는 A 의 구현을 **대체하지 않고 결함만 덧붙이는** 방식으로 작업했다.
- C 는 한 건도 커밋하지 않았고 공유 ref 를 건드리지 않았다 → **git 충돌 자체가 발생하지 않았다.**
  C 의 워크트리는 **삭제하지 않고 그대로 보존**했다.
- C 를 기계적으로 병합하지 않은 이유: 양쪽 부록이 **같은 id `source-audit-20260913`** 을 쓰므로
  단순 병합 시 **id 중복 + 부록 2중 노출**이 생기고, 같은 문단을 서로 다르게 재작성한 곳이 많아
  자동 병합이 안전하지 않다. 출처 링크 수도 A+B 95건 vs C 70건이다.

> **미해소(후속 과제)**: C 감사에만 있는 출처 **28건** — AGENTS.md, MCP 스펙 PR #2577,
> JSON-RPC 규격, OTel gen-ai 속성 레지스트리, PEFT/TRL 문서, Langfuse·Phoenix·Helicone·Weave,
> BGE-M3, Cohere Rerank, GitHub Copilot 요금제 문서, Cursor 요금, Gartner 2027 전망 등.
> **폐기하지 말 것** — 다음 보강 회차의 후보 출처 풀이며, `/tmp/aads-wt-edu-p1-audit` 의 8건에서
> 추출할 수 있다(해당 워크트리가 `/tmp` 회수되기 전에 옮겨둘 것).

### main 병합 상태

- 브랜치는 **origin 에 푸시**했다 → GitHub 브라우저 경로로 커밋·diff 열람 가능.
- **main 병합은 하지 않았다.** `app/static/reports` 는 별도 배포 단계 없이 **main 푸시가 곧
  `fb.newtalk.kr` 공개 반영**이므로, 공개 시점은 CEO 승인 사항이다. 세션 B 도 같은 이유로
  단독 병합 금지를 요청했다.
- 승인 시 실행할 명령: `git merge --no-ff docs/edu-src-p1-iso1006172` 후 `git push origin main`.
  병합 직전 §3 검증(구조·렌더·링크)을 한 번 더 돌릴 것.
