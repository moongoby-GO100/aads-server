# AI 교육자료 출처 검수 및 보강 (sources_p0) — 2차 보강 HANDOVER (PID 695508)

- **작업일시**: 2026-09-13 09:20~10:05 KST (서버 68)
- **base**: `48dc0f6d` (선행 에이전트 커밋 `12a891ff` 포함) — **선행 작업 위에 얹은 2차 보강**
- **작업 워크트리**: `/tmp/aads-wt-edu-src-695508` / 브랜치 `docs/education-sources-p0-20260913-p695508`
- **owned 파일 4종**: `20260912_{prompt_context_engineering, ai_security_governance, ai_evaluation_evals, ai_law_copyright_privacy}_education.html`

## 0. 직전 검수 지적에 대한 조치

| 지적 | 조치 |
|---|---|
| ① 변경사항 없음(Git Diff 공란) | 격리 워크트리에서 실제 편집·검증 후 **커밋 완료**. 4개 HTML + 증적 5종 변경 |
| ② 지시 위반 — 회피 | 4개 owned 파일을 직접 검수·보강함. 아래 2·3·4절이 실제 산출물 |
| ③ 증거 부족 — preflight만 제시 | 원문 문언 대조 10건, 링크 HTTP 전수 점검, 구조 검증, 렌더 스크린샷 16장 |
| ④ 지시 해석 오류 — 간섭 금지 조항 | 조항은 unrelated `runner-f69d78c9` 파일에 적용됨을 확인. owned 4종은 본 작업 대상으로 처리하고, 경합은 **격리 워크트리**로 해소 |
| ⑤ RESULT 미구성 | 1절에 유지/수정/신규/삭제 조사표 작성 |
| ⑥ 환경 변화 미활용 | HTTP 403 해소 상황을 이용해 **88개 링크 전수 HTTP 점검**을 실제로 수행 |

## 0-1. 선행 작업 보존 (중요)

작업 도중 선행 에이전트가 `origin/main`에 `12a891ff`를 올렸다. **덮어쓰지 않고 그 위에 다시 얹었다.**

- 내 최초 커밋(`ac06630c`, 원본 226c47a4 기준)은 폐기하지 않고 `backup/p695508-standalone` 브랜치로 보존.
- 선행본을 base로 재작업하면서, 선행 작업과 **중복되는 내 변경은 모두 버렸다**(법령 조문 교정, EU 연기 규정, MMLU-Pro 수치, 캐싱 배수 등은 선행본이 더 정확하므로 그대로 유지).
- 특히 내 최초본에는 "EU 고위험 규제 연기는 규정 원문 미반영"이라는 주석이 있었으나, 선행본이 제시한 **Regulation (EU) 2026/1744가 EUR-Lex에 실재함을 직접 확인**(HTTP 200, 제목 `Regulation - EU - 2026/1744`)했으므로 해당 주석을 **폐기**했다. 선행본 서술이 옳다.

## 1. STEP 0 조사표 — 유지 | 수정 | 신규 | 삭제

| 대상 | 분류 | 내용 |
|---|---|---|
| 선행 커밋 `12a891ff`의 모든 교정(법령 조문·EU 연기·벤치마크 수치·캐싱 배수·3사 정책) | **유지** | 한 줄도 되돌리지 않음 |
| 4종 본문 장 구성·앵커·목차·예시·도표·체크포인트·로드맵 | **유지** | 삭제·재작성 없음 |
| 4종 `<style>` | **수정** | 출처 표기 CSS 13줄 + 모바일 줄바꿈 규칙 1줄 추가 (기존 규칙 미변경) |
| 4종 목차 직후 | **신규** | 「출처 표기 범례」 블록 (검증/예시/제안/미검증 4분류) |
| evals 참고자료 표 | **신규 5행** | HELM 원 논문, MMLU 원 논문, τ-bench, TruthfulQA, NIST/SEMATECH 표본크기 |
| prompt 참고자료 표 | **신규 2행** | Lost in the Middle, RAG 원 논문 |
| security 참고자료 표 | **신규 3행** | MITRE ATLAS, 간접 프롬프트 인젝션 원 논문, OWASP GenAI LLM Top 10 본문 |
| law 참고자료 표 | **신규 3행** | 개인정보 보호법 원문, 저작권법 원문, 지식재산 기관 정정 |
| law 1장 EU 적용일 서술 | **수정** | 아래 2절 #2 |
| law 10장 출처줄 기관명 | **수정** | 아래 2절 #5 |
| law·evals 구조 결함 3건 | **수정** | 아래 3절 |
| 4종 모바일 가로 스크롤 | **수정** | 아래 3절 |
| **삭제** | **없음** | 문서 요소 삭제 0건 |

## 2. 원문 대조로 확인·정정한 주장

> URL 접속 확인은 내용 검증이 아니므로, 아래는 **원문을 내려받아 문언을 대조**한 결과다.

| # | 주장 | 판정 | 근거 |
|---|---|---|---|
| 1 | 한국 AI기본법 2026-01-22 시행 | ✅ 확인 | law.go.kr 제·개정문(lsiSeq=276241)에 「시행 2026. 1. 22」 |
| 2 | law 1장 "2026.8.2에 범용 AI(GPAI) 모델 규제 발효" | ❌ **정정** | EUR-Lex 제113조 원문상 GPAI(제5장)는 **2025-08-02** 적용(제101조만 제외), 2026-08-02은 일반 적용일 + 제101조 과징금 집행 개시. 선행본이 5장 연표는 고쳤으나 **1장 도입부는 옛 서술이 남아 있어** 연표와 불일치했다 → 본문 수정 + 원문 인용 박스 삽입 |
| 3 | security "과징금 3,500만 유로/7%, 1,500만 유로/3%" | ✅ 확인 | 제99조 3·4항 원문 문언 일치 (`EUR 35 000 000 … 7 %`, `EUR 15 000 000 … 3 %`) |
| 4 | 선행본의 "고위험 규제 2027-12-02 연기 = Regulation (EU) 2026/1744" | ✅ **선행본 확인** | EUR-Lex ELI 주소 HTTP 200, 문서 제목 `Regulation - EU - 2026/1744` 확인. 내 초안의 반대 주석은 폐기 |
| 5 | law "ipkorea.go.kr(특허청)" | ❌ **정정** | ipkorea.go.kr = **국가지식재산위원회**, 특허청은 현재 **지식재산처**(kipo.go.kr). 두 사이트 제목으로 확인 |
| 6 | arXiv 인용 논문 14편의 ID↔제목 일치 | ✅ 확인 | 전 건 abs 페이지 `<title>` 대조, 오기재 0건 |
| 7 | 인용 보도 4건(etnews·zdnet·impacton·aitimes) 실재성 | ✅ 확인 | 응답 페이지 제목이 인용 제목과 일치 |
| 8 | NIA 지원데스크 sw.or.kr/AI_act_helpdesk | ⚠️ 응답 없음 | 연결 실패. 링크로 승격하지 않음(선행본도 동일 처리) |

## 3. 구조·렌더링 검증 — 선행본에 남아 있던 결함 4건 수정

검증 도구 동봉: `validate_education_html.py` (태그 정합성·앵커·링크 HTTP 점검, 한글 경로 URL 인코딩 처리 포함).

| 결함 | 상태 |
|---|---|
| law 744·789행 — 여는 태그 없는 `</p>` 2건 | **수정**. 초판부터 있던 결함으로 선행본에도 잔존했음 |
| evals 822행 — 닫히지 않은 `<p>` 1건 | **수정**. 동일 |
| 모바일(390px) 가로 스크롤 | **수정**. law는 초판부터 31px, prompt는 **선행 커밋에서 새로 발생한 11px**. 원인은 긴 URL·경로 토큰이 줄바꿈되지 않아 `.alert` 폭을 넘긴 것 → `overflow-wrap:break-word` 규칙 추가로 4종 모두 **0px** |

검증 결과:

- **태그 정합성**: 4/4 PASS (선행본은 law·evals 2건 FAIL이었음)
- **내부 앵커** 66개 전부 대상 존재, 깨짐 0건
- **외부 링크 HTTP 전수 점검** (`link_check_20260913.txt`): 200 응답 58건. 그 외 — `pubmed` 203, `openai.com/enterprise-privacy` 403, `iso.org` 403, `ai.google.dev/gemini-api/terms` 302, `ifs.or.kr` 연결 오류. **403/30x는 봇 차단·리다이렉트이며 죽은 링크가 아니므로 교체하지 않았고, 접근 통제를 우회하지 않았다.**
- **렌더링**: Playwright desktop 1440×900 / mobile 390×844, 4종×2뷰포트 스크린샷 **16장** + `render_check_20260913.json`. 콘솔 오류 0건, 가로 스크롤 0px, 범례·배지 정상 렌더.
- 공개 경로(`fb.newtalk.kr/static/reports/*`) 확인은 선행 커밋 `48dc0f6d`의 실측 기록으로 갈음하고, 본 작업에서는 로컬 렌더만 수행했다.

## 4. 비용

외부 유료 LLM 호출 **0건**. 파일 편집 + HTTP 점검 + 로컬 Playwright 렌더로만 수행 → **외부 발생 비용 $0**. 지시서의 $5 중단 기준에 해당하지 않는다.

## 5. 병렬 작업 구분

작업 시점 동시 실행 중이던 동일 카드 작업자: `codex exec` PID 402892(`/tmp/aads-wt-runner-dfc22845`), `claude -p` PID 638746(`/tmp/aads-wt-edu-sources-p0`, → `12a891ff`의 저자로 추정), 본 검수 프롬프트의 동시 실행본 PID 692183/702421, 그리고 본 세션 PID 695508.

- 피어 워크트리·파일은 **쓰지 않았고**, 공용 워크트리 `/root/aads/aads-server`도 편집하지 않았다.
- 지시서의 간섭 금지 조항 대상인 `runner-f69d78c9`의 파일은 **일절 접촉하지 않았다.**
- 아직 작업 중인 codex 러너가 별도 결과를 낼 수 있으므로, **경쟁 산출물이 추가로 올라올 수 있다.**

## 6. 남은 과제

1. 기관 개별 문서 직링크 미확정 — NIA 가이드라인 2종, 저작권위원회 등록 안내서 PDF, 금융위 가이드라인 본문.
2. `ifs.or.kr` 보도 링크는 런타임에서 접속 불가 — 대체 출처 확보 또는 제거 판단 필요.
3. 나머지 교육자료 8종에도 「출처 표기 범례 + 1차 출처 링크 + 링크 점검 로그」 패턴 적용 가능(본 4종이 템플릿).
