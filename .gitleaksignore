# gitleaks 오탐 억제 목록 (fingerprint 1건당 1줄)
#
# 규칙: 시크릿이 아님을 확인한 건만 넣는다. 지문은 <파일>:<규칙>:<줄> 이라
# 줄이 밀리면 다시 막힌다 — 게이트가 다시 켜지는 쪽으로 틀리므로 그대로 둔다.
#
# 2026-09-18 — AAG 가 생성하는 mermaid 아키텍처 다이어그램에서, 매입자료 라우터의
# 노드 이름(모듈 경로와 라우트 네임스페이스를 이어 붙인 식별자)이 generic-api-key
# 로 잡혔다. 값이 아니라 경로 식별자이고 파일 전체가 자동 생성물이다.
# 주의: 이 파일에 원본 문자열을 그대로 옮겨 적으면 같은 오탐이 이 파일에서 다시 난다.
reports/aag/aads-arch.mmd:generic-api-key:77
