{
  "mcpServers": {
    "aads-tools": {
      "command": "docker",
      "args": [
        "exec", "-i",
        "-e", "AADS_SESSION_ID=",
        "aads-server",
        "python", "-m", "mcp_servers.aads_tools_bridge"
      ]
    }
  }
}
