# 브라우저 채팅 — 모델 정체·라우트 검증 (프로덕션)

**일시**: 2026-03-30 (화면 시각 KST)  
**URL**: `https://aads.newtalk.kr/chat`  
**세션**: AADS-007 (`1a281a16-...`)  
**계정**: 관리자 (로그인 후 검증)

## 요약

| 시험 | UI 선택 | 사용자 메시지 | 말풍선 메타 `[model]` | 비고 |
|------|---------|---------------|----------------------|------|
| A | Gemini 2.5 Flash | `[브라우저검증331-Gemini]` 백엔드 라우트 id 한 줄 영어 | `[gemini-2.5-flash]` | 본문은 self-eval/재시도로 `gemini-1.5-pro` 등 **오답·장문** 혼입 |
| B | Qwen3 32B (Groq) | `[브라우저검증331-Groq]` route model id 한 줄 | **`[gemini-2.5-flash]`** | UI는 Groq인데 **실제 호출은 Gemini 폴백**으로 추정 |

## 상세

### A — Gemini 2.5 Flash

- 콤보에서 **Gemini 2.5 Flash** 선택 후 전송·스트리밍 완료.
- 메타: **`[gemini-2.5-flash]`** (서버 기록 id와 일치).
- 본문: “이전 응답이 너무 짧다…” 등 **내부 재시도 문구**와 함께 `gemini-1.5-pro` 언급 — **자연어 본문만으로는 신뢰 불가**, 메타·라우트 id 우선.

### B — Qwen3 32B (Groq)

- 콤보에서 **Qwen3 32B** 선택 후 전송·스트리밍 완료.
- 메타: **`[gemini-2.5-flash]`** — 선택과 불일치.
- 코드상 Groq/LiteLLM 오류 시 `gemini-2.5-flash` 폴백(`model_selector.py`)과 부합 → **Groq 경로 실패 또는 한도** 가능성.

## 결론

1. **UI 콤보 선택은 API에 전달됨** (이전 단일 단어 스모크와 동일 흐름).
2. **정확한 “어떤 모델이 답했는지”는 말풍선 메타 `[...]`가 기준**; 본문의 모델명은 재시도·할루시네이션 가능.
3. **Groq 선택 시에도 메타가 `gemini-2.5-flash`면 폴백 발생** — 운영에서 GROQ 키·LiteLLM·RPM 확인 필요.

## 적용·배포

- 본 검증은 **프로덕션 UI**에서 수행 (코드 변경 없음).

## 후속

- [ ] Groq 실패 시 사용자에게 “Groq 실패 → Gemini 폴백” 한 줄 표시 검토.
- [ ] `aads_model_identity` 주입 후에도 self_eval 재시도가 모순 본문을 만드는지 점검.
