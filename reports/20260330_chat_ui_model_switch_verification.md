# 채팅 UI 모델 스위치 검증 보고 (프로덕션)

**일시**: 2026-03-30 (KST, 화면 표기 기준)  
**환경**: `https://aads.newtalk.kr/chat` (관리자 로그인 후 AADS-007 세션)

## 요약

상단 **모델 콤보박스**에서 선택한 모델이 실제 응답 메타 **`[model_id]`** 와 일치함을 UI에서 확인함. **Gemini·Groq(Qwen)** 모두 정상.

## 검증 절차·결과

| 순서 | UI 선택 | 사용자 메시지(요지) | 어시스턴트 본문 | 메타 표시 |
|-----|---------|---------------------|-----------------|-----------|
| 1 | Gemini 2.5 Flash | `[UI검증330]` 한 단어 영어로 `Gemini` | `Gemini` | `[gemini-2.5-flash]` |
| 2 | Qwen3 32B (Groq) | `[UI검증330-Groq]` 한 단어 영어로 `Groq` | `Groq` | `[groq-qwen3-32b]` |

- 스트리밍 완료 후 **전송** 버튼 복귀, 중지/중단 UI 해제 확인.
- 동일 세션 내 이전 메시지와 무관하게 **선택 모델이 그대로 반영**됨.

## 결론

채팅창에서의 **모델 스위치는 실사용 경로에서 적용됨** (Gemini·Groq 경로 모두 PASS).

## 적용·배포

- **코드 변경**: 없음 (검증만).
- **배포**: 해당 없음.

## 후속 체크

- [ ] Groq/외부 API 한도·에러 시 UI 메시지와 폴백 동작 점검 (별도 부하/장애 시나리오).
- [ ] 자동 라우팅 모드와 수동 선택 모드 혼용 시 기대 동작 문서화.
